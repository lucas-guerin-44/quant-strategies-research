# Research Notes — methodological lessons

**Scope**: cross-experiment lessons, methodological rules, and patterns that constrain how to design the NEXT thesis. Per-experiment state lives in [STATE.md](STATE.md) — this doc is intentionally scoped to *what we've learned*, not *what we've tried*.

If you want to know what experiments exist, their verdicts, and their headline numbers → [STATE.md](STATE.md).
If you want to know what to do BEFORE spinning up a new thesis → keep reading.

## How to use this file (query without reading it whole)

This is a **log**, not a document to read end-to-end (~90 lessons). To query it: find the relevant lesson number via the **flat index** (below) or the **thematic index** (after it), then ``grep '^<N>\.'`` to pull *only* that lesson's body. Numbers are **stable**; the body order is chronological, **not** numeric, so the index is the only way to jump by number.

## Lesson index (by number)

1. FX non-momentum factors are a graveyard 2015-26 (only slow momentum pays)
2. low correlation ≠ useful diversifier (needs positive standalone Sharpe)
3. negative IS→OOS degradation isn't good news; the honest estimate is IS
4. "different mechanic, same universe" = same bet (TSMOM↔XS-mom +0.69)
5. research→live haircut is confound-specific, not a generic multiplier
6. regime decay = REJECT even if it worked in its era (forward window is all that matters)
7. weight literature decay warnings heavily (recent number > canonical)
8. tiny active-return-over-cash can still be a useful diversifier
9. match filter speed to asset duration (vol-proportional lookback)
10. engine permutation test is meaningless for continuous-position strategies
11. extend the sample before accepting a marginal bootstrap
12. CFD intraday costs / tick-volume ≠ cash-equity
13. fade-variant null check on any signal-based intraday strategy
14. stops inside intrabar range = instant death
15. cross-instrument fade test separates signal from exit-structure artifact
16. recent-regime (holdout) sub-period is the honest deploy signal
17. retest under symmetric R:R before tombstoning on a fade-test
18. time-of-day exit as an alpha-discovery tool
19. refinement paths are instrument-specific
20. "marginal" strategies don't become non-marginal via refinement
21. QC Sharpe is risk-free-adjusted; research Sharpe is raw
22. CFD overnight/gap theses must be validated on real futures BEFORE refinement
23. pre-close MOC drift needs public imbalance publication + single-venue basket
24. generic intraday patterns get arbed on NDX M5; structural ToD can extract
25. "holdout is the best regime" is the strongest Phase-6 outcome
26. cost-insensitive vs cost-linear Sharpe is diagnostic (no-signal vs friction-eaten)
27. lunch fade is a structural exception, not a generic "fade overshoot" rule
28. holdout-decay magnitude as a 0DTE-amplification proxy
29. walk-forward Phase 6 surfaces failure modes single-split hides (TSMOM)
30. BTC institutionalization is a mirror-image driver across families
31. BTC deploy: W4 (post-institutionalization) Sharpe is the binding constraint
32. measure broker spread BEFORE locking cost-binding kill-criteria
33. Asian-session-handoff doesn't auto-transfer across 24/7 instruments
34. equity-index shorts structurally broken as a book hedge in the QE era
35. JPY-haven is regime-conditional, not structural (2018+)
36. hedge full-sample test fooled by single-regime fit; walk-forward mandatory
37. count the trading population before designing a complement strategy
38. direction hit-rate is the primary diagnostic for directional filters
39. sentiment intuition mirror-inverts on opening-impulse strategies
40. vol-target sizing isn't a robust Sharpe-lift for opening-impulse (variance reshuffle)
41. calendar blackouts don't move Sharpe unless variance-share ≫ day-share
42. monthly-OPEX pin-fade is dead on US indices post-0DTE
43. pre-committed fade direction inverts on post-2022 risk assets (named pattern)
44. single-stock earnings-fade arbed on Mag7, survives on low-0DTE-OI large-caps
45. retail vs institutional cost regime gates intraday-FX deployability
46. broker-history depth gates deploy, not just symbol existence
47. Mag7 earnings mechanism flips sign at 2022/23 (regime-conditional)
48. lunch-fade is index-arb-specific, not basket-generalizable
49. universe extension as a robustness check for XS D1 (24-name can be an artifact)
50. datalake silent-write failure under bulk ingest (200 OK, no persist)
51. lunch-fade is NDX/NQ-specific, not generic index-basis-arb
52. Hurst regime classifier asymmetric: useful for TSMOM, dead for MR/fade
53. regime-classifier overlay redundant when the primary signal is itself a regime filter
54. pre-FOMC drift is FOMC+US-equity-specific, not portable macro-event positioning
55. LONG-bias kill-criteria templates mis-evaluate asymmetric SHORT mechanisms
56. pre-NFP-NDX SHORT is the Friday exception in a LONG-bias macro-event family
57. OPEX-pin family fully tombstoned for US equities
58. PEAD direction-inversion is Mag7-specific, not market-wide
59. CFD swap-cost ceiling makes multi-day-hold equity strategies non-deployable on CFD
60. inv-vol dynamic-vs-static collapses on sparse weakly-corr books; deploy static quarterly
61. macro-event-drift doesn't generalise index→commodity-on-own-fundamental (flow must port, not calendar shape)
62. scheduled US-macro LONG drift is first-read / mid-month / mid-cycle specific (PCE falsifies broad #56)
63. absolute-ATR filter on a secular-drift instrument = year-filter, not vol-filter
64. TF-coarsening as a Phase-1.5 microstructure diagnostic
65. cross-asset event-flow extensions are magnitude-shadows of the primary, decay first
66. independent FX pre-CB flow only at no-equity-primary ∧ modal-non-event; carry-MAINTAIN
67. capacity-moat is a deploy-prerequisite, not a predictor
68. institutional-absence ≠ knowing the fill-flow; fill can sign-invert W2→W3
69. event-flow doesn't port across the temporal axis; Phase-0 magnitude gate catches it
70. academic FX direction priors can be regime-inverted; pre-commit catches the sign-flip
71. post-reform price-impact patterns default to fully-arbed at retail windows
72. structural-flow calendar audit is a productive idea-source
73. single-instrument retail-CFD TSMOM needs B&H + null-check Phase-0 gates
74. monetary-regime conditional inversion ×3 families; month-end needs a Fed-policy gate
75. structural-flow screen needs a 3× cost-floor ratio, not 1×
76. regime-gated v2 has a structural small-sample / deflated-Sharpe failure mode
77. same-bar look-ahead at entry is the highest-EV bug class; standard safeguards can't detect it
78. paired cross-venue experiments give sharper falsification; failure-shapes diverge
79. macro events: information-resolution (drift) vs supply-clearing (none); pre-tombstone the latter
80. MT5 MqlRates.time is broker-local-as-int, not UTC epoch; localize→convert (TZ-FIX)
81. stop-geometry guard: reject entries where the entry bar opens past the stop (phantom alpha)
82. four-direction screening pass — 3 generalizable rules
83. placebo-gap discriminates real sparse-event edge from bull-beta artifact (3rd phantom-alpha catch)
84. durable structural shorts are quarter-aligned forced-rebalance; sleeves diversify by jurisdiction
85. crypto funding-fade correctly signed but institutionalised to ~0 post-2022
86. a validated ensemble doesn't survive restriction to the broker-tradeable subset
87. forced-flow calendar mechanisms port by settlement-timing, not clock; event-vs-generic placebo

## Thematic index

Lessons cluster around a few load-bearing themes. Cross-references below are by ID (numbers are stable; chronological order in the body below is preserved for diff-friendliness).

- **Post-2022 0DTE-amplification killed mean-reversion / fade theses on US risk assets** — #1, #27, #28, #42, #44; meta-pattern at **#43**.
  - Lunch-fade (#25, #27) is the lone surviving exception — structural mid-day vacuum, calendar-orthogonal to 0DTE Friday-PM gamma.
  - For *any* future MR/fade thesis on post-2022 risk asset: read #43 first; pre-commit fade AND continuation as co-equal candidates.
- **Equity bear-hedge cluster (May-18, three attempts, all REJECT)** — #34, #35, #36. Conclusion: no Eightcap-tradeable hedge survives walk-forward; the fix is risk-budget-side, not hedge-side.
- **Holdout-is-the-honest-signal family** — #16, #25, #28, #29 (walk-forward Phase 6), #31 (BTC W4 floor).
- **CFD-vs-source-data integrity** — #12 (CFD-vs-cash-equity), #22 (CFD-vs-futures pre-Phase-2 gate), #46 (broker depth gates deploy).
- **Cost/friction diagnostics** — #6 (regime decay is still REJECT), #26 (cost-zero vs cost-linear), #32 (measure broker spread BEFORE pre-commit), #45 (retail-vs-institutional regime gates intraday FX).
- **Diversifier vs alpha confusion** — #2 (low corr ≠ diversifier), #4 (different mechanic same universe = same bet), #8 (small active return can still be a useful diversifier).
- **Asian-session-handoff family is driver-specific** — #33 (XAU / BTC / WTI mirror outcomes), with the categorization sub-rules.
- **Methodology / pre-commit hygiene** — #13 (fade-test null), #17 (symmetric R:R first), #20 (marginal-doesn't-improve via refinement), #29 (walk-forward replaces single-split for TSMOM), #37 (count-the-population first), #38 (hit-rate is primary diagnostic), #40 (year-by-year stability for vol-target), #41 (variance-share vs day-share for calendar overlays), #43 (pre-commit BOTH directions on risk-asset MR), **#64 (TF-coarsening as Phase-1.5 microstructure diagnostic — same wall-clock spec at next coarser TF; rules out M5/M15 autocorrelation artifacts at near-zero cost; companion to #13)**.
- **Single-stock vs index** — #5 (research-to-live haircut absolute not multiplicative), #44 (single-stock earnings-fade survives on lower-0DTE-OI large-caps).
- **Regime-classifier-as-gate** — #52 (Hurst-classifier asymmetric per-instrument), **#53 (overlays redundant when classifier correlates with primary signal — full Hurst-overlay-on-momentum family tombstoned via tsmom_hurst_gated REJECT; pre-commit corr(classifier, primary_signal) check before scaffolding any future overlay)**.
- **Macro-event-drift family is venue-specific** — #54 (pre-FOMC drift does NOT port to pre-ECB on GER40; W3 hiking-cycle regime sign-flips; pre-commit BOTH directions for any future "pre-{event}" thesis on a non-FOMC-and-non-US venue). **Validated first-application on pre_nfp_drift (2026-05-23)** — direction-TBD caught a deploy-grade SHORT that LONG-by-default would have rejected.
- **Filter-design pitfalls on trending instruments** — #63 (absolute-ATR or any absolute-vol-derived filter on a secular-drift instrument like XAU/BTC/NDX-tech is mechanically equivalent to a year-filter, not a vol-filter; use percentile-of-trailing-N or price-normalized vol instead).
- **Single-instrument TSMOM on retail-CFD post-2014 sample is passive-beta-with-extra-steps unless B&H + null-check both PASS** — **#73 (`gold_trend` REJECT; binding Phase 0 gate for any future single-instrument-TSMOM thesis: pre-commit Sh > B&H AND Sh − inverted-signal-LO Sh > +0.30. Companion to #29 — walk-forward alone is insufficient when the asset has a strong unconditional drift over the sample).**
- **Same-bar look-ahead at entry is the highest-EV bug class to audit on every new intraday strategy; existing null-check / cost-zero / WF safeguards CANNOT detect it** — #77 (`fra40_mid_morning_momentum`). Decision uses `close[N]`, entry at `open[N]` ⇒ 5-min leak; symmetric across momentum/fade so dir-gap stayed huge; uniform across time so WF stayed clean. Only soft signal: prior-violation (observed Sh 2.7× parent, 4-7× pre-committed prior). New rule: observed Sh > 2× prior-midpoint = mandatory code audit before verdict.
- **Paired cross-venue experiments at thesis-time give sharper mechanistic falsification than single-venue + post-hoc cross-checks; failure-shape divergence is itself the structural finding** — #78 (`eu_close_auction_fade`). FRA40 and GER40 both REJECT on the 17:30 CET post-auction FADE thesis but in distinct shapes: FRA40 sign-inverted continuation (cost-zero +0.27 MOMENTUM, Bogousslavsky-Muravyev consistent); GER40 zero-directional-content (cost-zero +0.05 both ways). Maps back to venue microstructure (Euronext gradual continuous-call vs Xetra random-end single-price). Also: 4th independent close-window REJECT on EU indices at retail M5 — tombstone any further close-window thesis on this venue/freq without (a) novel mechanism + (b) Phase 1 gross > 3× cost-floor (lesson #75).
- **Scheduled macro events split into INFORMATION-RESOLUTION vs SUPPLY-CLEARING families; only the former produces extractable equity drift. Pre-tombstone supply-clearing candidates without Phase 2** — #79 (`treasury_auction_drift`). Treasury coupon auctions decisively REJECT on both NDX/SPX (placebo > event, Phase 2 BTC z-score wrong-signed, cost-zero ann_Sh < 0.30). Auction is supply-clearing not info — reaction fully arbed sub-second. Q1-Q3 Phase 0 classification gate (published consensus? forward-looking quantity? `event_calendar` member?) pre-tombstones future EIA/CFTC/T-bill/RRP theses without Phase 1 cycles.
- **Kill-criteria templates must be mechanism-aware** — #55 (LONG-bias WR>55% template mis-rejects asymmetric-payoff SHORT mechanisms; for SHORTs use PF>1.3 + Sh>+0.30 + MDD<25% trio instead. Surfaced by pre_nfp_drift 7-of-8 PASS with only WR failing).
- **Macro-event-family on NDX is LONG-bias; NFP is the Friday-specific exception** — #56 (pre_cpi_drift PASS in LONG direction refutes the "hard-data → SHORT" framing; canonical rule is "scheduled US-macro events on NDX drift LONG in 24h-pre window"; NFP-SHORT is anomaly driven by Friday + first-read + bi-modal-bundled-data).
- **Portfolio overlays: static-vs-dynamic distinction collapses on weakly-correlated sparse-event books** — #60 (`portfolio_risk_parity` PASS Sh lift +0.21, but rolling 60d vol falls back to full-sample on 96/100 rebals for 4-of-7 sparse strategies; deploy as quarterly EA sizing review, not pipelined rebal; pre-commit the static-vs-dynamic check on any future overlay).
- **Institutional-absence / capacity-moat framing is necessary but not sufficient — four sub-failure shapes** — #67 (magnitude-failure, `cfd_wed_rollover_eurusd`), #68 (regime-dependent fill-flow / W2-pass-W3-fail-sign-inversion, `retail_overshoot_fade`), paired with #51 (mechanism-by-analogy fails at price-impact-channel level, `single_stock_lunch_fade`), and **#69 (temporal-axis non-portability / W1→W3-monotonic-decay-to-noise, `post_fomc_amateur_fade`)**. For any future "institutionally invisible" thesis, pre-specify the fill-flow shape (MR / momentum / regime-conditional / temporal-decay / none) against these four before Phase 2.
- **Phase 0 magnitude gates are the cheapest event-driven falsification primitive** — #69. Any thesis whose Phase 2 mechanism requires directional reversion/continuation at a fixed time-horizon should ship a Phase 0 gate set to "gross full-sample mean per-trade ≥ realistic-RT-cost on the candidate venue." Validated 2026-05-26 on `post_fomc_amateur_fade` (~30 min REJECT vs multi-hour Phase 2 saved). Also #70 (`usdjpy_tokyo_fix`) for the related case where Phase 0b catches a direction-inverted academic prior before post-hoc curve-fitting.
- **Academic-flow direction priors on FX can be REGIME-INVERTED — extend #43 from US-risk-asset MR/fade to FX-microstructure-literature priors** — #70 (`usdjpy_tokyo_fix`). Pre-commit BOTH directions at Phase 0; if unconditional aggregate has |t|>3 AND literature direction has gross < cost floor, treat as inversion evidence, REJECT current experiment, file a v2 pre-commit with the data-supported direction. Residual high-t signal is informational, NOT a PASS.
- **ORB-family mechanism is VENUE-specific, not timeframe-specific — cost-favorable instruments without single-venue concentrated cash auction are pre-tombstoned regardless of TF** — #71 (`xau_ldn_orb_m1`). orb_dax PASSES at M5 on Xetra single-venue cash auction; the M1 XAU port at the lowest-cost retail instrument (Eightcap 0.34 bp spread, ~2.5× tighter cost-to-bar-range ratio than indices) fails decisively — full Sh -0.17, W3 -0.24, dir-gap +0.24 (below +0.40 bar), cost-zero gross +0.69 bp/trade = sample-variance-grade. The LBMA-bullion-dealer-desk-staffing-at-LDN-open analog is NOT a sufficient substitute for Xetra's literal 09:00 single-auction information release — XAU 24/7 OTC trades continuously, no information-resolution event exists at LDN cash open. Companion to lesson #15 (single-venue prerequisite) and lesson #-14 (session-portability rule): together they specify that ORB transplants are gated by (a) literal single-venue cash auction AND (b) C1 in-session vs off-session control showing Δ Sh > +0.40. **Rule**: pre-tombstone ORB-family proposals on XAU/BTC/FX/commodities (all 24/7 or fragmented-venue) regardless of cost favorability. Future ORB transplants must (i) name the single venue and its auction mechanism, AND (ii) ship Phase 0b C1 control results BEFORE writing the simulator. Cost analysis alone is necessary but not sufficient — went down this path with M1=64bp-per-bar vs cost=0.64bp RT looking favorable, but the mechanism was missing the load-bearing structural feature.
- **Lesson #-14 binding rule (off-session C1 control) validated second-use; promote to Phase 0b standard for ALL intraday-microstructure transplant theses** — #72 (`xau_ldn_orb_m1`). Second consecutive REJECT decided primarily by the in-session vs off-session zero-cost Sharpe delta failing to clear +0.40 (xau_ldn_am_fade implicit; xau_ldn_orb_m1 explicit: LDN-open +0.32 vs shift+12h +0.39, delta -0.06). Both experiments would have closed in ~30 min instead of 60-90 min had Phase 0b been run BEFORE the simulator was written. **Operational rule (now binding)**: for any intraday-microstructure thesis that proposes a candidate session window on an instrument with existing deployed siblings, Phase 0b C1 control is REQUIRED before any simulator code is written. If candidate-window zero-cost Sharpe − best-control-window zero-cost Sharpe < +0.40, REJECT before Phase 2. The C1 control is ~30 lines of code (run the same trigger logic with a session-shift parameter, compare zero-cost daily means) and catches mechanism-not-session-specific failures with 10× less work than a full simulator + kill-criteria run. Pairs with #64 (TF-coarsening Phase-1.5 diagnostic): both are cheap pre-Phase-2 falsifiers that should be standard tooling.

## Key lessons

1. **FX crosses 2015-2026 is a graveyard for non-momentum factors.** Carry, carry+trend, short-term mean reversion all produced negative Sharpe. Only slow momentum (XS-mom and TSMOM-LO) made money.

2. **Low correlation ≠ useful diversifier.** FX carry and FX MR both had correlation < 0.3 with XS-mom (mechanically independent), but negative Sharpe means blending them subtracts returns. Correlation alone doesn't justify inclusion; positive standalone Sharpe does.

3. **Negative IS-OOS degradation is not automatic good news.** Every strategy we tested had OOS Sharpe > IS Sharpe, driven by the 2023+ momentum regime. The honest long-run estimate is the IS number, not the OOS or full-sample.

4. **"Different mechanic, same universe" = same bet.** TSMOM and XS-mom are both momentum on the same instruments; correlation turned out to be +0.69 despite different math. To get real diversification, need either a different factor OR a different market (equity single-names, crypto perps, vol products).

5. **Research-to-live Sharpe degradation is CONFOUND-SPECIFIC, NOT a generic multiplier (rewritten 2026-05-22 — original framing was overgeneralization from a single data point).** The original 2026-05 framing — "plan for a 50-70% Sharpe haircut research → live" — was generalized from one experiment (`xs_momentum` QC port, research 0.92 → QC live 0.35) and has been **incorrectly propagating as a generic prior** into every subsequent strategy's expected-live-Sharpe section. The actual 0.57 absolute gap on xs_momentum had **three distinct, separable sources**:

   **(a) Universe-swap confound (~0.3 absolute Sh)**: research on Yahoo ETF D1 data, deploy on MT5 CFD universe. Different instruments (ETFs ≠ CFDs), different fundamental-data dependence, different liquidity profile, different overnight gap behavior.

   **(b) Cost-model gap (~0.10-0.15 absolute Sh)**: research at 0.01% commission (Yahoo D1 assumption), QC live at realistic ~5 bps RT.

   **(c) Sharpe-formula confound (~0.20-0.30 absolute Sh) — per lesson #21**: QC reports risk-free-rate-subtracted Sharpe; our research reports raw Sharpe. At ~4-5% rf 2022+, this alone subtracts ~0.2-0.3 absolute from a 5%/yr strategy. **Not a real edge degradation** — pure metric mismatch. Once both sides are compared on raw Sharpe (per the QC-equity-curve recomputation done 2026-04-19), the xs_momentum "haircut" shrinks meaningfully.

   (a) + (b) + (c) ≈ 0.5-0.7 absolute, matching the observed 0.57. **NONE of (a), (b), or (c) apply to the current 4-strategy book** (orb_dax, lunch_fade, xau_session, macro_drift), all of which:
   - Were researched on actual Eightcap CFD data (no universe swap → no confound a)
   - Used conservative cost models in research (typically 2× actual broker spread → confound b inverts; live is CHEAPER than research)
   - Compute Sharpe identically on research and live VPS reporting (confound c eliminated)

   **Corrected expectation for the current book: 10-25% RELATIVE haircut, probably under 0.20 absolute in most cases.** This is also consistent with how QC live haircuts work in published quant-fund letters when methodology is held constant across measurement.

   **What CAN still cause real Sharpe degradation live** (and where larger haircut priors ARE appropriate):
   - **Mechanism decay** (lesson #6, #28): the regime shifts post-validation. Real, observed on multiple repo strategies. Magnitude varies by mechanism.
   - **Sample-size variance at small live n**: at < 200 trades, σ(realized Sharpe) ≈ 0.7. A single year's "measured haircut" is dominated by sampling noise, not by underlying degradation.
   - **Operational drag**: slippage on stops, partial fills, broker re-quotes during illiquid moments. Typically 0.05-0.15 absolute Sh, less with sticky-order routing.
   - **Survivorship bias**: deploying strategies that *looked good* out of many tested. The 39:4 reject:deploy ratio in this repo helps but doesn't eliminate this.

   **Procedure for new deploys (replaces the old "plan for 50-70% haircut")**:
   1. Identify which of confounds (a)/(b)/(c) apply to THIS specific deploy. For the current pipeline (Eightcap research → Eightcap live), the answer is usually "none of them".
   2. Estimate operational drag from Phase 0 broker spread / fill behavior (lesson #32) — that's the relevant cost-model haircut.
   3. Treat first 12-24 months of live data as the empirical haircut measurement for THAT specific strategy on THIS account.
   4. Use the measured number going forward — not the xs_momentum-specific 0.57.

   **Addendum 2026-05-24 — event-driven multi-strategy books need a BIGGER haircut than the 10-25% relative for single-strategy clean deploys.** The 10-25% framing applies when (a) a single mechanism is being deployed (no selection bias), (b) the mechanism is regime-robust (pre/post sub-sample both work), and (c) the cost model is conservative. For an event-driven *book* assembled from 4+ candidates (FOMC + CPI + RS + NFP, after rejecting ECB + PPI), the realistic live haircut stacks:
   - **Selection bias from the 4-of-6 survivor pool**: -0.2 to -0.4 Sh
   - **Regime concentration on the 2 of 4 strategies (CPI, NFP) whose pre-2022 Sh is ~0**: -0.3 to -0.6 Sh per affected strategy
   - **Pre-event spread widening on real broker** (broker raises spread 5-10× of normal ~30s before scheduled high-impact macro releases — actual Eightcap behavior): -0.1 to -0.3 Sh
   - **Mechanism decay on the Lucca-Moench-family events** (academic-publication tail; original effect already 50% decayed per macro_drift W3→W4): -0.2 to -0.4 Sh

   Stacked, realistic event-book live Sh is **40-60% of research book Sh**, not the 10-25% haircut that applies to single-strategy intra-day mechanisms like orb_dax / lunch_fade. Per joint_book_audit.py 2026-05-24: research book Sh +3.2 (daily-aggregated, sparseness-inflated) → realistic live book Sh **+1.0 to +1.5**, not +2-3.

   **Discipline for event-driven books going forward**:
   - Per-event sizing: full risk on regime-robust events (pre-2022 Sh ≥ +0.30), half risk on regime-conditional events (pre-2022 Sh < +0.30).
   - Pre-commit a regime kill-switch: if NDX 60d realized vol < 12% AND Fed funds target unchanged for 6+ months, disable regime-conditional events.
   - Honest expected-live communication: lead with per-event Sh (~0.75) not annualized-by-sqrt(N) book Sh — the annualized number is mathematically correct but psychologically misleading for sparse-event strategies.
   - Per-event-class kill-trigger discipline (don't disable the whole book on one event-class failure).

   **Why this lesson was rewritten**: the original 50-70% framing was casting unjustified pessimism over every subsequent deploy. orb_dax has been live 30 days at apparent ~+2.5 Sharpe annualized (n=10, statistically meaningless but at least not failing the old "plan for 50% haircut" prediction by a worse-than-noise margin). The rewritten framing lets each new deploy be evaluated on its own empirical merits as live data accumulates, rather than anchoring against an irrelevant single-experiment number from a different research-to-live methodology. **Concretely: the appropriate expected-live-Sharpe haircut for the 4-strategy book is 10-25% relative, not 50-70%.**

6. **Regime decay ≠ bad strategy, but still a REJECT.** VIX term-structure VRP had Sharpe 1.14 in 2015-2017 (textbook VRP era), survived Volmageddon via backwardation filter, then died post-2022 (2024-2026 Sharpe -0.19). 0-DTE option flow + compressed VX curve appear to have thinned the underlying premium. Regime-dependent edges don't get a pass just because they "worked in the right era" — the forward-looking window is all that matters for deployment.

7. **Weight literature decay warnings heavily.** Equity pairs thesis cited the 1962-2002 Gatev/Goetzmann/Rouwenhorst paper AND the post-2002 decay research from Do & Faff (2010). Estimated Sharpe 0.4-0.7; actual -0.99. The decay warning deserved as much weight as the headline number — maybe more. When a mechanism's academic half-life has been measured, the recent number matters more than the canonical number. Mega-cap US pairs 2015-2026 is now a graveyard alongside FX non-momentum 2015-2026.

8. **"Active return over cash" can be tiny and the strategy still valuable as diversifier.** Treasury trend (IEF) gained +2.14%/yr vs BIL's +1.89%/yr — only +0.25%/yr active return. Standalone this is underwhelming. But it has Sharpe 0.54, MDD -9%, caught the 2022 bond crash (+1.41% vs TLT -29%), and has ≈ 0 correlation with XS-mom. Blended into a 70/30 XS-mom + IEF-trend book, you give up little return and gain meaningful DD-reduction in regimes where bonds work AND unchanged exposure when they don't. Diversifiers shouldn't be judged on standalone CAGR — they should be judged on whether they help the existing book in the regimes where the existing book hurts.

9. **Match filter speed to asset duration.** Same 252-day TSMOM signal on TLT (18y duration, ~15% vol) vs IEF (8y duration, ~7% vol) gave Sharpe 0.14 vs 0.54. TLT's vol creates 10-15% transitional drawdowns in the 6-month filter lag window that IEF's lower vol absorbs. When designing trend on a given instrument, the filter lookback must be proportional to the instrument's own vol — a rule that was implicit but not stated in the existing XS-mom / TSMOM work.

10. **The engine's permutation test is meaningless for continuous-position strategies.** `backtesting.statistics.permutation_test` shuffles the return series; permutation preserves mean and std exactly, so the null is mathematically identical to the observation. This works for discrete-trade strategies (flip trade PnL signs) but degenerates for continuous-weight strategies like TSMOM. For those, use a **position-shuffle** test: shuffle the daily weight series, preserve the actual return series, recompute P&L. This tests "does the timing of the position choices add value?" — the right null. Implemented inline in `experiments/treasury_trend/treasury_trend_validation.py`; consider promoting to `backtesting.statistics` if it comes up again.

11. **Extend the sample before accepting a marginal bootstrap result.** IEF Phase 3 bootstrap 95% CI on 11 years was [-0.028, +1.138] — a near-miss that would have tombstoned under strict reading. Extending to IEF's full 24-year history (2002-2026) moved the CI to [+0.26, +1.08] and raised Sharpe from 0.55 to 0.67. If the underlying instrument has earlier data and the mechanism is expected to work across pre-sample regimes, refetch before rejecting. An overfit strategy would LOSE Sharpe on extension; a real edge gains.

12. **CFD intraday costs + tick-volume ≠ cash-equity intraday.** ORB on SPX500 M5 2019-2026 failed at Sharpe -0.92 against Zarattini/Aziz (2023) reporting Sharpe 1.65-2.81 on QQQ. Key differences: (a) CFD spread ≈ 2× their commission assumption, (b) no real share volume for filters, (c) SPX500 is less momentum-driven than QQQ, (d) broader 30-min OR vs their 5-min first-bar OR. Published intraday results on cash equities do not port 1-1 to retail CFDs even when the mechanism nominally translates — friction + noise thresholds matter more than they do at daily cadence.

13. **Test the "fade" variant as a null check on any signal-based intraday strategy.** If both the signal and its mechanical opposite produce similar loss patterns (18% WR in both directions for ORB SPX500), the signal has no informational content — it is generating noise trades, not alpha trades. ~30 lines of code, saves days of tuning a dead mechanism.

14. **Stops inside typical intrabar range = instant death.** On SPX500 M5, a stop at 25-33% of OR width is inside typical single-bar noise envelope. Tightening stops dropped WR from 18% to 4%, far faster than it shrank avg loss. For any intraday strategy: measure intrabar range against planned stop distance BEFORE committing — if stop < 0.5 × intrabar ATR, expect >80% of trades to stop-out regardless of signal quality.

15. **Cross-instrument fade test separates signal from exit-structure artifact.** Running the same ORB mechanism on SPX500, NDX100, and GER40 exposed that GER40 had the highest absolute Sharpe (+0.38) but its fade variant was also positive (+0.34) — the edge was a structural R:R artifact (full-OR stop + EOD close = asymmetric book-keeping), not directional signal. NDX100 had weaker absolute Sharpe (+0.03) but the fade variant lost 0.46, proving real directional content. For cross-instrument strategies, the **fade-gap** (baseline Sharpe minus fade Sharpe) is a cleaner edge-quality indicator than absolute Sharpe. Numerically-best ≠ deployment-best.

16. **Recent-regime (holdout) sub-period is the honest deployment signal.** NDX100 baseline ORB had full-sample Sharpe +0.03, but the 2023-2026 holdout was the strongest window at +0.19. Adding trend-filter + tight-stop raised full-sample to +0.31 but made the holdout -0.19. When a "better" config has a WORSE holdout, the full-sample improvement is overfit noise, not real improvement. Prefer the baseline with the stronger holdout over the optimized variant with the weaker holdout — always.

17. **Don't tombstone based on fade-test alone — retest under symmetric R:R first.** GER40 ORB initially looked like a structural artifact (baseline Sharpe +0.38, fade +0.34, small gap). The refinement run exposed that the EOD-close exit was giving both directions the SAME R:R asymmetry (fixed loss cap + random EOD distribution), which inflated fade's Sharpe by coincidence. Under fixed 1:1 R:R exits, baseline Sh -0.24 vs fade -1.21, gap +0.97 — real directional signal. **Rule**: when baseline and fade converge under asymmetric exits, rerun both under SYMMETRIC R:R before concluding the signal is vestigial. EOD-exit with fixed-distance stop is itself a confound.

18. **Time-of-day exit as an alpha discovery tool.** GER40 ORB refinement: full-sample Sharpe at EOD exit was +0.38; replacing with T+180min exit jumped to +0.58 with tighter DD. The edge in opening-impulse momentum has a half-life of about 3 hours on DAX M5 — holding longer accumulates noise. For any intraday strategy where you're exiting at EOD "because that's what the literature does", test a T+60/120/180/240min sweep — often the edge is concentrated in early bars and diluted by holding till close.

19. **Refinement paths are instrument-specific, not universal.** The T+180min exit that unlocked GER40's edge (Sh +0.38 → +0.58) was tested on NDX100 and FAILED decisively — NDX100's optimum is actually the LONGEST hold (EOD-exit Sharpe +0.03 beats T+180 of -0.09). Under tight R:R exits on NDX100, fade-variant beats baseline (-0.17 gap at 1:1 R:R) — opposite of GER40 where baseline dominates. Different instruments have different post-breakout autocorrelation profiles (DAX has clean 3h opening-impulse momentum; NDX has weak full-session drift + short-term mean reversion). When investigating a new instrument, run the symmetric-R:R-plus-TOD-sweep diagnostic from scratch — do NOT assume the previous instrument's refinement ports over.

20. **"Marginal" strategies don't become non-marginal through refinement.** NDX100 baseline was always the ceiling, not a floor — every refinement variant produced lower regime-averaged Sharpe. Lead variants that looked good in 2019-20 collapsed in 2021-26. Holdout-degradation was the kill signal each time: the "improved" variants had worse 2023-2026 holdout Sharpe than the baseline they replaced. If refinement hurts the modern-regime sub-period while "improving" the full-sample number, you're overfitting early-sample regimes. Prefer baseline.

21. **QC's Sharpe is risk-free-adjusted; our research Sharpe is raw.** First observed on Treasury-trend QC port 2026-04-19: research Sharpe 0.42 (OOS 2015-2026), QC reported Sharpe -0.263 on same period. Strategy wasn't broken — QC's formula subtracts ~3% annual risk-free rate; our `annualized_sharpe()` does not. Raw Sharpe computed from the QC equity curve (CAGR 2.26% / annualized std 3.4% = 0.66) matched research 0.67 almost exactly. For every future Phase 8 backtest: **don't compare QC-reported Sharpe directly to research-reported Sharpe**. Either compute the raw version from QC's equity output, or add risk-free-subtraction to our research sim. The comparison we actually care about (edge after realistic costs) needs the same formula on both sides. Corollary: at high rf (~4-5% post-2022), any strategy returning < 5%/year will have negative QC-reported Sharpe even if positive-returning. This is structural, not pathological.

22. **CFD overnight/gap theses must be validated on real futures BEFORE refinement work.** DAX overnight drift looked like a +0.80 Sharpe research result on MT5 CFD; FDAX futures via QC gave Sh -0.34 — a complete inversion. The CFD vendor's overnight pricing carries broker-specific synthetic mark-to-market that does not exist in real futures. Before sinking refinement time into any CFD-data thesis whose mechanism centres on the overnight/auction transition, port the baseline to QC futures and confirm directional sign matches. Costs more time upfront, saves more downstream when the CFD artifact is exposed.

23. **Pre-close MOC drift requires public imbalance publication + single-venue concentrated basket.** Tested on NYSE (SPX), Nasdaq (NDX), and Xetra (DAX). DAX has no public imbalance feed during continuous trading — mechanism cannot operate; result sign-inverted. SPX has imbalance feed but basket is split NYSE/Nasdaq, so signal dilutes — no directional content. NDX satisfies both (dir-gap +0.74) but gross effect ~4-5 bps per trade is fully consumed by 1pt CFD round-trip. **Microstructure prerequisite framework** for any "public-info intraday drift" thesis: require (a) public publication during continuous trading AND (b) tradeable basket's constituents concentrated on that one venue. If either fails, don't even spin up the thesis at M5 CFD friction.

24. **Generic intraday patterns get arbed on NDX M5; time-of-day structural theses can extract edge.** Four generic-trigger NDX M5 strategies (ORB continuation, z-score fade, BB-band fade, pre-close MOC drift) all REJECT despite real directional content in null-check diagnostics. But the time-of-day-structural lunch-hour fade PASSES decisively (Sh +0.89, holdout +1.06, dir-gap +1.87, cost-insensitive). The difference: lunch fade fires only on outlier-magnitude days (filter selects ~28 days/year), per-trade gross is 70-80 bps which dwarfs 2-5 bps CFD spread. Friction is overcome by signal magnitude, not by signal frequency. **For future NDX M5 work**: don't propose another generic-pattern variant; do consider further time-of-day-structural theses (pre-FOMC drift, post-open vol-crunch fade, end-of-month rebalance, futures-roll-day microstructure).

25. **"Holdout is the best regime" is the strongest possible Phase-6 outcome.** Most repo strategies show post-2022 decay (best pre-2022, worst post). NDX lunch fade is the inverse: 2019-2020 +0.61, 2021-2022 +1.00, 2023-2026 holdout +1.06. The mechanism may have INTENSIFIED with 0DTE-options-related intraday chop. When a holdout outperforms in-sample, weight the result more, not less — the deploy decision is forward-looking and the holdout is the most recent forward-look you have.

26. **Cost-insensitivity Sharpe vs cost-linear Sharpe is diagnostic.** When cost drag is ~0.04 Sharpe per pt RT (NDX lunch fade), per-trade gross dwarfs friction and the edge is robust to retail spread variation. When drag is ~0.16 Sharpe per pt RT (SPX lunch fade) or higher (~2.3 Sh/pt on SPX pre-close), per-trade gross is small and the friction is the binding constraint. **Cost-zero Sharpe extrapolation** separates "no edge" (cost-zero ≈ 0) from "edge eaten by friction" (cost-zero >> 0, cost-linear collapse) — different mechanistic stories, both REJECTs at retail M5 CFD, but the diagnostic guides where to look next (lower-cost execution venue for the latter, different mechanism for the former).

27. **Lunch fade is a structural exception, not a generic "fade overshoot on indices" rule.** Three independent fade-thesis attempts on retail-M5 indices have now sign-inverted: DAX gap fade (continuation +0.12 / fade -1.04), DAX US-lead (continuation -0.07 / fade -0.48), VWAP fade NDX (continuation +0.13 / fade -0.51). The 11:30-13:30 ET lunch window's success at fading morning moves cannot be generalised to "late session" or "session deviation" frameworks. The mechanism that makes lunch-fade work is SPECIFICALLY the simultaneous NY/Chicago lunch + EU close = institutional flow vacuum, leaving HFT inventory rebalancing dominant (mean-reverting). Outside that exact confluence, the late-session US-index tape trends, doesn't revert — and the trend bias has intensified post-2022 with 0DTE-options gamma flow. For future "fade overshoot" theses: require explicit identification of the liquidity-vacuum mechanism, not just "price has deviated from some reference."

28. **Holdout regime decay magnitude as a 0DTE-amplification proxy.** Strategies that mean-revert intraday on US indices show severe holdout decay post-2022: VWAP fade NDX +0.06 (2019-20) → -1.27 (2023-26), VWAP fade SPX +0.06 → -1.41. The lunch-fade is the ONLY repo strategy where post-2022 INTENSIFIED the mechanism (+0.61 → +1.06). The diagnostic: if the mechanism predicts mean-reversion AND 2023-26 is the worst regime by >0.5 Sharpe vs 2019-20, expect 0DTE-options trend-amplification to be the killer. If 2023-26 is the BEST regime, the mechanism is robust to or aided by 0DTE microstructure.

31. **BTC deploy-discipline (2026-05-13): W4 (post-institutionalization) Sharpe must be the binding constraint on any new BTC daily thesis.** Three BTC theses closed on 2026-05-13 (btc_trend retired on decay; btc_weekend rejected — activation but full-sample MDD fails; btc_volbreak rejected — one-window wonder in W2 2020-2021). Together they map the institutionalization phase transition from three different mechanism families. The deploy-relevant question is no longer "what's the full-sample Sharpe?" — it's "does the mechanism work in the post-institutionalization regime (CME volume scaled, Q1 2024 spot ETFs, professional 24/7 desks)?". **Rule for future BTC daily theses**: pre-commit a W4 Sharpe floor (e.g., +0.30 at honest costs, after the 0.3-0.6 research-to-live haircut from lesson #5) as a binding kill criterion, set tight enough that a W2-only-driven full-sample pass cannot survive. Strategies whose edge is concentrated in 2018-2021 are not deployable, period — even if the mechanism is "real" in a backtest sense. This is the BTC-specific instance of the more general "holdout regime is the honest deployment signal" rule (lesson #16), tightened from "best regime is informative" to "post-institutionalization regime is binding".

30. **BTC institutionalization is a mirror-image driver across thesis families.** Same maturation of BTC market structure (CME futures volume scaling 2018+, Q1 2024 spot ETF approval, professional 24/7 desks) acts on different mechanisms in opposite directions:
    - **Slow trend continuation (btc_trend MH-TSMOM)**: institutional flow smooths trend persistence → W4 Sh +0.50 vs +1.38/+1.61 earlier; W4 Sharpe roughly halves. EDGE DEGRADES with institutionalization.
    - **Weekend microstructure asymmetry (btc_weekend continuation)**: institutional Monday re-engagement vs retail weekend tape CREATES the asymmetry → W1+W2 Sh -0.01/-0.17 (mechanism dormant) → W3+W4 +2.11/+1.81 (mechanism activated). EDGE ACTIVATES with institutionalization.
    Both observed 2026-05-13 on the same 8-year BTCUSD D1 window. The institutionalization narrative is no longer a single-thesis artifact — it's a structural property of post-2022 BTC corroborated by two independent strategy outcomes pointing opposite directions. **For future BTC theses**: pre-commit on which side of institutionalization the mechanism lives. Slow trend / momentum / autocorrelation theses inherit the decay; microstructure-asymmetry / counterparty-driven / 24/7-vs-5d theses inherit the activation. Cross-corollary: a btc_weekend variant filtered to post-2022 with explicit regime-detection on the activation side is a candidate Phase 1 thesis worth scoping, but only as a separate experiment with its own pre-committed criteria — not as a refinement of btc_weekend (per lumber_oats lesson).

29. **Walk-forward Phase 6 surfaces structural failure modes that single-split Phase 6 hides.** BTC trend single-split Phase 6 (IS 2018-2021 / OOS 2022-2025) failed degradation by 0.04 — a borderline miss that looked like split-lottery noise. Walk-forward across 5 rolling 3y-IS/2y-OOS splits showed mean degradation +0.548 with **two outlier splits (+1.70 and +1.13) both at parabola-top → V-bottom transitions** (2021→2022 and 2025→2026). Two independent observations of the same failure mode = structural weakness of slow-TSMOM (still long at the top, slow to flip on sharp reversals), not lottery. Plus the lowest IS Sharpe in the walk-forward (+0.37 in S4, IS 2021-2023) corroborated the regime-decay signal that Phase 4's W4 (+0.50 in 2024-2025) had flagged independently. **Rule for any TSMOM-family thesis going forward**: replace single-split Phase 6 with a walk-forward across ≥4 rolling splits before deploy. Single-split is too vulnerable to which best-friend / worst-enemy regime lands in IS. For BTC specifically, walk-forward was decisive in retiring the deploy candidate; deep-history extension to 2013-2017 was rejected because pre-2018 BTC is a different asset (no retail CFD market, $10-50M daily vol vs $30B today, retail-cypherpunk vs institutional-derivatives microstructure).

32. **Measure broker spread BEFORE locking deploy kill-criteria on a cost-binding thesis.** xau_session Phase 0 came within one step of "pre-commit deferred / maybe tombstone" because the working cost assumption (3-6 bp RT for retail XAUUSD CFD) had been carried forward from the BTC-CFD lit-cost framing. The actual Eightcap median spread on XAUUSD turned out to be **0.35 bp RT** (30-day MT5 M1 sample) — under 1 bp across every hour of the day including the deploy-relevant 23-08 UTC window. With Raw-account commission ~1.5 bp RT, all-in cost is ~2 bp, not 6 bp. The 10× cost-model overstatement turned a Sharpe ~+1.0 deploy candidate into a "cost-binding, maybe tombstone" framing. **Rule**: for any thesis where per-trade gross is < 0.10% (i.e., where cost matters), run the broker-spread-distribution check (`_check_xau_spread.py` template — MT5 `copy_rates_range` with `TIMEFRAME_M1`, the bar's `spread` column is broker-quoted spread per minute) BEFORE writing the Phase 1 pre-commit. Don't carry CFD-cost assumptions across asset classes — gold's spread structure is nothing like BTC's. Corollary: spreads on different instruments at the same broker can vary 10×; the lit-stated "retail CFD cost = N bps" is a useless reference without an actual-broker measurement.

33. **Asian-session-handoff family does NOT auto-transfer across 24/7 instruments (2026-05-16).** Same hour-window (Asian session ~22-08 UTC, 9h overnight hold), three 24/7 instruments, three structurally opposite post-2022 (W4) outcomes:

   - **XAU** (`xau_session`): W4 Sharpe **+1.23** — institutional Asian-OTC physical / sovereign reserve diversification / Indian-Chinese ETF inflow ACCELERATING. Mechanism activation-side of institutionalization.
   - **BTC** (`btc_intraday`): W4 binding Sharpe **+0.64** — Tokyo/HK/SGP institutional open + spot-ETF inflow PEAKED Q1 2024 with partial decay 2026Q1. Mechanism activation-then-partial-decay.
   - **WTI** (`wti_session`): W4 Sharpe **-0.58** (FULL +0.32, fade-gap +1.53 — real directional content) — overnight oil flow was retail-and-Asian-physical extractable pre-2024, REVERSED post-2024 as professional electronic flow took over. Mechanism decay-and-flip side.

   Walk-forward exposes the decay timing cleanly on WTI: IS Sharpe uniformly strong across all 5 splits (+0.62 → +1.37 → +1.33 → +1.28), OOS collapses the moment OOS includes 2024+ (S3 -0.25, S4 -1.65, S5 -0.60). Regime change, not fit-failure.

   **Implication for scoping**: pre-commit which side of "post-2022 institutionalization" the candidate driver lives on BEFORE writing a thesis doc. The hour-of-day signal is necessary but not sufficient — driver-specificity is the binding factor.

   - Assets with STRUCTURAL Asian-physical / sovereign-flow demand → expect XAU shape (activation):
     gold, possibly silver, agricultural softs, perhaps platinum/palladium.
   - Assets with PROFESSIONAL/ELECTRONIC dominated Asian-hours flow → expect WTI shape (reversal):
     FX-G10, crude oil, base metals (copper/nickel), short-end rates futures.
   - Crypto-likes → expect BTC shape (activation then partial decay): tied to spot-ETF inflow trajectory.

   **Rule**: don't propose another Asian-session 24/7 thesis without naming (a) the specific Asian-hours flow driver and (b) which side of post-2022 institutionalization it lives on. Generic "Asian session has drift" framings are now disproven across asset classes.

36. **Full-sample integration test for hedge assets is fooled by single-regime in-sample fitting — walk-forward is mandatory before deploying a "marginal" hedge (2026-05-18).** Third attempted bear-hedge (`usd_safe_haven`, short EURUSD/GBPUSD on SPX-drawdown trigger) produced 2022 standalone Sharpe +0.63 (PASS the binding test that JPY-hedge failed) and full-sample integration test improving book Calmar 1.18→1.20. Looked like a deployable MARGINAL. Walk-forward (train 2018-2022 / holdout 2023-2026) **inverted the conclusion entirely**: every hedge weight from 0.25 to 1.50 degrades holdout Calmar vs book-only (book-only OOS Calmar 2.55; hedge_w=0.5 OOS 2.26). Dynamic scaling rules (drawdown-binary, drawdown-proportional) also fail to beat book-only OOS. The integration-test improvement was 2022-in-sample fitting; once 2022 sits inside TRAIN and 2023+ is held out, the hedge is dead weight. **Rule**: for any "hedge" thesis where the deploy case rests on integration metrics (book Calmar improvement) rather than standalone Sharpe, the walk-forward holdout test is the binding criterion — full-sample integration is insufficient. Hedges have low standalone Sharpe by design, so single-regime in-sample fitting is much easier to do accidentally than for alpha strategies. Combined with #34/#35: across three independent attempted mechanism families on the same problem, none produced a hedge that survives walk-forward. The 2018-2026 window has only TWO real stress events (COVID 2020, 2022 bear), so any hedge tuned in-sample to capture even one of them will have N=1 sample-size overfitting risk that walk-forward exposes. **Implication**: a robust SPX-stress-triggered hedge from the Eightcap tradeable universe likely does not exist for the current macro regime. The fix is risk-budget-side (lower book exposure, hold cash buffer) not hedge-side.

35. **JPY-haven property is regime-conditional, not structural in 2018+ data (2026-05-18).** `fx_safe_haven` tested classical "long JPY in equity stress" via SPX-triggered shorts on NZDJPY/CADJPY/AUDJPY/USDJPY. Result: 2020-Q1 COVID stress Sharpe **+1.39** (textbook mechanism worked); 2022 equity-bear Sharpe **−0.91** (Fed-vs-BoJ rate divergence overwhelmed the carry-unwind flow; JPY-crosses RALLIED during the equity bear); 2024-2026 calm-regime drag −0.36 from secular JPY weakening. Direction null-gap −0.55 (same triggers, long-side profits). Mechanism partially works for one regime type but catastrophically fails in the other — same SPX-vol detection cannot distinguish liquidity-shock from rate-divergence stress. **Rule for future safe-haven theses**: in 2018+ data, condition on the rate-policy state (Fed/BoJ/ECB rate-diff direction), not just equity-vol state. The classical pre-2022 "equity stress => JPY rallies" intuition is no longer reliable enough to underwrite a book hedge. Combined with lesson #34, both attempted bear-hedge mechanism families (equity-short and FX-safe-haven) have produced INVERTED null-checks: at exactly the conditions designed to detect stress, the OPPOSITE side of the trade is profitable in modern data. This is a deep structural property of the post-2008 / post-2022 macro regime, not an experiment-level fluke.

34. **Equity-index shorts are structurally broken as a book hedge in the QE era (2026-05-18).** Two independent repo experiments now confirm: classical long/short TSMOM rejected on 24-instrument universe (shorts hurt 12 of 24 instruments); short_tsmom with explicit V-recovery defense + bear-regime gating rejected with null-check INVERTED (long-only same conditions Sh +0.64, short-only −0.25, direction-gap −0.89). The entry conditions designed to identify "sustained bear regime" (mom_6m<0, close<SMA200, drawdown_52w>10%) select for buy-the-dip EV in modern equity markets, not trend-continuation EV. Fed-put + central-bank support since 2008 has made every meaningful equity drawdown mean-reverting, not trend-continuing. Even 2022 — the strongest sustained-bear case in the 2018-2026 window — only produced Sharpe +0.28 in the short_tsmom mechanism. **Rule for future "bear hedge" theses**: the hedge for an all-long equity book CANNOT come from another equity exposure on the short side. It must come from a different asset class (FX safe-haven, gold, USD long, vol-spike instruments) or a different mechanism family entirely (carry unwind, options-flow-driven). Don't propose another equity-short variant — the structural EV is on the long side at exactly the conditions a "bear hedge" would key off.

45. **Retail vs institutional cost regime gates intraday FX deployability (2026-05-16).** The pan-USD-major hour-23 UTC SHORT signal — end-of-day USD bid into NY close, t-stats -3 to -12 across EURUSD / USDJPY / AUDUSD / NZDUSD / USDCAD on 6mo-8yr of data — is statistically rock-solid (t=-12.28 on 1944 EURUSD obs) but per-trade gross sits in the 1-3 bp window. At retail Eightcap cost (~3 bp RT) all variants FAIL even with filter overlays. At institutional prime-brokerage cost (0.5-1 bp RT) the same signal produces Sharpe +1.2 to +2.8 with capacity to scale across multiple pairs. Mechanism is documented in academic FX literature (Evans/Lyons on intraday flow; Melvin/Prins on WMR fix). **Rule**: any thesis where per-trade gross < 5 bp must verify the broker's actual all-in cost (spread + commission + realistic slippage) BEFORE Phase 1, and explicitly compare to research cost-model assumption. Theses that pass research-Sharpe + statistical significance but sit in the 0.5-3 bp gross window are RETAIL-UNDEPLOYABLE — flag them as "institutional-only" rather than tombstoning, since they may be re-runnable on a different account tier. fx_session is the canonical instance: real signal, real mechanism, real fund-pod-deployable, dead at retail. Don't waste Phase 1+ cycles on retail deploy of <5 bp gross theses without confirmed sub-1bp execution cost.

46. **Broker-history depth gates deploy, not just symbol existence.** Two cases now: `treasury_trend` blocked because Eightcap has no bond CFDs at all (asset absence), and `softs_ensemble` blocked because Eightcap's softs D1 history is only ~18 months — insufficient to revalidate a 12M-lookback TSMOM (1 burn-in period + ~6 months validation = statistically empty). **Before scoping a deploy path on Yahoo/Tiingo-researched data**, check BOTH (a) does the broker offer the symbol (`mt5_fetch.py --list-symbols --match X`), AND (b) how much history does the broker carry (`mt5_fetch.py --symbols X --timeframes D1 --from 2015-01-01 --dry-run` shows fetched-bar count). If depth < 2× the strategy's longest lookback, shelf — Yahoo-data-trusted deploys ignore lesson #22 and reproduce the `dax_overnight` CFD-vs-source-data risk. The user's framing on this (2026-05-13): "way too much work just to not be sure of the viability afterwards" — shelve early when the path requires faith.

37. **Count the trading population BEFORE designing a complement strategy (2026-05-21).** `orb_compression` was designed as a "trade the days the parent ORB is silent on" complement — premise was ~20% of GER40 days are inside-day (no close outside OR through 12:00 cutoff). Empirically the figure is **0.16% (3 days / 7.3y)** — the conflation was "no LONG triggered" (parent LONG-only fires ~80% of days) vs "no break in either direction" (which is what inside-day actually means and is empirically ~empty on a volatile index across a 2.5h M5 window). A 60-second `print(num_inside_days)` diagnostic before writing the simulator would have killed the thesis without any backtest. **Rule for any complement/inverse/silent-day strategy**: a `len(filtered_days)` count is the first thing to run, BEFORE writing the inner loop or kill criteria. If the population is < 5× the trade-floor target, the thesis is mechanically infeasible regardless of edge quality. Secondary lesson: parent-strategy reliability and complement-strategy viability are *inversely related* — the more reliable the parent's mechanism, the smaller (and noisier) the complement population. orb_dax's 99.6% break-rate by 12:00 is itself a finding (strong-form confirmation of Xetra-auction mechanism), not a problem to be worked around.

38. **Direction hit-rate is the primary diagnostic for any directional-filter experiment; full-sample Sharpe lift WITHOUT hit-rate lift is overfit until proven otherwise (2026-05-21).** `orb_directional_bias` tested 4 prior-day predictors (gap direction, prior close-in-range, SPX overnight, 5d momentum) against GER40 ORB first-break direction. **All predictors failed the hit-rate test** (best 51.1% vs 52.0% drift-uncorrected baseline). Yet several filter_long variants showed apparent full-sample Sharpe lifts of +0.10 to +0.31 over the parent's LONG-only +0.76. Every one of those lifts came from 2019-2020 in-sample fitting and **DEGRADED OR NOISE-MATCHED the parent's load-bearing 2023-2026 holdout +0.93** (best filter holdout +0.97, lift +0.04; worst filter holdout +0.78, drop -0.15). **Rule**: when a directional-filter improvement shows Sharpe lift without hit-rate lift, the lift is mechanically driven by per-trade-EV correlation that doesn't generalize. Run the hit-rate diagnostic FIRST; if it fails, treat any subsequent Sharpe lift as overfit pending walk-forward proof. The hit-rate test is the load-bearing primary diagnostic for any "predict which side" experiment; Sharpe is the confirmation, not the leading indicator. Corollary mechanistic finding: DAX first-break direction is genuinely unpredictable from prior-day-available info — the Xetra 09:00-09:30 auction process fully absorbs overnight info before the OR-break trigger fires, so signals computed at prior-close are stale. This complements the orb_compression finding (99.6% break-rate by 12:00) and the dax_overnight REJECT (CFD overnight Sharpe +0.80 was a vendor artifact; FDAX -0.34) — three independent experiments now confirm Xetra efficiency at the cash open.

40. **Vol-target sizing is NOT a robust Sharpe-lift mechanism for opening-impulse intraday strategies — it is variance reshuffling unless trade-day Sharpe covaries with realized vol (2026-05-22).** `orb_dax_voltarget_events` tested classical Moskowitz-Ooi-Pedersen-style inverse-realized-vol position sizing on the deployed GER40 ORB T+180 LONG-only. Full-sample lift was +0.121 Sharpe and MDD improved 3.5pp — looked clean — but year-by-year: VT HELPS in 4/8 years (2020 +0.237, 2021 +0.475, 2025 +0.171) and HURTS in 4/8 (2019 −0.448, 2022 −0.180, 2024 −0.213). The full-sample number is an average of opposite-signed regime contributions. Mechanistic reason: VT only adds Sharpe when trade-day Sharpe positively covaries with realized vol (the assumption that "high-vol days are whipsaw days"). For opening-impulse breakouts the covariance is roughly zero — high-vol days are often the alpha-bearing days (2020 COVID-recovery rally, 2022 vol regime). Trend-following's vol-target works because trend SIGNALS strengthen in high-vol regimes (MoP 2012); opening-impulse signals do not. **Rule for any future intraday strategy considering vol-target sizing**: pre-commit a year-by-year stability check (≥6/8 years non-negative lift), not just regime-window breakdown. Sharpe-arithmetic at full sample is a misleading aggregator when the underlying lift is regime-dependent. The MDD improvement is real (vol-target IS a risk-shaping tool); the Sharpe improvement is conditional and not portable.

41. **Calendar-correct event blackouts identify bad days but don't move annualized Sharpe unless variance-share is HIGHLY disproportionate to day-share (2026-05-22).** Same experiment's EB lever (skip FOMC + ECB + NFP days) showed event-only-null Sh +0.090 vs baseline +0.460 — a +0.370 null-gap confirming event days ARE genuinely worse per bar. Yet EB-alone moved annualized Sharpe by only −0.006. Reason: event days are ~14% of trade-days (28/year of 250), and their variance contribution is ~proportional (~14% of total bar variance, since DAX volatility doesn't cluster on FOMC/ECB/NFP days the way it does on actual earnings or BOJ days). Annualized-Sharpe arithmetic rewards gating ONLY when the gated days' variance share substantially exceeds their PnL share. **Rule for future calendar-filter overlays**: before pre-committing the variant, run the cheap diagnostic `(variance_in_gated_days / variance_in_all_days)` vs `(|PnL|_in_gated_days / |PnL|_in_all_days)`. If the ratio is < 1.5×, calendar gating cannot lift annualized Sharpe by enough to clear a +0.10 threshold no matter how clean the null-check. The right move under those conditions is *down-sizing* on event days (e.g., 0.5×) rather than skip, but that crosses into fitted-overlay territory — pre-commit the scale factor, don't sweep it. Combined with #39 (sentiment overlay also failed on orb_dax), this is now TWO independent overlay families that fail to lift orb_dax above its deployed +0.76 — increasing confidence that the baseline IS the ceiling for ORB_DAX as currently structured.

43. **"Pre-committed fade direction inverts on post-2022 risk assets" — graduating from anecdote to named pattern (2026-05-22).** `eth_btc_ratio_mr` REJECT joins `short_tsmom` (#34), `orb_dax_sentiment` (#39), and `opex_pin_fade` (#42) as the **fourth independent instance** of the same structural failure mode: a pre-committed MR / fade / "trade against the extreme" thesis on a post-2022 risk asset gets cleanly inverted by its own null-check, with the momentum/continuation direction winning at the same trigger conditions where the fade was hypothesized.

   Concrete inversions observed:
   - `short_tsmom` (equity bear-trigger SHORTS): same conditions long-side +0.64, short-side −0.25, gap −0.89.
   - `orb_dax_sentiment` (gate-risk-off-quintile LONGS): Q5 risk-on was the only LOSING bucket (−0.0026% avg); fade direction beat hypothesis by +0.10 but symmetric null only +0.10 (half the threshold).
   - `opex_pin_fade` (Friday-PM pin-fade on indices): both NDX/SPX dir-gap NEGATIVE (NDX −0.48, SPX −1.33); continuation direction is the live one.
   - `eth_btc_ratio_mr` (post-Merge ETH/BTC ratio MR at |z|≥2): momentum-null +0.190 beats baseline −0.235; gap −0.42 fully inverted. H1 "MR working" was actually a slow directional re-rating; H2 ratio kept making new lows (BTC spot-ETF institutionalization) and time-stopped longs lost −16% to −30% each.

   **Unifying mechanism**: assets/markets undergoing a structural regime transition (post-2008 QE for equity, post-0DTE for index intraday gamma, post-2022 ETF-flow institutionalization for crypto, post-Merge supply for ETH/BTC) develop **persistent flow imbalances that look like MR-tradable extremes but are actually unfinished directional re-pricing**. The lookback window (rolling-mean, expanding-quintile, 60d/90d/200d) tracks behind the regime change, so z-score extremes keep recurring in the same direction for entire quarters or years. Fading them is buying a falling knife / shorting a rising rocket.

   **Rule for future pre-commits on any risk-asset MR/fade thesis post-2022**:
   1. The pre-commit MUST include BOTH directions as candidates, not just one. Write the kill criteria such that "fade wins" PASSES and "momentum wins" also PASSES — pre-commit *which direction wins* should be the post-Phase-2 finding, not the pre-commit input.
   2. The acceptable verdict states become {FADE PASSES, MOMENTUM PASSES, BOTH FAIL}. Goalpost-moving (REJECT fade but adopt momentum post-hoc) is still disallowed; either run both as co-equal pre-commits, or accept that whichever side failed is rejected and the other side requires a separate fresh pre-commit on truly OOS data.
   3. **Population check on the post-regime-break window only**: the regime that matters is post-2022 (or post the candidate's relevant structural break). Sharpe lifted by a 2018-2021 sub-window that fails 2022-2026 is not a deploy candidate (lesson `btc_volbreak` already on this; this rule extends it from BTC to all risk assets).
   4. Sub-window stability check is now Phase 0 not Phase 2 — split the post-break window in half, both halves must have non-negative Sharpe in the candidate direction, otherwise the result is one-window-wonder by construction.

   This pattern is at the same level as lessons #1/#27/#34 in terms of constraining future thesis selection — don't propose another retail-CFD MR/fade thesis on a post-2022 risk asset without explicitly addressing how the proposed direction survives the inversion test that has now killed four predecessors.

42. **Monthly-OPEX pin-fade on US indices is dead post-0DTE (2026-05-22).** `opex_pin_fade` tested third-Friday pin-fade on NDX100 and SPX500 M5 (the highest-conviction venues for the academic pin literature — Ni/Pearson/Poteshman 2005, Golez/Jackwerth 2012). **Both instruments sign-inverted identically**: NDX fade Sh −0.25 / cont Sh +0.23 (dir-gap −0.48), SPX fade Sh −0.68 / cont Sh +0.64 (dir-gap −1.33). Pre-committed 0DTE-decay scenario realized cleanly — 2023-2026 holdout was the worst regime on both (NDX −0.87, SPX −1.01). The calendar lock is anti-load-bearing: OPEX-only LOSES to generic all-Friday fade by −0.34 (NDX) and −0.42 (SPX). Cost-insensitive on both, so it's a signal-driven loss, not a friction-eaten edge (lesson #26 diagnostic). **Mechanism**: 0DTE customer flow is net-long-calls → dealers structurally short gamma at aggregate-book level → delta hedging is trend-AMPLIFYING on Fridays, the dual of the pre-2022 monthly-OPEX-pin mechanism. **Combined with vwap_fade, eod_unwind, gap_continuation, lessons #1/#27/#28**: this is now FOUR independent intraday US-index MR theses that have sign-inverted in the post-2022 regime. The lunch_fade exception holds because its mechanism (11:30-13:30 ET institutional flow vacuum) is calendar-orthogonal to the Friday-PM trend-amplification window where 0DTE gamma dominates. **Rule for future US-index intraday MR theses**: if the mechanism produces a position during the last 2 hours of any US session (especially Friday), pre-commit to the continuation direction, not the fade direction. The structural-gamma sign has flipped. Corollary methodological lesson: **calendar-restricted populations make magnitude-thresholds miscalibrate** — `thr=0.5` (lunch_fade-tuned for 1885-day universe) leaves only 5-7 trades on an 85-OPEX-day universe; recalibrate filters against the subset's own ATR distribution, not the parent universe's.

39. **Sentiment intuition mirror-inverts on opening-impulse strategies (2026-05-21).** `orb_dax_sentiment` tested a 7-factor risk-on/risk-off composite (VIX, VIX term-structure, SPX overnight, DAX trend vs MA50/MA200, EURUSD, HYG; expanding-window z-scored) as a selectivity overlay on the deployed GER40 ORB T+180 LONG-only. The pre-committed hypothesis ("risk-on tape = better breakouts") **failed decisively**: `gate_q1` (skip risk-off bottom quintile) Sh +0.46 vs baseline +0.46 (delta +0.004); `size_q5` (2× on risk-on top quintile) Sh +0.34 (WORSE). The mirror direction was where the signal lived: top-quintile Q5 is the **only losing bucket** at per-trade level (avg -0.0026% on n=288 vs Q1 risk-off +0.0262% on n=293 vs mid +0.0238% on n=873). The mirror overlay (`inv_gate`, skip Q5) hit Sh +0.56 (+0.10 lift) — but its symmetric null-gap is only +0.10 (half the pre-committed +0.20 threshold), and the 2021-2022 vol regime catastrophically breaks under the hypothesized direction (Sh +0.12 baseline → **-0.46**) precisely because the high-VIX days were the alpha-bearing days in that window. **Three mechanistic candidates for the inversion**: (i) risk-on exhaustion at the Xetra open — overnight FDAX has already priced the up-move 22:00-08:00 Berlin, so the OR-break is buying the high; (ii) risk-off provides the true information-resolution event that ORB's opening-impulse thesis depends on; (iii) low-VIX days have smaller intraday ranges, tighter ORs, more easily noise-driven breakouts. **Rule for any "sentiment / regime / discretionary feel" overlay on an opening-impulse strategy**: the intuition "trade in calm conditions" is wrong-signed. Pre-commit the mirror direction, and require null-gap ≥ +0.20 — most candidate overlays will fail this even when per-quintile bucket means look promising, because the signal is small relative to per-trade variance. Layer 2 (LLM-scored headline sentiment) becomes interesting only if the mirror Layer 1 direction validates on PRE-COMMITTED 2026-Q3+ OOS data.

44. **Single-stock earnings-gap fade has been arbed on Mag7 but survives on lower-0DTE-OI large-caps (2026-05-22).** `earnings_fade` REJECT on the pre-committed 24-name universe (Mag7 + S&P large-caps), failing Phase 6 holdout (2023-2026 Sh −0.22 vs 2021-2022 +1.51, Δ −1.73 — canonical lesson #28 0DTE-amplification signature). Critically the direction null-check PASSES decisively (dir-gap +1.35, fade +0.37 vs cont −0.97), meaning the mechanism is real and the sign is correct — this is a regime-decay REJECT, not a no-signal REJECT.

   **The decisive diagnostic was a clean Mag7 vs non-Mag7 bifurcation in the holdout sub-sample**:

   | Sub-universe | n | Holdout Sharpe | WR | MDD |
   |---|---|---|---|---|
   | Mag7 (AAPL MSFT GOOGL AMZN META NVDA TSLA) | 84 | **−1.67** | 46.4% | −20.7% |
   | non-Mag7 (17 names: banks, payments, staples, health, energy, software ex-FAANG) | 147 | **+0.67** | 53.1% | −14.7% |

   Δ = +2.34 Sharpe between sub-universes on the same mechanism in the same regime. Within Mag7: TSLA −15.26% / NVDA −6.85% / MSFT −3.53% are the heaviest drags; AAPL +5.31% is the lone Mag7 winner. Within non-Mag7: CRM +7.52% / MA +6.25% / XOM +4.79% lead, only UNH (−5.15%) is a meaningful loser.

   **Mechanism**: post-2022, single-stock 0DTE/weekly-options open interest concentrated on Mag7 names (NVDA + TSLA dominate retail 0DTE flow; Goldman/JPMorgan strategy notes 2024-2025). Customer flow is structurally net-long-calls into earnings → dealers are aggregate short gamma at the underlying open → MM delta-hedging is trend-AMPLIFYING on the gap move, not pin-reverting. **Same dealer-short-gamma flow mechanism as `opex_pin_fade` (lesson #42)**, applied at the single-stock event level rather than the index Friday-PM level. The cross-strategy corroboration is now decisive: TWO independent single-mechanism families (index OPEX Fridays + single-stock earnings days) on retail-tradeable instruments BOTH show post-2022 sign-inversion at the names/dates with highest 0DTE concentration.

   Secondary findings:
   - **|gap| magnitude bucket** (holdout): mid-gap 2.5-5% still fades (+0.58 / +0.88 Sh); <2.5% inside friction; >5% are real fundamental moves that continue, not noise. The fade mechanism survives only on outlier-but-not-extreme magnitudes.
   - **LONG/SHORT asymmetry** (holdout): fading UP-gaps still mildly positive (SHORT-side +0.12); fading DOWN-gaps broken (LONG-side −0.58). Bad-earnings down-gaps now run all day on retail-FOMO + dealer-short-gamma flow. The 0DTE arb is asymmetric — puts are bid more aggressively pre-earnings → dealers shorter gamma on the downside → bigger hedge-into-move on a miss.
   - **Cost sensitivity** (Phase 0 confirmed 2 bp basket median spread on Eightcap) is irrelevant to the kill: at zero cost full-sample Sh is only +0.64, and the holdout would still be ≈ 0 at zero cost. This is a signal-decay REJECT, not a friction REJECT (lesson #26 diagnostic).

   **Rule for any future single-stock equity intraday-fade thesis post-2022**:
   1. Pre-commit a Mag7-vs-non-Mag7 split as a binding diagnostic, not a post-hoc dissection. Reject the thesis on the *Mag7-included* universe if it fails any pre-commit; the non-Mag7 sub-strategy is a SEPARATE pre-commit, not a fall-back variant.
   2. The 0DTE-OI ratio (e.g., 0DTE open interest / total OI on the underlying, monthly average) should be the universe-selection variable, not market cap or sector. Lower-0DTE-OI names = mechanism survives; higher = mechanism arbed.
   3. The pivot path here (`earnings_fade_nonmag7`) requires fresh pre-committed kill criteria + walk-forward validation, NOT a within-experiment refinement of `earnings_fade`. Per lesson #20 ("marginal strategies don't become non-marginal through refinement"), promoting the winning sub-variant of a failed parent is the canonical overfit pattern.
   4. **Always Phase-0 broker-spread the single-stock universe** — Eightcap stocks came in at 2 bp basket median (vs 5 bp lit assumption), validating Phase 0 lesson #32 once more.
   5. The mechanism may still work on European single-stocks (less concentrated 0DTE options market — single-stock options in Europe are mostly Eurex EUREX-OPT, more institutional, less retail 0DTE flow). Promising future thesis path.

47. **Mag7 single-stock earnings mechanism FLIPS SIGN at 2022/2023 boundary — direction is regime-conditional, not directional (2026-05-22).** `earnings_continuation_mag7` REJECT directly tested the lesson #43 / #44 follow-up: pivot the rejected earnings_fade Mag7 sub-failure into its mirror direction. Result is the cleanest single-experiment confirmation of lesson #43 yet — **same data, both directions fail at full-sample** (fade -0.18 / continuation -0.18, dir-gap -0.02). The signal is decisively present at the regime level (fade holdout -1.67, continuation holdout +0.78, Δ +2.45) but full-sample averages to neutral because 2021-2022 was the *anti-regime* (fade worked, continuation lost) and 2023-2026 is the *active regime* (continuation works, fade lost). Walk-forward exposes this cleanly: IS-on-anti-regime / OOS-on-active-regime = OOS Sh +0.63 / +2.27 (very strong); IS-straddling-boundary / OOS-mixed = -0.09. Mean OOS +0.94 PASSES +0.30 floor but min OOS -0.09 FAILS the no-negative-OOS pre-commit; baseline Sh -0.18 FAILS the +0.40 floor.

   **The regime-flip happened at the 0DTE-options-ramp**: CME 0DTE volume scaled ~10× from 2022 to 2024 (CME data). Pre-ramp single-stock earnings gaps faded (dealer inventory rebalance was the dominant flow). Post-ramp the same earnings-day flow is dominated by short-gamma dealer hedging that AMPLIFIES the gap move — fade becomes continuation. Per-ticker: TSLA cont Sh +2.31 (most-arbed Mag7 name); NVDA +0.40; MSFT +0.17 (the top single-stock 0DTE OI names). AAPL/AMZN/GOOGL/META negative on continuation (smaller relative 0DTE OI / more institutional flow share).

   **Rule for ANY post-2022 risk-asset directional thesis**:
   1. Pre-commit fade AND continuation as co-equal candidates, with the verdict states being {FADE PASSES, CONTINUATION PASSES, BOTH FAIL}. Per lesson #43 rule 1, this is now load-bearing — without it, the full-sample analysis can wash out a real regime-conditional signal.
   2. If full-sample Sharpe ≈ 0 for both directions but each direction has |Sh| > +0.5 in opposite regimes, the mechanism is **regime-conditional, not directional**, and deploying requires a regime-detection overlay (which itself is a separate pre-commit + walk-forward).
   3. Walk-forward with IS/OOS straddling the regime boundary will systematically fail — this is a FEATURE, not a noise pattern. Do not relax the walk-forward pre-commit to accept boundary-straddling failures.

48. **Lunch-fade mechanism is INDEX-ARB-SPECIFIC, not basket-generalizable (2026-05-22).** `single_stock_lunch_fade` REJECT decisive: deployed lunch_fade NDX mechanism (Sh +1.02 LONG, holdout +1.51) tested on its 24 constituent + adjacent large-cap names produced **zero positive-Sharpe tickers** (best: GOOGL Sh -0.07; worst: AMZN -1.40). Basket Sh -1.06 (cost=4bp), -0.26 (cost=0bp), holdout -1.26, walk-forward 3/3 OOS negative (mean -1.19). Direction null-gap -0.53 — sign-flipped vs the NDX parent.

   **Mechanistic refinement of lesson #27**: the NDX lunch-vacuum mechanism is driven by **cash-vs-NQ-futures basis-arb flow** during the 11:30-13:30 ET institutional vacuum, not by retail/institutional inventory rebalance at the per-stock level. Single-name futures (single-stock CFD/futures) are illiquid relative to E-mini NQ — so there is no equivalent basis-arb at the constituent level. The lunch-hour flow at single-stock level is dominated by idiosyncratic news flow, sector rotation, and gamma-hedging, all of which are sign-NEUTRAL-or-trending, not mean-reverting.

   Sharpens lesson #27 from "lunch fade is a structural exception" to "**lunch fade is an INDEX-CASH-vs-FUTURES-BASIS-ARB exception that does NOT generalize to constituents OR to single-name CFDs**". For any future "lunch fade on X" proposal, the gate is: **does X have a liquid cash-vs-futures basis-arb counterpart that compresses during 11:30-13:30 ET (or local equivalent)?** If yes (FDAX vs cash DAX during Berlin lunch; FESX vs cash EUSTX50; possibly NKY vs Nikkei futures during Tokyo lunch) — candidate thesis. If no (single names, niche commodity baskets, FX crosses) — mechanism-empty, don't propose.

   Combined finding from #1/#27/#48: the lunch_fade NDX result is now the ONLY surviving mean-reversion strategy on US equity intraday — and it survives for a specific, NON-PORTABLE microstructure reason. Future single-stock M5 mean-reversion theses should not invoke "the lunch_fade mechanism" as ex-ante justification.

49. **Universe extension as a binding robustness check for cross-sectional D1 strategies — 24-name walk-forward can be a small-sample artifact (2026-05-22).** `sector_rel_strength` mirror-direction finding was the strongest of the session on the original 24-name universe: full Sh +0.43, walk-forward 3/3 cells PASS, lead cell mean OOS **+1.09** (5× the +0.20 floor), dir-gap +1.02, sector-neutral, per-ticker diversified, cost-insensitive. Promoted to DEPLOY CANDIDATE pending universe-extension robustness check. Backfilled 77 additional S&P-100-style stocks via MT5 to the datalake (100 total names after dropping BRK.A — datalake's instrument-name validation rejects dots).

   **Extension refuted the finding decisively**:
   - Baseline mirror Sh **+0.43 → +0.24** (44% degradation just from widening)
   - Holdout 2023-2026 Sh **+0.32 → −0.01** (binding floor broken)
   - **All 5 tested walk-forward cells FAIL** (mean OOS range −0.49 to +0.04 vs +1.09 on 24 names)
   - Every cell shows the canonical curve-fit signature: positive IS (+0.06 to +1.31), negative OOS (−0.24 to −1.43)
   - Direction-gap +0.70 still positive (mechanism is real and sign-correct) but weak; edge has been mostly arbed at the broader large-cap level
   - The 24-name version got inflated walk-forward results from a few names with exceptional post-2022 reversion (NVDA, GOOGL, TSLA, CRM) that don't generalize to the broader universe

   **Rule for any cross-sectional D1 strategy on a curated universe**:
   - Pre-commit a universe-extension robustness test (e.g., 24 → 100 names) as a binding gate, NOT an optional follow-up. The small-universe walk-forward result is information; it is not the deploy-candidate evidence.
   - If the small-universe Sharpe is concentration-driven (e.g., 1-2 names dominate per-ticker breakdown, even when ranking sub-universes look diversified), expect substantial degradation on extension.
   - The IS-strong / OOS-negative-on-every-cell pattern is diagnostic of universe-overfit when it appears on the extension. Single-cell OOS failures are noise; **5/5 OOS failures across parameter cells are signal**.

   This is a corollary to lesson #20 ("marginal strategies don't become non-marginal through refinement") extended to universe-selection: **a winning sub-universe is exactly the canonical overfit pattern at the universe-selection level**, distinct from parameter-cell overfit but mechanically equivalent.

50. **Datalake silent-write failure under bulk ingest — 200 OK without persisting (2026-05-22).** When `_universe_extension.py` pushed 77 single-stock M5 CSVs (each ~5MB, ~89K rows) sequentially without inter-call delay, the server returned 200 OK + `{"status":"ok"}` JSON for all 77 — but the catalog showed only ~28 of them persisted afterward. Re-pushing the missing 73 names individually with `time.sleep(0.3)` between calls succeeded for 72/73 on first attempt; only `BRK.A` consistently failed with a clean 400 + `{"detail":"Invalid instrument name: must contain only alphanumeric characters, underscores, hyphens, and ampersands"}` (server rejects dot in ticker name).

   **Root cause likely**: server-side write-queue overflow under no-delay bulk POST. Server accepts the request, returns 200, but drops the persist downstream. Cannot reproduce with deliberate delays.

   **Rules / mitigations going forward**:
   - **Always verify ingest via `/catalog` after a bulk operation** — don't trust client-side `+N` success messages. Add a verification step to `mt5_fetch.py` / extension scripts that checks the catalog returned the expected instrument list before reporting "done".
   - **Add inter-call delay (≥ 0.3s) to bulk ingest scripts** as a defensive measure until the server's silent-drop is fixed.
   - **Server fix recommendation**: have `/ingest` endpoint return actual persisted-row-count (not just `"ok"`) so the client can detect partial writes. Or return 429 with backpressure when the queue is full.
   - **For dot-in-name tickers** (BRK.A, BRK.B, BF.B, etc.): the server validation is strict-by-design and breaks on these names. Either rename to underscore form (`BRK_A`) or skip these tickers from the universe. Document the limitation in the datalake API client.

51. **Lunch-fade is NDX/NQ-specific, not generic index-basis-arb (2026-05-22).** Direct sign-test of lesson #48's "future thesis path: lunch fade on FUTURES BASKETS (FDAX, FESX during EU lunch)" — REFUTED. `fdax_lunch_fade` REJECT decisive: GER40 baseline Sh -0.10, cost-zero Sh -0.04 (signal-absent), dir-gap -0.08 (no directional content), only 1/3 regimes positive (the smallest sample). Berlin-lunch (12:00-13:30 CET) does NOT replicate the NDX 11:30-13:30 ET vacuum despite FDAX being the world's #2 deep index futures market with comparable HFT basis-arb depth.

   **Sharpens lesson #48** from "index-cash-vs-futures-basis-arb-specific" to "**NDX/NQ midday-vacuum specific**". The basis-arb mechanism alone is insufficient — the **simultaneous NY-lunch + EU-close confluence at 11:30-13:30 ET appears to be load-bearing**. Berlin lunch has only ONE flow source removal (European desks); US is not yet open (15:30 CET), Asia closed. NDX lunch has TWO simultaneous flow removals which creates the vacuum-MR pressure that FDAX lacks.

   **Combined with single_stock_lunch_fade REJECT** (lesson #48), lunch_fade has now been tested on (a) NDX constituents — fail, (b) GER40/FDAX — fail. **Zero validated sibling mechanism in the repo.** The strategy is more fragile than the prior "structural exception" framing — if NDX/NQ market structure shifts further (additional 0DTE saturation, MM behavior change, NY-EU session-overlap dynamics), there is no fallback within the lunch-fade family.

   **Three mechanistic reasons lesson #48 doesn't generalize**:
   1. No simultaneous flow-removal during Berlin lunch (US not yet open at 12:00-13:30 CET; only one institutional cohort vacates, not two)
   2. FDAX/cash-DAX basis-arb depth, while deep, may not reach the HFT-density-per-unit-of-residual-flow threshold required to produce vacuum-MR pressure
   3. DAX morning impulse is structurally PERSISTENT (orb_dax LONG-only deploys *because* of this) — fading the morning move during lunch directly fights the same up-drift that orb_dax extracts as alpha; mechanism collision

   **Rule for any future "lunch fade on X" thesis**: the gate is no longer "does X have liquid cash-vs-futures basis-arb?" (FDAX has that). The gate is "**does X have SIMULTANEOUS removal of two distinct institutional flow cohorts during a defined midday window?**" Candidates that might still pass:
   - Nikkei lunch (12:30-13:30 JST has a real *trading halt + restart* — different mechanism entirely, not vacuum)
   - Hang Seng lunch (12:00-13:00 HKT has a halt) — different mechanism
   - No European candidate appears to have the required double-flow-removal property

   **Practical implication**: do not propose further lunch-fade variants without explicit identification of the simultaneous-double-vacuum mechanism. The basis-arb framing of lesson #48 was insufficient; the simultaneous-flow-removal framing of this lesson is the actual generalization rule.

52. **Hurst-exponent regime classifier is asymmetric across the strategy zoo — useful for TSMOM-family, dead for MR/fade-family (2026-05-23).** `regime_hurst_diagnostic` Phase 1 study computed rolling 252d DFA Hurst on 8 D1 instruments (SPX/NDX/GER/BTC/ETH/XAU/USO/EUR) and partitioned per-bar returns of a fixed naive-TSMOM probe (60d lookback, 1-bar hold) and a fixed naive-MR probe (z>1.5 fade on 20d, 5-day hold) by Hurst regime (TREND H>0.55, MR H<0.45, NEUTRAL between). Pre-committed pass bar: Δ Sharpe ≥ +0.30 in matched-vs-mismatched regime on ≥5/8 instruments.

   **TSMOM side**: PASS — 6 of 8 full-sample (SPX +1.32, NDX +1.16, GER +0.90, BTC +1.14, USO +1.20, EUR +0.36; ETH -1.29 and XAU -0.42 fail). Walk-forward stable — 3 of 5 post-2023-eligible instruments preserve the direction. Hurst label is a legitimate classifier for *when* trend works.

   **MR side**: FAIL — 4 of 8 full-sample, **0 of 5 post-2023-eligible**. The MR-side null-collapse is not a Hurst-classifier failure; it is the same post-2022 0DTE-amplification taking down MR independent of any regime label (the 4 sign-inversion REJECTs catalogued in lesson #43). No Hurst gate can rescue a mechanism that doesn't work in either bucket of the holdout sample.

   **Why the asymmetry matters for future thesis prioritization**:
   - **Hurst-as-MR-gate is now tombstoned at universe level**. Any future fade/MR proposal whose pitch includes "we'd add a Hurst regime filter" is pre-rejected — the regime label does not separate post-2023 MR sign-flip from genuine MR opportunity. Rescue path is closed.
   - **Hurst-as-TSMOM-gate is alive and worth a Phase 2** — proposed follow-up `tsmom_hurst_gated`: re-run the existing `tsmom` baseline (currently KEEP_FOR_REFERENCE at Sh 0.40, +0.69 corr with xs_momentum) with per-instrument H_t > 0.50 entry filter (relaxed from 0.55 since the diagnostic showed Δ already monotonic by 0.50). Pre-commit: W4 Sharpe improvement ≥ +0.15 absolute, correlation with xs_momentum reduced by ≥ 0.10, trade count ≥ 200 over backtest. If those three hold on the holdout, this is a deploy-grade TSMOM variant with diversification value. If they don't, Hurst overlay joins the tombstone pile generically.
   - **Don't extend this conclusion to intraday strategies.** 252-day lookback survived for month-to-quarter-cadence mechanisms (TSMOM's natural pace); it is way too laggy for M5 intraday and there is no reason to expect a *shorter*-window Hurst to behave better than the full-sample one given the known DFA-estimator variance at low N (theoretical σ(H) ≈ 0.05 at N=252; smaller windows are worse).

   **Mechanistic interpretation — equity-index Hurst lives near 0.5**: SPX/NDX/GER median H = 0.476 / 0.479 / 0.495, matching Lo (1991)'s modified-R/S finding that equity indices are NOT long-memory. The TSMOM result therefore extracts edge from the relatively small fraction of time those indices spend in H>0.55 sub-windows — not from "equities are persistent". Crypto (BTC +0.512, ETH +0.516) sits highest and FX (EURUSD 0.465) lowest, consistent with reflexive flow and carry/MR equilibrium respectively. The result is *not* that "Hurst predicts the market" — it is that "rare H>0.55 sub-windows are exactly when long-trend probes are richest in TSMOM Δ-Sharpe", which is what TSMOM theory predicts at the *meta* level.

   **Rule for future regime-classifier proposals**: before scaffolding a new regime-classifier-as-gate experiment, run this diagnostic's pattern first — fixed naive-TSMOM + fixed naive-MR probes, bucket by the proposed classifier, look for the asymmetric MR-collapse in the holdout. If MR-side fails post-2023 universally (≥4/5 eligible), the classifier is at best a TSMOM-helper and should be scoped to that family only. Saves the overfit-driven path of "tune classifier threshold until MR comes back" — which lesson #43 predicts can't be done because the underlying mechanism is gone, not gated.

   **2026-05-23 UPDATE**: The TSMOM-helper follow-up `tsmom_hurst_gated` ran and REJECTED on its own pre-commits (W4 lift -0.13 FAIL, inverted-gate null-check W4 +1.11 BEATS proper-gate +1.03). The Hurst-overlay family is now fully tombstoned. See lesson #53 for the generalizable "regime-classifier redundancy" rule that emerged from this REJECT.

53. **Regime-classifier overlays on strategies whose primary signal is itself a regime filter are STRUCTURALLY REDUNDANT and cannot add value at portfolio level (2026-05-23).** `tsmom_hurst_gated` REJECT decisive: the Hurst gate that looked promising in the per-instrument naive-TSMOM diagnostic (lesson #52, 6/8 instruments full-sample, 3/5 W4-eligible) produced -0.13 W4 lift when bolted onto the actual `tsmom` portfolio (full Sh +0.50 → +0.33; W4 +1.17 → +1.03). The null-check is the smoking gun: the INVERTED gate (long when H<0.50) produced HIGHER W4 Sharpe (+1.11) than the proper gate (+1.03), and correlation with the ungated baseline was identical between the two gate variants (0.83 / 0.85). At portfolio level the gate has zero directional content.

   **Why the diagnostic was right per-instrument but wrong at portfolio level**:
   - The 12-1 momentum signal IS itself a slow-trend persistence filter. A positive 12-1 return overlaps heavily with H>0.5 by construction. The gate gates trades the signal has already vetted — no new information.
   - 252d-rolling DFA Hurst LAGS the 12-1 signal in regime turns. When an asset transitions chop→trend, the 12-1 fires ~21 bars after the trend starts; Hurst needs ~half its window of new-regime data to drift above 0.50. The gate cuts the EARLIEST trades of each new trend, which are the highest-Sharpe trades.
   - Per-instrument vs portfolio aggregation: the diagnostic ran fixed-cadence per-bar TSMOM; the portfolio rebals monthly with vol-targeting and equal-weight across 24 instruments. Per-instrument effective trade count drops from ~135 (over 11y) to ~100 after gating — into σ(Sharpe)-dominant territory, where the predicted ~+0.3 per-instrument lift cannot reliably surface.
   - Vol-targeting (15% per position) is the load-bearing risk-shaping tool. MDD improvement from gating (-10.5% → -8.1%) is a mechanical "fewer trades = smaller DD" effect, not Hurst-conditional — the inverted-gate MDD is -3.9%, *better* than either ungated or proper-gate.

   **Generalizable rule**: a regime classifier whose label correlates with the strategy's primary signal cannot add value via an entry gate. The gate is redundant by construction. Hurst-as-regime-filter is only useful for strategies whose primary signal is NOT itself a trend filter (e.g., yield-curve shape, vol-of-vol, calendar mechanisms, basis-arb). The "let's add Hurst to our existing momentum strategy" branch is permanently closed.

   **Pre-commit pattern for future regime-overlay proposals**: before scaffolding the Phase 2, compute corr(classifier_label, primary_signal) on the full universe. If |corr| > 0.5, REJECT the proposal at thesis stage — the overlay is structurally redundant and will fail at portfolio level even if a per-instrument diagnostic looks promising. This is a much cheaper failure mode to spot up-front than to discover via a 280-line backtest after the fact.

   **Corroborating lessons**: #6 (regime decay ≠ free pass), #20 (marginal-doesn't-improve-via-refinement), #43 (post-2022 MR sign-flip can't be regime-rescued), #52 (Hurst-classifier is asymmetric per-instrument). Together these now constrain a wide design space: regime overlays are NOT a generic Phase-2 winning lever; they require a specific orthogonality between the overlay's signal and the strategy's primary signal that almost no momentum-family strategy in this repo satisfies.

54. **Pre-FOMC drift mechanism is FOMC-and-US-equity-specific, not a portable "macro-event positioning" effect (2026-05-23).** `pre_ecb_drift` REJECT decisive: 24h LONG into ECB Governing Council monetary policy meetings on GER40 M5, 2018-2026, n=58 events, baseline mean -0.056% (Sh -0.11) vs macro_drift NDX FOMC +0.276% (Sh +1.04). Five of six binding kill-criteria FAIL; direction null-gap collapses to -0.011% (LONG and SHORT identical, zero directional content); walk-forward 3/3 OOS splits near-zero or negative.

   **The most diagnostically useful finding** is the W3 sign-flip: 2022-2023 (rate-hike cycle) was macro_drift's BEST regime (mean +0.70%, Sh +2.38) and pre_ecb_drift's WORST (mean -0.159%, Sh -0.50). Same monetary-policy regime, opposite-signed pre-event drift across the Atlantic. This rules out the most charitable interpretation ("the FOMC mechanism is really about rate-hike-cycle positioning, which is universal") — it isn't.

   **Three mechanistic reasons for the non-transfer**:
   1. **Press-conference confound**. ECB has the rate decision at 14:15 CET + press conference at 14:45 CET; FOMC has the rate decision + statement as a single event at 14:00 ET. European institutional flow appears to wait for the press-conference window, leaving the 24h-pre-decision window as wait-and-see / noise, not positioning flow.
   2. **DAX banks-heavy bi-modal direction**. Bank stocks RESPOND to ECB decisions (front of the yield curve, NIM repricing) but the response DIRECTION is bi-modal — a "long-only-into-rate-event" prior has no theoretical basis when 30%+ of the index is rate-sensitive and the surprise sign is unknown.
   3. **Asymmetric Fed-vs-ECB importance for European equities**. Surprise from FOMC affects DAX (USD funding stress, EU exporter currency translation) almost as much as it affects NDX. ECB surprise primarily drives EUR FX and EU bonds; cross-asset transmission to DAX is smaller and direction-uncertain.

   **Additional finding worth noting** (not load-bearing for the REJECT but useful): placebo non-ECB Thursdays at 14:15 CET anchor show mean -0.261% (t -1.94, almost significant). The ECB baseline of -0.056% therefore reflects a +0.205% RELATIVE lift attributable to ECB events vs structural Thursday baseline. The structural Thursday baseline itself is consistent with a Xetra mid-day microstructure negative drift (13:15-13:45 UTC) — possibly futures-roll-related or US-pre-market-positioning-related. Not standalone-tradeable but useful context for any future GER40 intraday strategy in this time window — it's fighting a -0.26% drift headwind on entry.

   **Updated priors for the rest of the macro-event-family**:
   - `pre_nfp_drift` on NDX (same venue as macro_drift, different event): ~30% chance of PASS — still worth running because the venue prior is strong, but the event-portability has been weakened.
   - `pre_cpi_drift` on NDX (same venue, scheduled monthly): ~30% chance.
   - `pre_boe_drift` on UK100 (different venue + different central bank): ~15% chance.
   - `pre_boj_drift` on JPN225 (different venue + different central bank): ~15% chance, also has overnight-gap-window issues.

   **Rule for any future "macro-event drift on X venue / Y central bank" thesis**: do NOT propose the LONG-only prior by default. Pre-commit BOTH directions as co-equal candidates (per lesson #43's MR principle, now extended to event-drift mechanisms). Specifically: write the thesis stating "LONG OR SHORT TBD per direction null-check first", then test both directions in Phase 0 before pre-committing the deploy direction. The pre-FOMC LONG prior was a US-specific accident of bank composition + statement-timing that does not generalize.

   **2026-05-23 first-application validation**: `pre_nfp_drift` on NDX100 was the first experiment after lesson #54. Under macro_drift's LONG-by-default template it would have been a -0.341% per trade REJECT (t -2.28, statistically significant negative). The direction-TBD pre-commit FOUND a SHORT mechanism with +0.241% mean (Sh +0.60, MDD -8%, walk-forward 3/3 OOS positive mean +0.76 min +0.52, W4 OOS Sh +1.09 INCREASING vs IS — opposite of macro_drift FOMC's W4 decay trajectory). Lesson #54 paid off on first application; the discipline is empirically vindicated. See lesson #55 for the follow-on kill-criteria-template insight that pre_nfp_drift's MARGINAL verdict exposed.

55. **Kill-criteria templates inherited from LONG-bias mechanisms mis-evaluate asymmetric-payoff SHORT mechanisms (2026-05-23).** `pre_nfp_drift` Phase 2 passed 7 of 8 binding kill-criteria (mean +0.241%, W4 +0.441%, Sh +0.60, MDD -8.07%, events 86, null-gap -0.582, walk-forward 3/3 OOS positive mean +0.76, placebo benign +0.010% t +0.08) — but FAILED the inherited "WR > 55%" check at 47.7%. Closer look: PF 1.62, classic asymmetric-payoff profile (47% winners are ~62% larger than 53% losers; positive expected value).

   **The macro_drift kill template was a LONG-bias template by construction**: pre-FOMC drift up most days, occasional larger down days; WR > 55% is a sensible prior for that mechanism shape (macro_drift realised 60.7% WR). For a SHORT mechanism whose alpha is structurally in asymmetric-payoff tail capture (de-risking flow into a bi-modal release), WR > 55% is the **wrong criterion** — it would auto-fail genuine deploy-grade SHORT signals.

   **Mechanism-appropriate kill-criteria template for asymmetric-payoff mechanisms**:
   - Mean per trade > +0.10% (unchanged)
   - W4 mean > +0.05% (unchanged)
   - **PF > 1.3** (replaces WR > 55%)
   - **Sh > +0.30 ann** (replaces WR > 55%; alternative formulation)
   - MDD < 25% (unchanged)
   - Events ≥ 50 (unchanged)
   - Direction null-gap |LONG − SHORT| ≥ +0.30 (unchanged)
   - Walk-forward OOS mean Sh ≥ +0.30, min ≥ 0 (unchanged)
   - Placebo benign (unchanged)

   **Honest discipline applied here**: pre_nfp_drift's pre-commit said WR > 55%. That pre-commit failed. The verdict is MARGINAL per the pre-commit rules, NOT auto-PASS via post-hoc threshold relaxation. The corrective is to **revise the kill-criteria template for the next experiment**, mechanism-aware — not to retroactively rescue this one. The user can make a judgment call on whether to deploy pre_nfp_drift via Option A (revise WR criterion → deploy), Option B (strict pre-commit MARGINAL → watch-list 6-12 months for live confirmation), or Option C (Phase 3 regime-filter extension to push WR above 55% via "good-news-is-bad-news" gating).

   **Rule for future mechanism-specific thesis docs**: pre-commit a kill-criteria set that's mechanism-aware. For LONG-bias drift mechanisms (pre-FOMC, pre-CPI in dovish-pivot regime): WR > 55% is appropriate. For asymmetric-payoff SHORT mechanisms (pre-NFP "good-news-is-bad-news" post-2022, hedge mechanisms, fade overshoot mechanisms): use PF > 1.3 + Sh > +0.30 + MDD < 25% as the trio instead. Generic blanket-application of the macro_drift template to any pre-event drift thesis was the failure mode this lesson corrects.

56. **Pre-NFP-NDX SHORT is a single-event microstructure exception within an otherwise LONG-bias scheduled-US-macro-event family (2026-05-23).** Three same-day Phase 2 experiments now establish the family-shape: `macro_drift` (FOMC LONG, deployed, Sh +1.04) + `pre_cpi_drift` (CPI LONG, 9/9 PASS, **W4 Sh +1.15, walk-forward mean OOS +1.04 min +0.81**) + `pre_nfp_drift` (NFP SHORT, 7/8 PASS MARGINAL, W4 Sh +1.09). The CPI result is a **direct refutation** of the "scheduled hard-data → SHORT" framing proposed in the pre_nfp_drift writeup — CPI is also a hard-data release and drifts LONG cleanly. NFP is the **single-event exception**, not the canonical case.

   **What actually distinguishes NFP from CPI / FOMC** (NDX 24h-pre LONG bias on Wed-Thu events flips to SHORT bias on Friday NFP):
   1. **Friday release** — only NFP lands on Friday. 0DTE-Friday-gamma + weekly-rebalance + weekend-de-risking flow biases positioning short on Friday-pre-event windows in a way Tue/Wed/Thu pre-event windows don't.
   2. **Pre-cash-open release on Friday specifically** — NFP's 24h-pre window includes Thursday overnight thin-liquidity (no major event-anchoring overnight); CPI's 24h-pre window starts on Mon/Tue/Wed with normal post-cash-close cadence.
   3. **Bi-modal surprise structure** — NFP bundles payroll counts AND unemployment-rate with separable surprise directions; CPI bundles headline + core both moving same direction usually. The bi-modal-tail-risk hedging story applies to NFP, not CPI.
   4. **First-Friday "first read" effect** — NFP is the first major macro print of each month; CPI arrives mid-month after PPI, ISM, payroll-revisions, and the first-read uncertainty has dissipated. Higher full-information uncertainty → de-risking dominates accumulation.

   **The actual canonical rule for the macro-event-drift family on NDX is**: scheduled US-macro events drift LONG in the 24h-pre window via institutional risk-premium accumulation. NFP is a Friday-specific exception. ECB on GER40 was a venue-portability exception (lesson #54). Other scheduled US-macro events on NDX (PPI, Retail Sales, Jobless Claims weekly) are now stronger candidates for LONG-direction testing — pre-commit BOTH directions (per #54) but expect LONG to win unless the event has NFP-like features (Friday + first-read + bi-modal-bundled-data).

   **Practical implications for the consolidated `event_calendar_ea.mq5`**: as of 2026-05-23 the toggle list is FOMC (LONG, enabled) + CPI (LONG, enable after Phase 7 paper PnL ≥ 0 over 3 events) + NFP (SHORT, enable after kill-criteria-revision decision). Total expected cadence: ~28-32 trades/year, calendar-orthogonal, mixed directions.

   **Same-day discipline win**: the framing in pre_nfp_drift was published in a thesis doc at ~T-2h before pre_cpi_drift ran; pre_cpi_drift's PASS-on-LONG refuted the framing within hours. Cost of having published the wrong framing: one corrected mechanistic-interpretation section in pre_nfp_drift.md. Cost of having NOT immediately run the corroboration: an unrevised framing that would have biased every future macro-event proposal toward the wrong "hard-data → SHORT" prior. Honest-immediate-corroboration > polished-but-untested framing.

57. **OPEX-pin family is fully tombstoned for US equities — 0DTE has metastasized from Mag7 to non-Mag7 high-IV single stocks (2026-05-24).** `opex_pin_singlestock` REJECT on 15-name basket (LULU, COIN, MSTR, NFLX, SHOP, CRWD, NET, AVGO, ASML, MU, ROKU, DOCU, PLTR, SNOW, NOW) — Sh -1.24, BOTH directions lose (fade -1.24, cont -0.58), holdout is the WORST regime (Sh -1.27 on the largest n=360), all-Friday-null delta -0.06 (calendar lock NOT load-bearing — generic Friday-fade matches), cost-zero Sh -0.33 (signal-driven loss, not friction-eaten). Mirrors `opex_pin_fade` index REJECT (lesson #42 / pattern #-5) exactly. **Pre-commit hypothesis was**: mid-cap non-Mag7 names with concentrated monthly OPEX OI would preserve the pin mechanism that 0DTE killed at the index level. **Refuted**: the post-2022 0DTE structural-short-gamma flow that flipped the index version has now leaked to the broader high-IV non-Mag7 universe — exactly the population the pre-commit selected as a "safe harbor".

   **Methodological wins**: (a) the "all-X null" (OPEX-only vs all-Friday delta on the SAME basket and SAME signal) was the cleanest single mechanism falsification — when a generic baseline exists for a calendar-restricted strategy, the delta is the strongest available null and routinely outperforms threshold-based kill criteria for borderline calls; (b) both-directions-lose is a stronger refutation than direction-inversion: an inverted result means there's signal in the wrong direction; both-lose means there's NO signal in the feature, just noise around zero (and 15bp cost makes any noise-trade lose).

   **Operational implication for any future single-stock options-mechanism thesis**: pre-commit an *external* 0DTE-share indicator (CBOE single-stock 0DTE OI / total OI, or comparable) as a regime filter, NOT population selection. The "this name is mid-cap so 0DTE hasn't hit it yet" heuristic is now stale by 2026; need indicator-driven gating. Future "options-expiry hedging flow" theses should pivot to: (a) non-US venues (Nikkei OPEX, FTSE OPEX — untested in this repo); (b) different calendar events (quarterly futures roll, OPEX-Monday-before-expiry); (c) genuinely low-0DTE single-stock subsets (defensives, REITs, utilities — completely different population than the high-IV growth-tech basket tested here).

58. **PEAD direction-inversion is Mag7-specific, NOT market-wide (2026-05-24).** `pead_midcap` PHASE 1 PASS on 168-name Eightcap NAS+NYS non-Mag7 mid/large-cap universe: drift Sh **+0.76**, direction-gap **+1.71** (DECISIVE drift > fade — same canonical direction as Bernard-Thomas 1989), 3/3 regimes positive INCLUDING 2023-2026 holdout (+0.77 on n=1267 — the largest and most recent sub-sample). Refutes the worry that lesson #43's "post-2022 fade-direction inversion on Mag7" had metastasized to the broader US single-stock universe at the multi-day-PEAD horizon. The Mag7-specific 0DTE-gamma flow that flipped earnings_continuation_mag7 / earnings_fade / opex_pin_fade / opex_pin_singlestock direction stays in its lane: it operates at the INTRADAY (M5/M15) and OPTIONS-EXPIRY (afternoon-of-OPEX-Friday) timescale on high-IV growth-tech names, but does NOT touch the multi-day PEAD drift on the broader 168-name mid/large-cap basket. Mechanistically this makes sense: 0DTE dealer gamma flow is intraday and clusters at high-IV-name OPEX afternoons; it has no mechanism by which to invert a 20–60-day post-earnings drift on a diversified non-Mag7 universe.

   **Phase 2 cross-sectional decile REJECT is the load-bearing nuance**: top/bottom-20% by SUE basket Sh **-0.55**, XS direction-gap **-0.83** (INVERTED). Combined with Phase 1 PASS this says **PEAD works on the body of the surprise distribution, NOT the tails**. Extreme positive SUE → price-exhaustion (already gapped fully on day 1, no further drift); extreme negative SUE → distress bounce (forced sellers exit, partial reversal). MIN_SUE sweep confirms: peak Sharpe at 5% (Sh +0.76), monotonic decay at 20%+ (Sh +0.30 at 20% MIN_SUE — same range as XS-decile). Deployable form is per-event book at MIN_SUE=5–10%, NOT cross-sectional decile.

   **Methodological introduction — concurrent-position equity curve as the proper MDD diagnostic for multi-day-hold strategies.** For any strategy with HOLD ≥ 10 days and overlapping positions, the entry-day-aggregated equity curve (the obvious "mean of entries on day t" basket) overstates MDD by 3-4× because it ignores diversification across concurrent positions. The proper curve: each position contributes 1/N where N = simultaneous open positions; PnL amortized linearly over HOLD days. On `pead_midcap`: entry-day-basket MDD -95% vs concurrent-position MDD **-24.76%** — both numerically true under different aggregation assumptions, only the concurrent figure reflects a realistically-sized live book. Apply this whenever future strategies have multi-day overlapping holds (HOLD ≥ 10d); single-day strategies don't have the issue.

   **Operational implications**: (a) the deployable mid-cap PEAD universe is **non-Mag7**; (b) Mag7 stays quarantined to the regime-conditional pivot path documented in `earnings_continuation_mag7`; (c) deploy as per-event basket, NOT XS-decile basket; (d) half-Kelly sizing recommended before walk-forward (concurrent-MDD margin is thin at 24.76% vs 25% floor); (e) per-name Eightcap M5 spread audit on top-50 active names before EA build to confirm 10bp RT cost assumption (the cost-sensitivity sweep shows positive Sharpe up to 30bp, so 5-10bp upward revision is comfortably survivable).

59. **CFD swap-cost ceiling makes multi-day-hold equity strategies structurally non-deployable on retail CFD brokers (2026-05-24).** `pead_midcap` is the load-bearing example: Phase 2 per-event PEAD PASSES every commission-only kill criterion (Sh +0.76, dir-gap +1.71, 3/3 regimes, cost-insensitive to 30bp commission), but **adding the realistic CFD swap cost destroys it**. Eightcap (and CFD brokers generally) charge ~6.5–8.5% annualized financing on long stock-CFD positions; shorts pay the negative leg AND don't refund the carry. For a 20-business-day hold at ~7% carry: swap cost ≈ 7% × 20/252 = **55 bp per side**, or **~110 bp round-trip** on a long-short basket. PEAD per-event gross is 100–200 bp — swap eats more than half, taking live Sharpe to ~0 or negative. This was NOT in the pre-commit cost model (which only tested commission); swap is a structural CFD-broker cost that does not appear in equity-cash backtests and was incorrectly omitted.

   **Mandatory Phase 0 gate going forward — for ANY multi-day-hold thesis (HOLD ≥ 2 days) on this CFD book**:
   ```
   swap_cost_budget_rt = (broker_carry_pct + 2.5%) × hold_days / 252 × 2
   require: backtest_gross_per_trade > 1.5 × swap_cost_budget_rt
   ```
   At ~7% carry:
   - HOLD=1d → swap_budget ≈ 5.6 bp → gross needed > 8 bp (trivial)
   - HOLD=5d → swap_budget ≈ 28 bp → gross needed > 42 bp (manageable)
   - HOLD=10d → swap_budget ≈ 56 bp → gross needed > 84 bp (tight)
   - HOLD=20d → swap_budget ≈ 110 bp → gross needed > 165 bp (most equity-drift mechanisms fail this)
   - HOLD=60d → swap_budget ≈ 330 bp → gross needed > 495 bp (only the strongest trend-following clears)

   **Strategies in the existing repo this constraint already explains**: the 4 deployed strategies (orb_dax / lunch_fade / xau_session / macro_drift) and 1 candidate (pre_cpi_drift) are all single-day or intraday — no swap. The KEEP-FOR-REFERENCE multi-day strategies (`tsmom`, `btc_trend`, `btc_intraday`) are positioned where multi-day gross is large enough or live trading isn't on Eightcap CFDs. `treasury_trend` / `softs_ensemble` are blocked at instrument access, but even if those broker-blockers cleared, swap-cost-vs-gross would need re-checking.

   **What this rules out vs leaves open**:
   - Rules OUT on current CFD book: classical multi-day PEAD, multi-week mean reversion, slow-trend equity ensembles, long-hold gap-continuation, anything with HOLD > ~10 days on stock CFDs.
   - Leaves OPEN: same-day / intraday strategies (no swap), short-hold (≤ 5d) strategies with gross > 50 bp, multi-day strategies on FX (negative-carry pairs can earn swap), multi-day on indexes (lower carry than single stocks), or any strategy reformulated for cash-equity execution at a prime broker (IBKR margin, Schwab, etc.) where 20d holds are essentially free of financing.
   - Future thesis prompts implicitly screened by this: don't propose "trade earnings drift over 4-6 weeks" or "weekly XS basket rotation" on the CFD-only book; either pivot to short-hold variant, or flag as "cash-equity-only" upfront in the thesis.

60. **On weakly-correlated books mixing sparse-event and continuous strategies, the dynamic-vs-static distinction in inv-vol risk-parity collapses to ~0 — deploy as static quarterly review, not pipelined rebal (2026-05-24).** `portfolio_risk_parity` PASS: book Sh +1.71 → +1.92 across 7 strategies (orb_dax / lunch_fade / xau_session / macro_drift / pre_cpi / pre_rs / pre_nfp), 3/4 regimes positive lift incl. holdout +0.36. **But the dynamic part adds nothing**: of 100 monthly rebalances, the 60d rolling-vol estimator fell back to full-sample vol on 96/100 for each of the 4 sparse macro-event strategies and 67/100 for lunch_fade — only orb_dax and xau_session ever contribute time-varying weight, and even there the rolling estimate barely deviates from the full-sample value. The mean monthly-rebalanced weights are within 1pp of a single static full-sample inv-vol vector. **Operational implication**: skip the monthly-rebal pipeline. Compute per-strategy ann-vol once per quarter (or after a regime change), translate to per-EA `risk_per_trade` (= 7 × inv-vol weight to preserve the 1%-per-strategy book gross), and only push EA config changes when any strategy's vol has moved >25%. Two adjacent rules: (a) lesson #40 still binds — this is **portfolio-overlay sizing**, not per-strategy vol-targeting (which is tombstoned for opening-impulse mechanisms); (b) the lift is structurally upper-bounded by the cross-strategy vol heterogeneity — on this book the ann-vol spread is 2.3% (xau) to 6.8% (orb_dax), so inv-vol moves the lowest-vol strategy's weight up by ~60% and the highest-vol's down by ~33%, and that's the ceiling of what the overlay can produce. For any future overlay proposal, pre-commit the static-vs-dynamic check explicitly: if you'd get the same lift from a static full-sample inv-vol vector, the rebal pipeline isn't paying for its complexity.

62. **Scheduled US-macro-event LONG drift on NDX is first-read-mid-month-mid-cycle specific, NOT a generic family rule — PCE falsifies lesson #56's broad framing (2026-05-24).** `pre_pce_drift` REJECT (decisive, framework-falsifier): LONG full Sh +0.07, W4 Sh **-1.23** (vs CPI W4 Sh **+1.15** on near-identical inflation information), walk-forward 3/3 OOS NEGATIVE with monotonic decay (-0.14 → -0.55 → -1.23), null-gap +0.161 (half the +0.30 threshold), 6/9 binding pre-commits FAIL. **Placebo benign** (mean -0.040%, t -0.29) — critically rules out the month-end-structural-drift confound, so the rejection is PCE-specific signal failure, not a calendar artefact.

   PCE and CPI carry near-identical information (~0.85+ headline-YoY correlation) — the "inflation print drives LONG accumulation" framing predicted PCE PASSES with CPI-magnitude. It doesn't. The actual distinguishing axes (extracted ex-post from the four passing events vs PCE's REJECT):

   | Event | Cal. position | Day-of-week | Info-cycle | Verdict |
   |---|---|---|---|---|
   | FOMC | mid-cycle | Wed | first-read (rate decision) | LONG PASS |
   | CPI | mid-month | Tue/Wed/Thu | first-read (inflation) | LONG PASS |
   | Retail Sales | mid-month | Wed | first-read (real economy) | LONG PASS |
   | NFP | first-Friday | Friday | first-read (payrolls) | SHORT (Friday exception) |
   | **PCE** | **end-of-month** | **Friday-dominant (76%)** | **confirming-read** | **NO drift** |

   **Refined framework (replaces lesson #56)**: scheduled US-macro events on NDX drift LONG via institutional risk-premium accumulation ONLY when the event is the *first major print of its information cycle* AND lands in the *mid-month / mid-week slot*. Friday-dominated end-of-month confirming-reads do NOT inherit the LONG drift. The three distinguishing axes — (a) calendar position within month, (b) day-of-week composition of the 24h window, (c) information-cycle position (first-read vs confirming-read) — must all align for the LONG mechanism to fire. The macro-event-family deploy book is therefore **NOT auto-expandable** to every scheduled US-macro release; each new candidate needs explicit screening on these three axes before pre-commit.

   **Operational implications for future event candidates**:
   - **pre_ppi_drift**: mid-month Wed/Thu, first-read (precedes CPI by 1 day) — strong LONG candidate per refined framework.
   - **pre_jolts_drift**: first-Tuesday-of-month, first-read employment-side — moderate LONG candidate (Tuesday not Friday, first-read, but labor-side gets less positioning vs payrolls).
   - **pre_durable_goods_drift**: end-of-month Mon/Tue/Wed, confirming-read (releases after Retail Sales) — PCE-adjacent risk; pre-commit BOTH directions (lesson #54) with explicit prior that LONG may fail like PCE.
   - **pre_gdp_drift**: quarterly Wed/Thu morning, first-read of quarterly output — LONG candidate but n≈30 over 8 years is sample-size constrained.
   - **pre_consumer_confidence_drift**: end-of-month Tue, soft survey — confirming-read by the time it lands; expect PCE-style null.
   - **pre_ism_pmi_drift**: first-business-day-of-month, first-read manufacturing — strong LONG candidate (first-of-month + first-read + Mon/Tue).

   **What this REJECT does NOT do**: weaken CPI's deploy stance. The PCE placebo (non-PCE weekdays at 08:30 ET) is benign, which means CPI's LONG signal is NOT just "any 08:30-ET pre-release weekday drifts LONG" — it's specifically the mid-month inflation-print accumulation. PCE is the falsifier of the generic-weekday null, and that strengthens CPI's claim to a real PCE-orthogonal mechanism.

   **Methodological win — canonical-test design**: deliberately picking the closest-correlated event in the same family as a corroboration test (not a diversification add) generated a sharper framework refinement than three diverse "diversification add" tests would have. PCE's CPI-mirror failure is more diagnostic than would have been a marginal PASS by some unrelated event. **Rule for future framework-generalisation testing**: when a hypothesised generalisation has a near-twin candidate (PCE/CPI, PPI/CPI, M2/inflation), run the near-twin first as an intentional falsification test before running diverse extensions — the highest-information-content REJECTs come from the closest analogs failing in unexpected ways.

   Companion to adjacent lesson #61 (`pre_natgas_eia` REJECT on commodity-on-own-fundamental venue): together these two cleanly bound the macro-event-drift family — the mechanism is (i) US-equity-index venue specific (lesson #61), AND (ii) first-read-mid-month-mid-cycle event-shape specific (this lesson). Both axes must align.

   Also: second confirmation of lesson #20's "single-split passes / walk-forward fails" pattern within the macro-event family — PCE's IS Sharpe (+0.35 to +0.47) would have looked deploy-grade on a single-split test; 3/3 monotonic OOS decay is what exposes the IS-overfit. Walk-forward is binding on this family, not optional.

61. **Macro-event-drift family does NOT auto-generalise from index-on-US-macro to commodity-on-own-fundamental release — the *flow mechanism* must port, not just the *event-calendar shape* (2026-05-24).** `pre_natgas_eia` REJECT decisive: 24h pre-EIA Weekly Natural Gas Storage Report (Thu 10:30 ET) on XNGUSD Eightcap CFD, M5 2023-2026, n=177 events. LONG mean -0.235% at 30bp realistic NG-CFD cost / Sh -0.53 / MDD -64.5%; SHORT mean -0.365% / Sh -0.82. **Null-gap +0.130% < +0.30 threshold** — mechanism lacks directional content full-sample. W3 (2023, post-Ukraine-war NG collapse) drags -1.199% mean / Sh -3.06; W4 (2024-2026) tentatively activates LONG (+0.179% / Sh +0.39 / walk-forward OOS mean +0.76 min +0.39) but doesn't clear the null-gap or the per-trade-mean pre-commits on the full sample. The macro_drift / pre_cpi_drift / pre_nfp_drift family all run institutional-equity-risk-premium-accumulation flow on NDX100 ahead of US-macro releases; that mechanism story does NOT apply when (a) the asset is the *underlying* of the event (XNGUSD ↔ Henry-Hub NG storage), (b) the print is direct fundamentals not policy-decision context, (c) weather + pipeline data leak the magnitude in advance, (d) asymmetric bearish tail discourages pre-event LONG positioning. **Generic rule**: when extending an event-drift family to a new asset class, the prior must come from the *flow mechanism that would operate in the new venue*, not by analogy to the *event-shaped calendar*. For commodity-on-own-fundamental events specifically (EIA NG, EIA crude, USDA WASDE, etc.), neutral-prior or asset-specific-positioning-data (COT, weather-forecast error, seasonal carry regime) gates are required before testing — do not import the NDX LONG prior by analogy. Pattern of caution: this is the *fifth* domain in 2026-05 where mechanism-by-analogy was the load-bearing falsification (after lesson #48 lunch-fade-NDX/NQ-specific, #51 lunch-fade-not-FDAX-generic, #52 Hurst-asymmetric-not-universal, #54 macro-event-positioning-not-portable-across-venues, #58 PEAD-direction-inversion-Mag7-specific). Family-extension is a *test*, not a *deploy candidate by default*.

62. **Cross-asset family extension MUST run a weekday-matched placebo on the *candidate* instrument — a clean placebo on the *original* does not transfer (2026-05-25).** `pre_xau_macro_drift` REJECT decisive across all 4 events from the NDX event_calendar book (FOMC/CPI/RS/NFP) ported to XAUUSD M5 2018-2026. Per-event Sharpes on XAU look plausibly deploy-grade in isolation (FOMC +0.31, CPI +0.54, RS +0.28, NFP +0.36) and 3/4 direction-agree with NDX — superficially "extension confirmed." The weekday-matched placebo on non-event days completely dissolves the story:

   | Event | Event LONG-SHORT gap | Placebo LONG-SHORT gap | Net edge over placebo |
   |---|---|---|---|
   | FOMC | +0.227% | +0.037% | +0.190% (real event-specific lift) |
   | CPI  | +0.255% | **+0.290%** | **−0.035%** (placebo BIGGER than event) |
   | RS   | +0.179% | -0.022% | +0.201% (real but small) |
   | NFP  | +0.206% | +0.184% | +0.022% (basically none) |

   On CPI/NFP/RS the apparent "event edge" is just the post-2022 structural gold-bull drift (central-bank buying + inflation-hedge bid) sampled coincidentally on event days. Only FOMC has an event-specific lift on XAU after the placebo control. On NDX the same placebo is benign for all 4 events because NDX does NOT have a comparable structural background drift — the placebo control was inert on the original instrument, so its omission would never have surfaced this confound.

   **Generic rule**: a cross-asset family-extension must include the weekday-matched non-event placebo on the *candidate instrument*, not just inherit the placebo's clean result from the original. The placebo's job is asset-specific: it controls for whatever structural drift dominates THAT instrument's calendar bucket. The same null is benign on NDX (low calendar-day structural drift) and contaminating on XAU (high structural drift post-2022).

   **Operational checklist for any future cross-asset extension** (`x_macro_drift`, `x_session_extension`, `x_event_anything`):
   1. Run weekday-matched non-event placebo on the candidate instrument BEFORE evaluating per-event Sharpes.
   2. Compute placebo null-gap and require event null-gap ≥ placebo null-gap × 1.5 (an extra 50% buffer above the structural baseline).
   3. If the placebo null-gap ≥ event null-gap, REJECT regardless of event Sharpe — the apparent edge is structural drift, not event-specific.
   4. Make this check binding *per-event*, not aggregated — CPI on XAU failed even though FOMC on XAU passed.

   **Sharpens lesson #56 framing**: NDX-family LONG bias on scheduled US-macro events is mechanism-specific to (a) pre-cash-open institutional positioning into a fixed RTH boundary, AND (b) equity-risk-premium accumulation. XAU has neither — it trades 24h with no concentrated positioning window, and its flow drivers (real yields/DXY/risk-off) are two-sided around macro releases rather than one-sided. The 3-of-4 direction agreement with NDX on XAU is coincidental (structural gold drift happens to point LONG on Tue/Wed/Thu), not mechanistic. **The NDX event_calendar book is NDX-cash-equity-microstructure-specific**, not a generic macro-event trade. Future cross-asset extension candidates: SPX500 (same microstructure family), MES/MNQ futures (same), NDX-component single stocks (degraded but same family). NOT: commodities, FX, crypto (all have either no cash-open boundary or fundamentally different flow mechanisms).

   Companion to lesson #61 (commodity-on-own-fundamental refutation): together these bound the macro-event family at *two* axes — (i) flow-mechanism portability (#61), (ii) instrument-microstructure prerequisites (this lesson). A future macro-event extension must clear BOTH.

63. **Absolute-ATR (or any absolute-vol-derived) filter on a secular-drift instrument is mechanically equivalent to a year-filter, not a vol-filter (2026-05-25).** Surfaced jointly by `xau_imbalance_m15` REJECT and `xau_break_retest_m15` Phase 2. Both demos pre-committed an "ATR(14) at signal-bar > X USD" filter to capture the user's discretionary heuristic ("ATR > 8-10 tradeable, < 4-5 skip"). On XAU's 2018-2026 sample, the ATR(14) distribution shifts monotonically upward — driven by the underlying level rising ~1800 → ~5000+ USD (XAU is up 3× over the window, and absolute ATR scales roughly with price for percentage-vol-stable instruments). Result: **any "ATR > 5 USD" floor mechanically selects post-2022 bars** at ~90% concentration. On `xau_imbalance_m15`, ATR-floor variants 5/7/10 dropped W1 (2018-2020) sample to 4-18 trades and W2 (2021-2022) to 0-5 trades — INSUFFICIENT_N — while the W3 (2023-2026) sample stayed at 100+ trades and showed apparent "regime-isolation lift" of +1.30. On `xau_break_retest_m15`, the same artifact: all 4 ATR-floor variants (3/5/7/10) collapsed W1+W2 to n<10, while W3 retained 60-200 trades. The "ATR isolates vol regime" intuition is correct in principle but **broken in practice** for instruments whose price level has trended significantly over the sample window.

   **Generic rule for any vol-derived filter on a secular-drift instrument**: do NOT use absolute thresholds (ATR > X USD, range > Y pips, etc.). Use one of:
   - **Percentile-of-trailing-N**: `ATR_today > percentile(ATR_trailing_252d, 50)` — bins by within-year vol regime, not cross-year price level
   - **Price-normalized vol**: `(ATR_today / close_today) > X%` — strips out the level-trend
   - **Z-score-of-trailing-N**: `(ATR_today - mean(trailing)) / std(trailing) > 1.0` — same as percentile but parametric

   **Affected instruments to pre-flag**: XAU 2018-2026 (3×), BTC 2017-2026 (multi-decade), NDX/QQQ tech 2010-2026 (5×), MES/SPX 2009-2026 (4×), Russell 2009-2026 (3×), most single-stock CFDs over multi-year windows. **NOT affected**: range-bound FX crosses (EURUSD has stable absolute range), VIX index (stationary by construction), stable-fundamentals commodities like agricultural softs at multi-year horizons.

   **Discovery context**: both M15 follow-up experiments (imbalance + break-retest) used the user's discretionary ATR rule as a pre-commit. The user's heuristic was *correct in spirit* (skip dead-vol days) but absolute-threshold implementation captures the wrong axis on XAU. The same heuristic would work cleanly on relative-ATR terms. **For the discretionary record**: the user is unknowingly running a within-day-relative vol filter (his eye anchors today's ATR against recent days/weeks, not against the absolute price level) — that's why it works for him but breaks when literalized as `ATR_USD > 5` over a 7.9y backtest sample.

   **Companion to lesson #5 (research-to-live haircut framing)**: this is another instance of a heuristic that works at the trader's experience-window timescale but fails when ported to a longer backtest window where the underlying regime has shifted. Any future "user-discretionary-rule → systematic-filter" port must validate that the rule's mathematical formulation is stationary across the full backtest sample, not just within the trader's recent experience.

64. **TF-coarsening as a Phase-1.5 microstructure diagnostic — re-run the baseline at the next coarser TF holding wall-clock spec constant (2026-05-26).** For any M5/M15 strategy that clears Phase 1: resample bars to the next-step-up TF (M5→M15 or M15→H1), scale lookback/retest/hold by the TF ratio (so a 45-min hold stays a 45-min hold, not 3 bars), keep the session window and cost-bps fixed, and rerun the baseline simulator. The diagnostic bucket separates real session-flow edges from M5/M15 microstructure or autocorrelation artifacts at near-zero engineering cost.

   **Critical sub-rule — wall-clock confounds dwarf TF effects on session-handoff strategies**: when comparing the same strategy at a different TF, the wall-clock convention (open-of-bar vs close-of-bar) must be explicitly matched at both TFs. A "TF refinement" that silently changes the wall-clock is testing two things at once. Empirically observed on an overnight-session-handoff strategy: on a 2×2 (TF × wall-clock) grid, the wall-clock effect was **19×** the TF effect (+0.19 vs +0.01 Sh) — the entire apparent "TF advantage" was actually a 1-hour wall-clock shift. Future TF-refinement tests must hold (TF, wall-clock, filter, session, hold-duration) all constant except for the one variable under test; the standard procedure is a 2×2 cell grid that decomposes ΔSh by axis before drawing any TF conclusion.

   **Generic rule (Phase 1.5)**: every M5/M15 strategy that passes Phase 1 baseline produces a TF-ladder readout before promotion to Phase 2. Same wall-clock spec at coarser TF. Comparison buckets:
   - **PASS-DIAG (STABLE)**: |ΔSh| < 0.20, regime structure preserved → promote.
   - **PASS-DIAG (MILD-DECAY)**: 0.20 ≤ |ΔSh| < 0.50, dir-gap and W3 holdout stable → promote with note (deployed-TF cadence matters but mechanism is real).
   - **PASS-DIAG (REFINEMENT-ADVANTAGE)**: coarser TF *strengthens* edge (or finer TF strengthens, in refinement direction) → investigate whether the deployed TF is suboptimal.
   - **MICROSTRUCTURE-SUSPECT**: |ΔSh| ≥ 0.50 with degradation, dir-gap collapses → tombstone unless a structural reason for TF-sensitivity is documented.
   - **DIFFERENT-SPEC**: TF change forces session/parameter change beyond the simple bar-ratio scaling (e.g. a 2h session can't fit a 3-bar-pattern mechanism at H1 because only 2 H1 bars sit in the session) → test invalidates itself; rely on the original-TF verdict.

   **Discovery context**: surfaced from the canonical example XAU NY 12-18 UTC FADE (M15 +1.26 gross → H1 +1.78 gross at live cost — edge GROWS) vs XAG LDN-AM (M15 +1.26 gross → H1 -2.40 gross — edge COLLAPSES). The mechanical fingerprint cleanly separates real session-flow from M15 autocorrelation noise. Adding the diagnostic as a routine Phase 1.5 gate prevents the M15-microstructure-vs-real-edge confusion from propagating into Phase 2 work.

   **Companion to lesson #15 (cross-instrument fade test)**: both #15 and #64 are *cheap mechanism-content tests* that should run before any costly Phase 2 sweep. Fade-test rules out "no directional content"; TF-coarsening rules out "microstructure artifact". A strategy that passes Phase 1 baseline AND fade-test AND TF-coarsening is much more likely to survive Phase 2 than one that passes Phase 1 alone.

65. **Cross-asset extensions of an event-flow strategy ride the *same* primary flow via known cross-asset correlations — they are magnitude-shadows, not independent flow legs, and decay first (2026-05-26).** `pre_fomc_drift` (EURUSD M5, FX-side falsification of the deployed NDX-LONG-pre-FOMC mechanism). User-posed the test as a binary: PASS = USD-risk-premium-accumulation flow exists ⇒ refines the framework; FAIL = mechanism is equity-vessel-only ⇒ sharpens lesson #62. **Result: third outcome.** Full-sample directional signal IS there (null-gap +0.75 Sh, +0.0869% mean, clean placebo at -0.025% t -0.55) but (a) the signal direction is **LONG EURUSD = USD weakens pre-FOMC, OPPOSITE the user's SHORT-prior** which was rooted in a "investors rotate into USD safe-haven cash" story, and (b) the W4 (2024-2026) deploy-relevant regime is dead (mean -0.0093%, Sh -0.07, n=19) AND walk-forward third split (deploy-binding per lesson #29) is OOS Sh -0.07.

   **Mechanism**: post-2022 dovish-pivot regime has a persistent **DXY-equity negative correlation**. Pre-FOMC equity risk-on (the macro_drift mechanism that's deployed LONG NDX) co-occurs with USD weakening; EUR is the opposite side of a DXY trade, so the *same* equity-risk-on flow shows up on the FX vessel as LONG EURUSD via the DXY-equity coefficient. The FX vessel is not running an independent USD-risk-premium accumulation flow — it's a mechanical mirror of the equity leg. Magnitude(FX) ≈ |ρ(DXY, SPX)| × Magnitude(equity), which is *necessarily* smaller and decays first as the primary mechanism decays. macro_drift W3 → W4 Sh fell from +2.38 to +0.41 (still deploy-viable); EURUSD W3 → W4 Sh fell from +0.48 to -0.07 (crossed zero). Same fractional decay; the FX leg started smaller and crossed the deploy floor first.

   **Operational rule (binding)**: for any deployed event-flow strategy on a primary vessel, do NOT propose "secondary-vessel-of-X" extensions where the secondary is reachable from the primary via a known cross-asset correlation. The default expectation is the third-outcome shape (signal exists on secondary via mechanical correlation, direction may invert the naive prior, magnitude is the cross-correlation coefficient, decay matches primary's fractional decay starting from a smaller base ⇒ secondary crosses zero first). Pre-tombstones the obvious follow-ups: pre-CPI / pre-RetailSales FX-side legs (would be LONG-EURUSD shadows of LONG-NDX events); pre-NFP FX-side leg (would be SHORT-EURUSD shadow of SHORT-NDX event); cross-asset extensions of XAU break-retest (would ride USD-XAU correlation in mechanical-mirror form); cross-asset extensions of lunch_fade (won't propagate because lunch_fade's mechanism is index-cash-vs-futures-basis-arb specific per lesson #-8, not a cross-asset-correlated flow).

   **Methodological rule (binding)**: when proposing a cross-asset extension of a deployed strategy, the pre-commit should enumerate THREE outcomes, not two: (1) independent flow leg exists on secondary vessel ⇒ direction matches the user-stated mechanism prior ⇒ refine framework + consider partial-correlation deploy; (2) no signal on secondary vessel ⇒ sharpens primary-vessel-specificity story; **(3) signal exists on secondary vessel but via mechanical correlation with primary ⇒ direction may invert the user prior ⇒ secondary is a magnitude-shadow not an independent leg ⇒ reject for deploy and tombstone the cross-asset family.** Outcome (3) is the *most likely* prior because most pairs of liquid assets have non-zero cross-correlation; outcome (1) is the rare case of a genuinely independent parallel flow.

   **Companion to lesson #62 (NDX event_calendar XAU-extension REJECT) and lesson #-14 / #-15 (session-transplant and TF-stretch transplant rejects)**. Together they bound four failed transplant axes from the deployed book: (a) same-instrument-different-session (#-14), (b) same-instrument-wider-TF-window (#-15), (c) different-asset-class-via-mechanism-by-analogy (#62 — XAU on US-macro events), (d) different-asset-class-via-mechanical-cross-correlation (#65 — EURUSD on FOMC). The clean transplant primitive remains very narrow: same-instrument-different-TF-on-the-TF-where-M-shape-peaks (per #-15).

   **Diagnostic value of the user's wrong-signed prior**: the SHORT-EURUSD prior was generated from a coherent narrative (de-risking into uncertain Fed event ⇒ rotate to USD cash ⇒ buy USD sell EUR). The data refuted that narrative directly — LONG-EURUSD signal means EUR strengthened pre-FOMC, the opposite of de-risking. This is one of the cleanest examples in the repo of *narrative-vs-mechanism* divergence: a plausible-sounding flow story turned out to be the wrong-signed account of what's actually happening, and only the data-driven both-directions-pre-commit (lesson #54) caught it. Any future "I think X will happen because flow Y" thesis should pre-commit both directions as co-equal candidates and treat the user-stated prior as a hypothesis to be tested, not a deploy direction to be confirmed.

66. **FX has independent pre-CB-event flow only at the intersection of (a) no equity primary in the way to shadow AND (b) event's modal outcome is a non-event — direction is carry-position-MAINTAIN, not carry-unwind (2026-05-26; Phase 3 corroborated same day).** `pre_boj_drift` (USDJPY 24h pre-BoJ MPM, n=29 events 2022-10→2026-04). Phase 2: 9/9 binding pre-commits PASS (W4 Sh +1.55 on n=19). **Phase 3: 3/4 binding controls PASS (strong MARGINAL — sample-size-limited, NOT mechanism-limited).** The two PASSES that matter for mechanism interpretation both pass *decisively*: **C1 (modal-outcome partition)** shows expected-no-action W4 subset (n=16) Sh +2.75 vs expected-policy-shift subset (n=3) Sh -1.90 — a +4.65 Sh delta between subsets, fully corroborating the carry-maintain-on-modal-non-event mechanism story. **C4 (cross-asset shadow control)** shows SPX500 on the same 24h pre-BoJ windows is flat (mean -0.04%, t=-0.17) and per-event USDJPY-SPX correlation is -0.16 (negative) — decisively ruling out the hidden-mechanism-B-shadow alternative hypothesis. **C3 (Deflated Sharpe)** confirms the result survives the implicit 20-cell window-x-buffer sweep deflation (observed Sh +1.55 → deflated +0.75, well above +0.30 threshold). **C2 (block-bootstrap CI)** fails by 0.01 (Sh lower-95 +0.29 vs +0.30 threshold) — a small-sample issue (n=19) that resolves naturally as more events accumulate. Diagnostic C5 (permutation null) gives p=0.064 vs 0.05, same sample-size noise.

   **Mechanism**: when (a) holds — the candidate event has no equity primary already running the same flow on a near-correlated equity index — AND (b) holds — the event's *modal outcome* (the most-likely individual realisation) is "no policy change," allowing carry-positioning to persist through without forced de-risking — speculators *maintain* the dominant carry-trade direction (here, JPY-funded long-USDJPY carry) into the 24h pre-window. The signal direction is LONG-carry-side. Hawkish surprises (W4 had 2: 2024-07-31 rate-hike and 2025-01-24 rate-hike) are *negative contributors the strategy survives*, not the source of the alpha — single-event sensitivity drops STRENGTHEN the result (drop 2024-07-31 → W4 Sh +1.55 → +2.34).

   **Regime conditionality (critical)**: the same mechanism fails wrong-signed in W3 (2022-10→2023, Sh -0.72 on n=10) because the YCC-policy-shift era had 6 of 10 BoJ meetings produce surprise policy moves — i.e., condition (b) failed. The lesson is therefore *not* "USDJPY drifts up before BoJ in general" but "USDJPY drifts up before BoJ when BoJ is in a low-surprise regime; reverts to mean or negative when BoJ is in a high-surprise regime." Forward-looking deploy must include a kill-trigger sensitive to regime shift: rolling-3-event live OOS Sh < 0 → halt.

   **Three-mechanism decomposition of the macro-event-drift family** (this is the bigger framework deliverable):

   - **Mechanism A — equity-primary on first-read mid-month/mid-cycle US-macro events** (#-13). Institutional risk-premium accumulation into NDX (= cash-equity-microstructure flow into a fixed RTH boundary, per #62). LONG on FOMC/CPI/RS, SHORT on NFP. Deployed.
   - **Mechanism B — FX-magnitude-shadow on equity-primary events via cross-asset mechanical correlation** (#65). FX rides the post-2022 DXY-equity negative correlation; magnitude on FX ≈ |ρ(DXY,SPX)| × magnitude(equity-primary). Smaller than primary by construction, decays first, direction is *DXY-mirror of equity direction* (LONG-NDX-pre-FOMC ⇒ LONG-EURUSD shadow ⇒ USD weakens, which refutes the naive "USD safe-haven" prior). REJECTED for deploy.
   - **Mechanism C — FX-independent on no-primary-equity CB events with modal-non-event outcomes** (this lesson #66). Carry-position-maintain through MPMs that the market correctly anticipates as non-events. Direction is *maintain the dominant carry*, not unwind. Regime-conditional on the event-surprise distribution. MARGINAL on the only available test case (BoJ-on-USDJPY).

   Three different mechanism families, three different sign-direction priors, two different deploy verdicts (A deployed, B rejected, C pending Phase 3). **The user-narrative direction has been wrong-signed for all three of B and C tests** — the carry-unwind / risk-premium-flow / safe-haven-rotation stories are *intuitively plausible but data-refuted*. Lesson #54 (pre-commit BOTH directions, validated 4× now in 2026-05: pre_nfp_drift, pre_fomc_drift_eurusd, pre_ecb_drift_eurusd, pre_boj_drift) is the load-bearing safeguard that converts these would-be-bias-confirmations into framework-sharpening findings.

   **Operational implications**:

   - Do NOT auto-extend mechanism C to other CB pairs by analogy. SNB/CHF and Riksbank/SEK are theoretical candidates (each has CHF/SEK carry and modal-non-event MPMs in W4) but their carry magnitudes are an order smaller than JPY-funded carry, so the absolute signal would be sub-deploy-grade even if the mechanism holds. BoC/CAD has CAD-carry but BoC MPMs are NOT modal-non-events in W4 (BoC has been active on rates 2022-2025) → condition (b) fails.
   - Pre-CPI / pre-NFP / pre-RS on USDJPY are NOT mechanism-C candidates because those events have *equity primaries* (mechanism A) and USDJPY would carry the FX-shadow (mechanism B, REJECTED) of those primaries. Already pre-tombstoned by #65.
   - For any new "pre-X-event-on-FX-pair-Y" thesis, the three-mechanism decomposition is the screening framework. Identify which mechanism (if any) the candidate sits in; mechanism-B candidates are pre-tombstoned; mechanism-C requires (a) AND (b) both hold; mechanism-A is equity-side and the candidate isn't FX.

   **Methodological tightening**: small-sample mechanism-C deploys require an explicit regime-shift kill-trigger. The mechanism works because of a *property of the event distribution* (modal-non-event), not because of an immutable structural flow. The property can change (e.g. BoJ entering a hike cycle, YCC reintroduction). Live monitor must catch the regime shift early — pre-commit rolling-3-event live OOS Sh < 0 as a hard kill.

   **Why this matters for the deployed book**: it's the first FX deploy candidate that's survived Phase 2 in 2026-05. If Phase 3 confirms, it adds a calendar-orthogonal trade (BoJ meetings don't co-occur with FOMC) at zero correlation with the existing equity-side macro_drift book. That's genuine diversification — not the "FX-shadow doubling-up on the same flow" that #65 rejected. But it's also fragile (regime-conditional, small W4 n=19), so deploy-paper-only with tight kill-trigger is the conservative path.

67. **"Institutionally invisible" / capacity-moat framing is a deploy-prerequisite, NOT a deploy-predictor — capacity moat ≠ edge (2026-05-26).** `cfd_wed_rollover_eurusd` REJECT decisive. Thesis was the second deliberate application of the "chase mechanisms institutional capital structurally cannot access" reframing (after deployed `lunch_fade`, whose institutional-absence story is the cash/futures basis-arb compression during NY lunch). This one bet on retail-CFD-broker triple-swap accrual: Wednesday-night triple-rollover at 22 UTC exists only in retail CFD accounting (spot FX accrues daily, futures roll quarterly, prime brokerage uses spot/futures) and supposedly drives retail-cohort LONG-EURUSD unwinds in the Wed 10-20 UTC window, producing SHORT-EURUSD edge that is *structurally inaccessible* to spot/futures/PB FX. The capacity-moat argument was strong: the venue truly is microstructurally walled off from institutional arb. **The signal was not there.** Full sample n=253 (Eightcap 0.86 bp RT, |Fed-ECB|>100bp), mean -0.28 bps net / Sh -0.04; gross only +0.58 bps at zero cost — *order of magnitude below* the +3 to +12 bps thesis prior. W3 (2023-2026, n=164) sign-flipped to -1.16 bps Sh -0.23. Walk-forward 3-of-3 IS positive / OOS negative. Rate-diff tercile was **u-shaped not monotone** (LOW -0.85 / MID +6.93 / HIGH -6.76) — directly refutes the "mechanism scales with carry magnitude" prediction; MID +6.93bp on n=80 is small-sample artifact. Placebo non-Wed pool -0.33 bps and Goodhart Tue 18-22 UTC -0.71 bps both benign → edge has not migrated to a different day or upstream, **it simply does not exist at detectable magnitude**.

   **Why the framing was necessary but not sufficient**: institutional inaccessibility means no entity exists that *could* arb the mechanism. It does *not* imply that the mechanism *exists* at a magnitude detectable in M5 closes. The retail cohort generating the unwind flow can be too small a share of total EURUSD CFD volume to move the print, regardless of who else is or isn't in the venue. `lunch_fade` works because (a) the venue is institutionally walled AND (b) the cash/futures basis-arb compression mechanism produces detectable price reversion at the M5 level — both clauses are load-bearing. This experiment validated only (a) and assumed (b); (b) failed empirically.

   **Operational rule going forward**: capacity-moat / institutional-inaccessibility is now a *prerequisite* for retail-deployable mechanisms, not a *predictor* of edge. Any future "institutionally invisible" thesis must independently argue that the mechanism produces *detectable* price impact — and the bar for "detectable" is set by Phase 2 cost-realistic gross magnitude exceeding 3-5× the venue's spread, not by mechanistic plausibility alone. A failure to clear this gross-magnitude prior in Phase 1 reasoning (we predicted +3-12 bps; observed +0.58 bps) is the gating signal. Pairs with lesson #51 (lunch-fade is NDX/NQ-specific, not generic index-basis-arb): both establish that mechanism-by-analogy and venue-by-analogy expansions need *direct empirical validation of the price-impact channel*, not just the structural-access argument.

   **Secondary observation — third 2026-05 instance of retail-positioning-direction-on-EURUSD being wrong-signed in W3.** After `pre_fomc_drift` (user SHORT-EURUSD prior, actual LONG-EURUSD via DXY-mirror) and `pre_ecb_drift_eurusd` (same DXY-mirror), this experiment's W1→W3 sign-flip (W1 +1.56 bps, W3 -1.16 bps) is a third independent demonstration that "retail is structurally long EURUSD" is not a stable assumption across the post-2022 rate-divergence regime. Any future EURUSD thesis whose mechanism depends on retail-positioning *direction* needs a regime gate or, better, *measured* positioning data (CoT-adjacent retail-broker series if obtainable) — assumption-from-narrative is now data-refuted three times.

   **Why this is one lesson, not three**: the load-bearing finding is the capacity-moat-not-sufficient principle (applies broadly to future "institutionally invisible" theses, retail-CFD or otherwise). The retail-positioning-direction observation is a corroborating sub-finding within the EURUSD-W3 family that already has its own context in #65 and #66. Cross-pair Phase 3 (GBPUSD/USDJPY/AUDUSD) is deferred indefinitely: EURUSD is the largest-carry / cleanest-setup pair in the FX universe; if it doesn't work, cross-pair priors are substantially worse and the data-and-time cost of three more sims is not justified.

68. **Institutional-absence framing identifies who is NOT in the window — not what flow IS in the window. The fill-flow can be regime-dependent and W2/W3-sign-inverting (2026-05-26).** `retail_overshoot_fade` REJECT (7/9 binding fail) on 7-name retail-popular single-name CFD basket (TSLA NVDA PLTR COIN MSTR HOOD RDDT, after Phase 0 removed MARA RIOT GME AMC SOFI as not on Eightcap MT5). Thesis: 5%/1h intraday spike → SHORT, 2-day hold, 5% stop, 30bp RT cost. Mechanism story: retail-popular names have a 1-3d institutional dead-zone (borrow-cost asymmetry / mandate carve-outs / time-horizon coverage gap) where retail-flow exhaustion drives multi-day mean-reversion. Third deliberate application of the "chase mechanisms institutional capital structurally cannot/will-not access" reframing (after deployed `lunch_fade` and REJECTED `cfd_wed_rollover_eurusd` per #67).

   **Headline finding is the W2→W3 sign-inversion.** Regime breakdown: W2 2021-2022 vol Sh **+1.30** (n=35, MDD -23%, near deploy-bar quality) → W3 2023-2026 holdout Sh **-1.07** (n=91, MDD -69%). Same mechanism, same trigger, same direction, opposite sign across consecutive regimes. Direction null-check FAILS cleanly: FADE -0.26 vs CONT -0.16, gap **-0.10** (no directional content full-sample). Cost-zero gross Sh **+0.00** — signal-absent per lesson #26, NOT friction-eaten. Walk-forward 3/3 OOS negative (mean -1.37, min -2.31). MDD -71% catastrophic at basket level — red flag #3 (correlated universe: COIN/MSTR move on BTC, PLTR/RDDT on sentiment) fired exactly as pre-committed. HOLD_DAYS=5 outperforms HOLD_DAYS=2 (+0.14 vs -0.24) — consistent with retail-attention half-life extending past the 1-3d window in the post-2024 short-form-video regime (red flag #6).

   **Mechanistic interpretation extending #67.** Capacity-moat / institutional-inaccessibility identifies *who is NOT in the window*. It does not identify *what flow IS in the window* — which is what determines the strategy's directional content and its persistence across regimes. In 2021-2022 the window-fill on these names was retail-flow exhaustion (the thesis's binding mechanism, and W2 Sh +1.30 confirms it existed at that time). In 2023-2026 the window-fill is *something else* — most plausibly 0DTE-gamma desks taking the multi-day book, prop CFD market-makers on the spike-retracement, long-vol ETF flow into single-name gamma — and that fill produces zero-directional-content noise around the same trigger. **The window wasn't empty in W3; it was differently full.**

   **Operational rule (tightened from #67).** Future "institutional-absence" / capacity-moat theses must pre-specify a diagnostic that distinguishes three fill-regimes:
   - **absence-with-MR-flow** — deployable, e.g. `lunch_fade`'s cash/futures basis-arb compression. The flow exists, has known cross-regime persistence, and produces detectable price reversion.
   - **absence-with-momentum-flow** — deployable inverse direction with explicit sign re-pre-commit per lesson #43.
   - **absence-with-regime-conditional-flow** — *this experiment*. The flow exists in one regime, gets replaced or arbed in the next, and produces W2-pass / W3-fail sign-inversion if not gated. Not deployable without an external real-time regime indicator (and even with one, fragile).
   - **absence-with-no-persistent-flow** — un-deployable noise. Magnitude failure shape (per #67).

   Without that pre-specification, the institutional-absence framing is a *research-priority filter* (which thesis families to spin up first), not a *Phase 2 entry ticket*. The framing has now produced one deploy (`lunch_fade`, basis-arb fill) and two REJECTs (`cfd_wed_rollover_eurusd` magnitude-failure shape #67, `retail_overshoot_fade` regime-decay shape this lesson) — three data points enumerate three distinct sub-failures, and the next institutional-absence thesis should be pre-classified against the three before pre-commit.

   **Reinforces lessons #16 / #25 / #28 / #29 (single-regime-PASS is not a deploy signal).** Quoted standalone, W2 Sh +1.30 (MDD -23%, n=35) looks like a deploy-bar clear. The 3-window split (CLAUDE.md convention) and the direction null-check (#43) prevented the deploy mistake by exposing the W3 -1.07 / dir-gap -0.10. Methodological win: both safeguards were load-bearing here; either alone would have caught the REJECT.

   **Falsifiable forward prediction (HOLD_DAYS sweep).** If retail-attention half-life IS extending into the TikTok era (red flag #6), the next institutional-absence single-name thesis should pre-commit on a 5-10d hold window with cost-budget pre-calibrated to extended-swap, not the 1-3d window this thesis used. Re-running on the 1-3d window will reproduce this REJECT shape; running on a 5-10d window may surface different fill-flow content. This is a pre-committed hypothesis for whoever picks up the family next.

   **Cross-experiment pattern (paired with #67 and #51).** Three failed institutional-absence sub-cases now bound the framework:
   - **Magnitude-below-cost-bar even at zero arb pressure** (#67, `cfd_wed_rollover_eurusd`).
   - **Mechanism-by-analogy fails at the price-impact-channel level** (#51, `lunch_fade` doesn't generalise beyond NDX/NQ basis-arb).
   - **Regime-dependent fill-flow that worked in W2 and inverted in W3 to no-directional-content** (this experiment).

   The clean institutional-absence deploy primitive remains very narrow: cash/futures basis-arb compression during a fixed-clock vacuum on a deep, walled venue (`lunch_fade`). Other institutional-absence shapes need to clear three independent priors before Phase 2: (a) magnitude-of-price-impact, (b) cross-regime persistence of fill-flow, (c) deployability of any required external regime gate.

69. **Event-flow mechanisms do NOT auto-port across the TEMPORAL axis of the same event — pre-event anticipation flow ≠ post-event rebalancing flow, and Phase 0 magnitude gates catch the difference cheaply (2026-05-26).** `post_fomc_amateur_fade` REJECT at Phase 0 binding magnitude gate (+3.84 bps full-sample FADE mean < +5 bps pre-committed floor). Thesis was the temporal-axis complement to the deployed `event_calendar` FOMC LONG (24h-pre, anticipation-window risk-premium accrual per Lucca-Moench 2015 / Hu-Pan-Wang-Zhu 2022): hypothesized retail-amateur spike in the first 15min post-FOMC → SHORT/LONG fade T+15→T+60min as institutional algos re-assert. Same event (FOMC), same instrument (NDX100 M5), same calendar (`fomc_calendar.csv`), different intra-event horizon. **The mechanism did not inherit.**

   **Empirical shape.** W1 2019-2020 FADE +10.13 bps (n=5, Sh-per-trade-style +0.70 — pre-COVID era did show the amateur-fade signature), W2 2021-2022 +4.64 bps (n=8), W3 2023-2026 holdout **-0.36 bps** (n=9, modern regime zero). Monotonic decay, not sign-inversion: the mechanism *evaporated* in W3, it did not flip to continuation (CONT W3 also +0.36 bps, the trivial mirror at fixed floor). **This W1→W3 decay shape differs from `retail_overshoot_fade` (#68) where W2→W3 sign-inverted to active loss** — calling it out because the institutional-absence failure-shape taxonomy now needs a fifth category: **temporal-axis-decay-to-noise** (different from #67 magnitude-below-cost, #68 regime-conditional-sign-flip, #51 mechanism-by-analogy, and the un-deployable-noise endpoint).

   **Why post-event does not inherit from pre-event on the same calendar event.** Pre-event 24h window flow is *anticipation* (slow institutional positioning, monotonically directional, cross-regime persistent because the positioning constraint is unchanged across regimes). Post-event 15-60min window flow is *rebalancing* (algorithmic re-hedge after IV collapse + delta-one ETF arb + 0DTE-gamma desk close-out — none of which carries a directional prior). The temporal-axis extension assumed the post-event window inherits exploitable directionality from the pre-event window; the flow-type changes across the announcement, so no inheritance. **Direct extension of #56's "macro-event-family extension requires the flow mechanism to port"** from the *event-family* axis (FOMC → ECB / PPI / NFP) to the *intra-event-temporal* axis (24h-pre → 15-60min-post).

   **The user-pre-flagged HFT-absorption red flag was empirically correct.** Pre-run prior was ~30-40% PASS-shape with red flag #1 ("0DTE/HFT-era post-event flow may already be absorbed in <60s") as the most likely failure mode. Phase 0 result confirms: spike trigger density is adequate (38% of events have |first-15min move| ≥ 10 bps, mean 10.7 bps — flow IS happening) but the T+15 → T+60 *reaction* has zero magnitude in W3. By T+15, absorption is done. Modern-regime retail reacts inside the same temporal bucket as the algos (Robinhood-app push + CBOE 0DTE every weekday + social-feed real-time alerting), eliminating the amateur-vs-algo gap the thesis depended on.

   **Methodological win: the Phase 0 magnitude gate worked exactly as designed.** Pre-committed binding floor (+5 bps gross full-sample mean) extracted the REJECT verdict in ~30 min of simulator + write-up, instead of the multi-hour Phase 2 sweep (baseline + cost-stress + walk-forward 3-fold + regime + SPIKE_FLOOR sweep + EURUSD robustness) that would have been built on the +3.84 bps signal. The +5 bps floor was chosen pre-run as "magnitude must exceed realistic cost (~5 bps RT on NDX H1 CFD)"; the headline came in at +3.84 bps, below cost, so even an optimistic Phase 2 could not have delivered net-positive net of fees regardless of regime/cost-sweep gymnastics. **Codify**: any thesis whose Phase 2 mechanism requires a directional reversion/continuation at a fixed time-horizon should ship with a Phase 0 magnitude gate set to "gross full-sample mean per-trade ≥ realistic-RT-cost on the candidate venue." This is the cheapest possible falsification primitive on event-driven theses and avoided 1× the build-cost-equivalent of a Phase 2 simulator on this experiment alone.

   **Falsifiable forward prediction.** If the temporal-axis non-portability is the binding mechanism (and not some FOMC-specific quirk), the same Phase 0 magnitude gate should also REJECT post-event amateur-fade on the OTHER deployed event-family LONG events (CPI / RS) at the same T+15 → T+60 horizon on NDX100, and the W1→W3 monotonic decay should reproduce on each. Cheap to test (~10 min per event-family via the same `_phase0_magnitude_check.py` scaffold with a different calendar file). Pre-tombstones the analogous `post_cpi_amateur_fade` / `post_rs_amateur_fade` extensions by mechanism inheritance unless one of those Phase 0 magnitude gates surprises.

70. **Academic / institutional-flow direction priors on FX can be REGIME-INVERTED — pre-commit catches the sign-flip; the residual signal in the opposite direction is informational, NOT a PASS (2026-05-26).** `usdjpy_tokyo_fix` REJECT at Phase 0b magnitude gate. Pre-committed DOW direction map from the Tokyo-fix exporter/importer literature (Melvin-Prins 2015 / BIS Triennial / Evans-Lyons): Mon/Tue = SHORT USDJPY (month-start exporter JPY-buying flow at the 09:55 JST fix), Wed = LONG (importer JPY-selling). On USDJPY M5 at the 00:45→01:05 UTC window 2022-10 → 2026-05 (~3.6y, W3+W4 BoJ-intervention era only — Eightcap has no pre-2022 data), the **DOW-mapped strategy gross is -1.337 bps per-trade** on 560 trades, below the +1.5 bps Phase 0b floor. Only Wed (LONG, +1.444 bps t=+3.42) agrees with the prior; Mon (SHORT, gross -4.155 bps i.e. USDJPY actually GAINS +4.155 bps on Mondays at the fix, t=+5.98) and Tue (SHORT, -1.315 bps i.e. USDJPY gains +1.315 bps, t=+4.66) both invert.

    **The underlying data signal is real and strong — but pointing the opposite direction from the prior.** Aggregated unconditional LONG across all 5 weekdays at the 00:45→01:05 UTC window: n=936, mean **+1.672 bps**, t = **+8.03 vs zero**, 65.8% positive trades. Mon is dramatically the strongest arm at **+4.16 bps with 75% positive Mondays**; median ≈ mean (NOT outlier-driven); **stable year-by-year, every year 2022-2026 between +3.51 and +6.24 bps Mon mean, no year inverts.**

    **The pre-commit is the load-bearing methodological win.** Without it, the +8 t-stat all-weekday LONG signature would look like a deploy-grade discovery (~250 trades/yr × +0.67 bps net at 1 bp cost ≈ +170 bps/yr; Mon-only LONG at +4.16 bps gross has 2-3 bps cost headroom). With the pre-commit, the experiment correctly REJECTs because direction was selected from prior literature, not the in-sample data — and post-hoc-fitting direction to a high-t signal is exactly the failure mode the pre-commit guards against (cf. `earnings_continuation_mag7` lesson #43 — same data + opposite directions both lose at full sample because the mechanism flipped sign at a regime boundary).

    **What this lesson adds beyond #43.** Lesson #43 was framed for US risk-asset MR / fade mechanisms where the 0DTE-gamma-flow shift made fade-vs-cont a sign-uncertain question. This lesson extends the same rule to **academic and institutional-flow priors on FX** specifically: a published, mechanistically-coherent direction prior (WMR fix exporter-flow, Melvin-Prins peer-reviewed) can still be regime-inverted in modern broker-cost-window data. The inversion candidates here (post-2022 Japan energy-import deficit driving importer JPY-selling; life-insurer hedge-rebalance JPY-selling on the multi-year USDJPY uptrend; Asian-session yen-funded carry-trade resumption post-YCC-exit; Monday weekend-gap mean-reversion) are *all* mechanistically plausible — but none of them is in the original Melvin-Prins exporter-flow narrative. The prior was correct in the pre-2008 / pre-YCC era; the modern regime has overwritten it.

    **Forward operating rule.** For any FX intraday thesis whose direction prior is sourced from FX-microstructure literature (pre-2015 academic work, or pre-2022 BIS surveys), **pre-commit BOTH directions at Phase 0** — run the magnitude gate with `direction = +1` AND `direction = -1` separately, AND aggregate-unconditional. If unconditional aggregate has |t| > 3 vs zero AND the literature direction has gross < cost floor, treat it as **inversion-by-regime evidence**, REJECT the current experiment, and file a fresh v2 pre-commit with the data-supported direction (NOT a continuation of the current experiment). The residual high-t signal is informational, not a PASS — it must clear a fresh out-of-sample window in a separate experiment.

    **Lesson #45 is NOT the binding failure here.** Cost-ceiling (lesson #45) would imply prior-correct direction shows ~+0.5 bps gross eaten by ~1 bp cost; here prior-correct direction is -1.34 bps gross, i.e. *signal-absent in the pre-committed direction*. Mis-attributing this to #45 would lose the regime-inversion finding.

    **Falsifiable forward prediction.** If the regime-inversion is driven by the structural Japan post-2022 importer + life-insurer JPY-selling story, then (a) Mon LONG USDJPY at the Tokyo fix window should hold in 2027+ data, (b) the inversion should NOT replicate on EURJPY/GBPJPY where the importer/life-insurer flow direction is opposite (yen-buyers, not yen-sellers, on the cross), (c) a hypothetical pre-2022 USDJPY M5 sample would show the original Mon/Tue=SHORT direction working. (a) tests over the next 12 months; (b) testable now as part of a v2 thesis; (c) requires non-Eightcap data and is low-priority.

71. **Published price-impact patterns that have been subjected to post-publication market-microstructure REFORM default to "fully arbed at retail-accessible windows" — academic "compression-not-elimination" claims should be discounted by another order of magnitude before being treated as a deployable prior (2026-05-26).** `xau_fix_drift` REJECT (10 of 11 binding fail). LBMA Gold Price was reformed Aug-2015 from a four-bank twice-daily *telephone-call* fix to an *electronic IBA-administered auction* in direct response to the manipulation scandals + Caminschi-Heaney (2014) 8-22 bp pre-fix drift documentation. Thesis prior assumed ~50% compression to a 2-6 bp residual (LBMA market reports + Ronen-Wei 2015) — i.e. the venue was reformed but the *mechanism* (MM hedging of auction-imbalance in OTC spot at sub-bp/minute rates) was still expected to leak a retail-tradeable signal at the 25-minute pre-fix window on M1. **The data shows the gross is essentially zero in both directions.** On XAUUSD M1 2018-2026, 25-min pre-fix lookback / 5-min entry-before-fix / 5-min exit-after-fix / MIN_PRE_DRIFT=3bp filter / 0.20pt RT Eightcap-conservative cost, COMBINED CONT mean = −1.01 bp / FADE mean = −1.05 bp / direction-gap +0.08, with the cost-zero gross extrapolated to **≈ −0.06 bp/trade** — inside the M1 measurement noise floor. Per-window dir-gaps: AM +0.57 (only AM is weakly mechanism-coherent), PM −0.42 (PM mechanism-INVERTED) — consistent with sample noise, not asymmetric flow. Pre-drift-threshold sweep 1→8 bps: per-trade mean LOCKED at ~−1.0 bps across every cell — tighter filter cannot unmask a residual edge. **Cost-zero diagnostic per lesson #26**: cost-zero Sharpe ≈ −0.93 / cost-zero mean ≈ −0.49 bp says **signal-absent NOT friction-eaten** — no tighter-cost venue (zero-spread bullion-RFQ, prime brokerage) would deploy this strategy.

    **What this lesson adds.** Lesson #62 ("event-flow mechanisms inherit, but cross-asset extensions ride the *same* primary flow") and lesson #69 ("temporal-axis non-portability of event-flow") both warn against *extension* moves. This lesson warns against a different operation: **rebooting a mechanism inside its own venue/instrument after the venue has been reformed in response to the very paper documenting the mechanism**. Specifically: when (a) a price-impact pattern was published with concrete magnitude estimates AND (b) the venue underwent a structural reform within ~12 months of the publication AND (c) the reform was explicitly motivated by closing the publicized leak, the rational prior is "the reform closed the retail-accessible window completely". The literature's post-reform "compression" estimates tend to be insider-data-driven (LBMA market reports, IBA auction logs) and reported on participation-bank-level slicing, NOT on retail-M1-window slicing — they over-estimate what residual flow is visible at the retail latency.

    **The corr-tombstone PASS (−0.02) is informational but moot.** Pre-committed criterion #11 (trade-by-trade per-day PnL corr vs deployed XAU book `xau_session ∪ xau_break_retest_m15 ∪ xau_break_retest_h1`) **passes at −0.02**. That tells us the pre-fix 25-min window has *independent* information content from the deployed XAU book — and that independent content is *zero*. A high-corr FAIL would have been "the mechanism is real but already captured"; a low-corr PASS with positive Sharpe would have been "deploy-able marginal addition"; a low-corr PASS with negative Sharpe is "mechanism dead, not even worth tombstoning by redundancy". This is the third corner of the 2×2 corr × Sharpe interpretation matrix and it is the most common one for marginal-extension theses; criterion #11 should be evaluated AFTER criterion #1 (full Sharpe), not before.

    **Operational ports of the lesson.**
    - **Don't auto-spin** on post-reform-venue extensions of published patterns: any rerun of MiFID-II's TVR-reform-affected European-equity microstructure mechanisms (post-2018) inherits this prior. Specifically: pre-LSE-cash-auction order-book imbalance (LSE moved to anonymous bidding 2017), pre-Xetra-auction VWAP-tail drift on DAX (Xetra Frankfurt 2017 reform), pre-NYSE-cash MOC imbalance pre-2024 (NYSE reformed display timing 2024). Each gets this lesson applied as a Phase-0 cheap pre-tombstone before Phase 2 build.
    - **Cost-zero gross is the binding diagnostic for "mechanism dead vs eaten by friction"** (sharpens lesson #26): per-trade cost-zero mean below the M1 measurement noise floor (~1 bp on 5-min holds of XAU; instrument-specific) = mechanism dead; per-trade cost-zero mean clearly above noise floor but cost-net mean negative = friction-eaten (potential lower-cost-venue rescue). The xau_fix_drift cost-zero gross of −0.06 bp on a per-trade std of ~50 bp is unambiguous "below noise floor".
    - **Compression-not-elimination claims should be discounted by another order of magnitude** when (a) the reform happened ≥ 5y ago AND (b) intervening HFT capacity has scaled. The 50% compression in Ronen-Wei (2015) was a year-1 estimate; year-10 capacity scaling has done the rest.

    **Why we ran it anyway.** The experiment is cheap: M1 data already fetchable via MT5 (2.8M bars, ~3 min), single instrument, two events/day, ~6h end-to-end including doc + tombstone. The cost of confirmation is small and the value is concrete: a verbatim verification of the thesis's own red-flag #1, and a tombstone shape ("famous published pattern + structural reform → fully arbed at retail latency") that's directly portable to the next MiFID-II / NYSE-2024 / Xetra-2017 extension. Don't refuse to run a cheap experiment just because the prior already predicts REJECT — the value is in the *shape* of the confirmation (which red flag was binding, which diagnostic was decisive), not in the binary outcome.

    **Tombstoning impact on XAU intraday infrastructure.** Combined with `xau_ldn_am_fade` REJECT (FADE, 2026-05-26) and `xau_ldn_orb_m1` REJECT (CONT+FADE, 2026-05-26), the LDN-AM 07-11 UTC envelope is now tombstoned for XAU intraday at M1 / M5 / M15 across all directional flavours. XAU intraday infrastructure is bounded by deployed `xau_session` (Asian-handoff LONG, 23→08 UTC), `xau_break_retest_m15` (NY-AM 13-15 UTC FADE), `xau_break_retest_h1` (NY 12-18 UTC FADE) — no further intraday-XAU extension thesis worth spinning until a structurally different mechanism (e.g. options-expiry / OPEX-pin / triple-witch) is identified.

72. **Structural-flow calendar audit is a productive idea-source — first deploy candidate from the methodology PASSES Phase 2 12/12 (2026-05-27).** Triggered by the live-book origin audit (2026-05-27), which showed the existing 6-strategy deploy book skews **58% literature / 33% external-agent / 17% discretionary / 0% structural-or-enumeration-derived** — with structural-first identified as the deepest untapped source (the user's discretionary playbook of FVG + break-retest patterns has been strip-mined into the deployed `xau_br_m15` + `xau_br_h1`; further discretionary ideation is zero-EV per hour). [`structural_flow_audit`](../experiments/structural_flow_audit/structural_flow_audit.py) screened 17 cells of (event × instrument × intraday-window) forced-flow calendar mechanisms — JPM Hedged Equity collar reset (last biz day of Mar/Jun/Sep/Dec, 15-16 ET), month-end WMR FX fix (15:45-16:15 London), VIX SOQ settlement (Wed before 3rd Fri, 08:30-09:30 ET), OPEX day-after gamma unwind (Mon after 3rd Fri, 09:30-12:00 ET), quarterly triple-witch closing hour, month-end USD funding squeeze (last biz day, 14-15 ET), quarter-end pension last 2h (last biz day of Q-end month, 14-16 ET) — on 6 retail-tradeable Eightcap instruments (SPX500, NDX100, EURUSD, USDJPY, GBPUSD, XAUUSD). The screen used Welch t-test of event-window returns vs same-weekday placebo, ranked by `|t-stat| × sign(cost-headroom)` against pre-committed STRONG/MEDIUM/WEAK/REJECT tier thresholds.

    **Result**: 1 STRONG (`quarter_end_xau_short` — null-gap −13.32 bp, t=−3.28, n=27 events on XAU 14-16 ET) + 3 WEAK (`month_end_usd_funding × EURUSD`, `month_end_usd_funding × GBPUSD`, `opex_day_after_am × SPX500` — all worth refinement) + 13 REJECT (pre-tombstoning candidate theses that would otherwise absorb cycles). Pre-Phase-2 gates ran on the STRONG candidate: regime breakdown (W1 +8.83 / W2 +5.66 / W3 +3.01 bp net per event — all positive, monotonic-decay flagged for live monitoring), direction null (gap +1.20 SHORT − LONG), corr-tombstone vs deployed `xau_br_h1` (+0.317, well below 0.70 redundancy bar). **All three Phase 0+ checks PASSED.** Phase 2 simulator with 12 pre-committed kill criteria → **12/12 PASS**: full mean net +11.83 bp/event, ann-Sh +1.14 (at 4 events/yr), MDD −0.30%, WR 70.4%, bootstrap 95% CI on full mean **[+4.77, +20.11]** decisively excludes zero, deflated-Sh +0.68 after 17-cell selection-bias adjustment, placebo non-event SHORT mean −1.12 bp (cleanly different from event-day +12.52 bp), cost-stress @ 0.30pt (5× default) mean net still +11.49 bp, walk-forward halves both positive (H1 +13.56 / H2 +10.23 bp).

    **Mechanism reconfirmation**: quarter-end pension/SWF/insurance institutional rebalancing — equity-overweight portfolios (predominant during 2019-2026 except W2-vol pockets) forced to SELL bonds/gold/cash and BUY equities to restore target weights; XAU is the marginal safe-haven sell because bond rebalance is structurally hedged via futures, cash is mechanically neutral, gold-long portfolios are smaller than equity-overweight portfolios → asymmetric SHORT flow. 14-16 ET concentrates spot XAU institutional liquidity (London PM session ends 16:00 GMT, overlapping NY peak). Pairs cleanly with Etula, Rinne, Suominen & Vaittinen (2020) "Dash for cash" month-end USD funding evidence — quarter-end is the same mechanism at 3× monthly magnitude.

    **The deepest finding is the holdout-regime result.** W3 2023-2026 mean net +9.32 bp (t +1.97) decisively positive **despite** XAU having a structural LONG-trending tailwind in W3 (XAU rallied $1900 → $3300, +75%, in W3). The SHORT strategy bucking that trend and still profiting +9 bp per quarter-end event is the strongest possible evidence the mechanism is real — it has to overcome a strongly LONG-trending instrument and still deliver SHORT-direction edge on the targeted days. Compare to `pead_midcap` (lesson #59) where the W3 holdout was just barely net-positive (+0.77 with same-direction tailwind); here W3 is net-positive *against* the instrument's tailwind.

    **Three operational lessons for the idea-source pipeline:**
    - **The structural-flow audit takes ~1 day to build** (single Python file with calendar generators + placebo + Welch t-test + tier ranking) and surfaces deploy-grade candidates at **~6% strict-hit rate** (1 STRONG of 17 cells) and **~25% worth-investigating rate** (1 STRONG + 3 WEAK). For comparison, literature mining typically requires 1-2 weeks per candidate and the candidates are already arbed. The structural source is positive-EV per hour and **uncorrelated with what literature surfaces**.
    - **The screen pre-tombstones REJECT cells**, saving cycles. Of the 13 REJECTs, several would have been "obvious to try" theses (JPM collar SPX, triple-witch close, VIX SOQ SPX) that the user might have manually proposed and run through a full Phase 2 cycle — each ~2-3 days of work. Net cycle savings ≈ 30-40 days.
    - **The screen + Phase 0+ + Phase 2 pipeline produces the same kill-criteria discipline** as literature-derived strategies — pre-committed kill criteria, bootstrap CIs, deflated Sharpe for selection bias, regime breakdown, placebo, direction null, walk-forward. No methodology compromise relative to deployed-book quality.

    **Methodological side-finding (binding for screen rerun):** the screen's `COST_FLOOR_BPS` dict had a 10× scaling error on indices and XAU (XAU was set at 7 bp when actual `0.20pt / $2900 ≈ 0.69 bp`; SPX500 at 5 bp when actual `0.4pt / $5800 ≈ 0.7 bp`; NDX100 at 4 bp when actual `0.4pt / $20000 ≈ 0.2 bp`). The bug made the screen **STRICTER** than intended, so no false positives surfaced — but several REJECT cells may surface as WEAK / MEDIUM / STRONG on rerun with corrected costs. **The XAU STRONG result is unaffected** because gross magnitude (13.32 bp null-gap) far exceeds either cost estimate. Fix scheduled — when run quarterly the corrected costs will give a sharper rank.

    **Forward priority for the pipeline**: re-run the structural-flow audit quarterly with (a) corrected cost floors, (b) widened universe (multi-day mechanisms: Japan fiscal year-end Mar 31, tax-loss harvesting concentration Dec 15-31, tax-loss reversal Jan 2-15, ECB TLTRO repayment dates), (c) additional intraday windows that weren't screened in v1 (e.g. 30-minute windows around scheduled events instead of 2h, time-of-day breakdown within each 2h cell). Each quarterly rerun should be expected to produce ~1 candidate worth Phase 2 thesis lock at the observed 6% strict-hit rate. **The pipeline now has a non-zero answer to "where do new ideas come from"** that doesn't depend on academic publication cycles or the user's discretionary repertoire.

    **What this DOESN'T validate (yet):** the live-haircut for structurally-sourced deploys. None has live-traded. The first 1-2 years of `quarter_end_xau_short` paper trading will be the test — 4 trades/yr cadence means 8 trades is the minimum sample for any live-vs-research comparison and that's 2 years out. Until then, the pipeline is research-confirmed but live-unconfirmed.

73. **Single-instrument TSMOM on a retail-CFD post-2014 sample requires BOTH a buy-and-hold benchmark AND a directional null-check as a Phase 0 gate — kill criteria alone hide a passive-beta-with-extra-steps result (2026-05-27).** `gold_trend` REJECT decisive. Multi-horizon 12-1 TSMOM on XAUUSD D1 2015-2026 (2,902 bars, 5bp/side cost, 15% vol target, monthly rebal, 1M/3M/12M lookbacks averaged, optional ATR pyramid). All 4 pre-committed numeric kill criteria PASS technically on best variant MH-LO-Pyramid (Sh +0.80 / MDD −16.5% / 85 trades / +127% total return) — but two pre-committed fail conditions both trip and the canonical direction null-check fails. (1) **Fail #2 (B&H benchmark)**: Sh +0.80 < XAU buy-and-hold +0.85. On a 62%-long-bias 15%-vol-targeted strategy, the only way to *underperform* unconditional B&H is for the signal to mis-time the regimes — and it does. (2) **Fail #5 (regime concentration)**: 100% of full-sample Sharpe is the 2023-2026 holdout (Sh +1.59); the other 4 windows are 2015-2017 −0.14 / 2018-2019 +0.10 / 2020-2021 +0.46 / 2022 −0.21. (3) **Null-check FAIL**: inverted-signal long-only (NULL-LO) makes +0.54 Sh full-sample and is positive in 4 of 5 regimes (including 2015-17 at +1.44 and 2022 at +0.89 where MH-LO is negative). Signal-vs-null gap is only **+0.21 Sharpe, below the +0.30 convention threshold**. (4) **L/S vs L-only diagnostic** confirms the short side LOSES money cleanly (NULL-LS −0.51 / MH-LS Sh +0.45 vs MH-LO +0.75 → −0.30 contribution from short side). Signal has no negative-direction skill. **Mechanism interpretation**: the TSMOM literature (MOP 2012, HOP 2017) holds on 30-200y futures samples, but the post-2014 retail-tradeable sample is dominated by one macro regime (the 2023+ central-bank-buying + inflation-hedge gold bull run) and produces a passive-beta-with-extra-steps result. Adds nothing to the deployed 24-instrument `tsmom` basket which already includes XAU and benefits from cross-asset diversification. **New rule (Phase 0 gate, binding for any future single-instrument-TSMOM thesis on a retail-CFD sample under 15 years)**: pre-commit BOTH (a) "Sh > buy-and-hold of the same instrument over the same window" AND (b) "Sh − (Sh of inverted-signal LO at same vol target) > +0.30" as kill criteria, BEFORE running the Phase 2 simulator. The full-sample Sharpe number alone is uninformative on a single-asset long-bias strategy when the asset has a strong unconditional drift — what you measure is "is the signal better than random sign for going long this drift?" not "does the signal predict returns." Companion to lesson #29 (walk-forward replaces single-split for TSMOM): even with walk-forward PASS, B&H + null-check are still required to distinguish edge from drift-sampling. Pairs with #56 (macro-event LONG drift on NDX is regime-stable, not regime-conditional): the difference between gold-trend (regime-conditional) and a deployed momentum strategy is that the latter's full-sample Sharpe survives the worst regime, not just rides the best one.

74. **Monetary-regime conditional direction inversion now documented across three independent mechanism families — month-end equity-flow theses must pre-commit a Fed-policy gate (2026-05-27).** `last_hour_month_end_ndx` REJECT (7/13, direction-lock criterion #13 caught W2 sign-flip). Pre-committed BOTH directions per lesson #54. Best-direction SHORT wins on aggregate (+3.57 bp net, ann-Sh +0.29) and W1 2019-2020 (+12.40 bp SHORT) and W3 2023-2026 holdout (+3.52 bp SHORT) — funding-pressure mechanism (Etula et al. 2020) dominates. **W2 2021-2022 inverts** (SHORT −5.17 bp / LONG +4.67 bp): the QE/pandemic stimulus era flips the winner because turn-of-month positive drift (Lakonishok-Smidt 1988) + monetary-stimulus-driven equity inflows dominate over funding-pressure selling. Three competing mechanism stories ALL real, alternating dominance by monetary regime. **Direction-lock criterion #13 (same direction must win in all 3 regimes)** caught this cleanly — without it the strategy would have looked borderline-deployable at +3.57 bp aggregate. Live deployment risk: any policy shift back toward QE-style stimulus immediately inverts the live P&L sign. **The pattern is now a documented family failure**: lesson #-7 (Mag7 single-stock earnings: 2022/2023 fade→continuation sign-flip via 0DTE-amplification + monetary regime), lesson #2 (US-index intraday MR: 2023-26 0DTE-amplification proxy), and now lesson #74 (month-end equity rebal flow: 2021-2022 QE-stimulus inversion) — all driven by *monetary regime change*. **Operational rule (binding for any future month-end equity-flow thesis)**: pre-commit a monetary-regime gate at thesis time. Acceptable gates: (a) Fed funds rate direction over trailing 6 months, (b) Fed balance sheet QE-on vs QT, (c) real-rate sign (10y nominal − 10y breakeven), (d) NDX 60d realized vol regime. The strategy operates only in one regime at a time; deploy must be gated. A regime-gated v2 of this experiment (`last_hour_month_end_ndx_v2_gated`) is the right next step IF the regime classifier is built independently — naive backtest on the unconditional sample produces a deploy-blocked aggregate. **Methodological deliverable**: criterion #13 (direction-lock across all 3 regimes) should be **promoted to a default pre-commit criterion** for any strategy where competing mechanism stories predict opposite directions. The 13-criterion gate worked exactly as designed here. Pairs with lesson #-7 (same regime-conditional sign-flip pattern), #43 (0DTE-amplification family), and #54 (pre-commit BOTH directions).

75. **Structural-flow audit screen needs a 3× cost-floor ratio rule, not 1× — magnitude must exceed cost by 3× for Phase 2 candidate-promotion (2026-05-27).** `month_end_usd_short` REJECT (basket SHORT EUR + SHORT GBP, 7/13 binding fail; magnitude/cost ratio ~1.5×). The mechanism IS real — every mechanism-falsification criterion PASSES: EUR + GBP co-direction in W3 holdout (criterion #13 PASS), direction-gap +0.45, placebo basket +0.05 bp on n=2359 non-event same-weekday days, walk-forward halves both positive, all 3 regimes net-positive. But cost-net magnitude (basket +0.85 bp) is barely above cost floor (~1.75 bp blended EUR+GBP), giving magnitude/cost ratio ~1.5×. Bootstrap CI [−1.46, +3.06] crosses zero; cost-stress @ 2× kills it (−0.77 bp net); deflated Sharpe collapses to +0.02 after screen-selection adjustment. The screen's existing `cost-headroom > 0` criterion (magnitude minus cost-floor positive) is *necessary but NOT sufficient* — for deploy-grade Sharpe at the relevant cadence, the ratio must be ~3× or higher. Re-applying the tightened 3× rule to the original 17-cell screen output: `quarter_end_xau_short` 13.32 / 0.7 = **19× ratio → STRONG-tier** (correctly promoted, Phase 2 PASSED 12/12); EUR `× month_end_usd_funding` 2.20 / 1.5 = **1.5× ratio → would be pre-tombstoned** (Phase 2 REJECT confirmed); GBP `× month_end_usd_funding` 2.34 / 2.0 = **1.2× ratio → would be pre-tombstoned**; `opex_day_after_am × SPX500` 9.46 / 0.7 = **13.5× ratio → still promoted**, still in Phase 2 queue. The refined rule pre-tombstones the EUR/GBP basket attempt while keeping the OPEX-day-after candidate active. **Operational rule (binding for structural-flow audit v2)**: update `structural_flow_audit.py` to require `magnitude / cost-floor ≥ 3.0` for STRONG-tier promotion, `≥ 2.0` for MEDIUM, `≥ 1.5` for WEAK. Cells below 1.5× ratio are pre-tombstoned at screen time. **Project-level lesson**: structural-flow audit produces ~50% strict-PASS at Phase 2 from STRONG-tier hits (only `quarter_end_xau_short` qualified by the tighter rule) and ~0% from WEAK-tier hits. WEAK-tier candidates are mechanistically validated (signal exists) but cost-blocked — useful for *family-mapping* via cross-confirmation but not deploy candidates. The screen's purpose is candidate generation; the magnitude-vs-cost gate determines deploy-feasibility. The cost-headroom rule refines lesson #72 with empirical calibration from the 3-data-point pipeline (1 PASS + 2 REJECT). Companion to lesson #26 (cost-zero Sharpe diagnostic): both lessons distinguish "real signal eaten by friction" (cost-zero positive, cost-net sub-threshold = THIS lesson's family) from "signal absent at any cost" (cost-zero near-zero in both directions = lesson #26). Two different REJECT shapes; two different next-step implications. Sub-finding worth recording: `month_end_usd_short` is `VALIDATED_BLOCKED_AT_COST`, analogous to `pead_midcap` (research-PASS, CFD-swap-cost-blocked) and `treasury_trend` (research-PASS, instrument-availability-blocked). At institutional FX cost (< 0.5 bp RT via PB or CME 6E/6B futures) the strategy clears the 3× ratio and becomes deploy-grade — but not in the repo's current Eightcap execution stack.

76. **Regime-gated v2 of any strategy has a structural small-sample / deflated-Sharpe failure mode — partitioning a 7-8y sample by a 3-state gate leaves 15-30 events per regime, exactly where deflated-Sh penalty bites hardest. The methodology DETECTS real conditional signal; the deploy bar is hard to clear with currently-available data (2026-05-27).** Built `regime_classifier_diagnostic` to audit deployed strategies through 4 candidate macro gates (FFR direction, real-rate sign, yield curve sign, VIX regime). The diagnostic showed strong empirical signal — Pool A "DEPLOYED-BOOK GATING HAS LEGS" verdict with 5+ strategies showing >+0.5 deployable Sharpe spread under multiple gates, Pool B identified `last_hour_month_end_ndx` as a resurrect candidate. Two v2 attempts tested through Phase 2 with calibrated pre-commits:

    - **`xau_session_v2_ffr_gated`** (Pool A — gating a DEPLOYED strategy): REJECT 9-10/11. Gate works (NEUTRAL-regime regret −0.01 confirms strategy is dead in that regime; gated full Sh +1.11 vs parent +0.79 = +0.30 lift). Failed criterion #1 (full Sh ≥ +1.20) by ~0.10. The +1.20 bar was calibrated to the diagnostic's active-aware Sh of +1.74 in TIGHTENING — but the active-aware annualization OVER-STATES deployable lift by ~2-3× because it uses `sqrt(active_per_year_IN_regime)` rather than `sqrt(active_per_year_FULL_SAMPLE_with_gate)`. **Lesson methodology fix**: deflation factor `sqrt(regime_years / full_sample_years)` applied to active-aware Sh produces the honest deployable estimate. Predicted deployable Sh post-deflation: +1.09; observed: +1.11 (0.02 error). Diagnostic v2 with built-in deflation now produces honest predictions.

    - **`last_hour_month_end_ndx_v2_vix_gated`** (Pool B — resurrecting a REJECTed strategy): REJECT 8/10. Gate works decisively (mechanism falsification PASSES across direction-gap +0.84, bootstrap CI [+2.06, +40.48] excluding zero, placebo clean +0.12 bp). Gated full Sh +0.64 vs parent +0.29 = +0.35 lift. Failed: (#7) direction-lock pre-commit didn't have an exception for empty regimes — W2 2021-2022 had zero CALM events (VIX was elevated the whole regime, gate correctly excluded it) but the criterion mechanically fails on "no data"; (#10) deflated Sh +0.04 vs +0.20 bar — at n=17 with 20-trial diagnostic selection bias, Bailey-LopezdePrado deflation eats ~0.60 Sh.

    **Both REJECTs are real-signal small-sample-defeated**, not signal-absent. Pattern: regime-gated subsets of multi-year strategies typically have 15-30 events per gated regime — exactly the n-range where deflated-Sh penalty combined with bootstrap CI width is hardest to clear, even when the underlying mechanism is real. At n=30+ both v2s would plausibly clear their respective bars; current data (7-8y at 4-12 events/yr × 30-40% regime coverage) doesn't support it yet.

    **Operational consequences for the regime-book research line**:
    - **The diagnostic methodology is empirically validated** (predicted v2 Sh within 0.02-0.04 of observed). Keep `regime_classifier_diagnostic` as repo infrastructure for future use.
    - **Calibration rule for future v2 theses**: deploy lift target = active-aware regime Sh × `sqrt(regime_years / full_sample_years)`. Set Phase 2 pass bars to this calibrated estimate, NOT the raw active-aware diagnostic number.
    - **Direction-lock criterion needs an empty-regime exception**: if a regime has zero events of the strategy's cadence within the sample, exclude it from the lock check rather than mechanically failing. The gate is *correctly* excluding the regime; the criterion shouldn't penalize that.
    - **Regime-gated v2 attempts at current sample sizes (<10y) are deploy-marginal at best**. The decisive bar (deflated Sh) doesn't clear unless either (a) sample grows to n≥30 per regime by natural data accumulation (~2027-2028 calendar timeline for both REJECTs), or (b) the strategy has unusually concentrated active-period Sharpe that produces large enough magnitude to overcome deflation hit (uncommon — most mechanisms in the repo have moderate Sharpe distribution).
    - **Pool B resurrect line specifically**: only 1 of 2 Pool B candidates loaded with sufficient n (opex_pin_fade fires only 7 times in 7y in deploy config — too sparse). Expansion to earnings_continuation_mag7, cfd_wed_rollover_eurusd, pre_fomc_drift_eurusd is v3 work — each has its own demo-interface adaptation cost (~30-45 min each). Expected hit rate: ~1 deploy per 5-7 resurrect attempts given the small-sample-deflation pattern, so v3 Pool B expansion is positive-EV but slow.

    **Project-level lesson**: the regime-book vision is empirically supported (mechanism signal exists, gates discriminate) but deploy-undelivered at currently-available data. Bank the methodology (lessons #74-#76 + diagnostic infrastructure) for ~12-18 month maturation. Re-run diagnostic + v2 retests when sample sizes have grown. Methodology is sound; harvest is on calendar-natural sample accumulation. Pairs with lesson #29 (walk-forward as binding for TSMOM): both lessons point at sample-size as the binding constraint for deploy-grade Sharpe estimation on retail-cadence strategies. The strategies with > 100/yr cadence (orb_dax, xau_br_h1, xau_br_m15) don't have this issue; the strategies with ≤ 50/yr cadence (event_calendar sub-events, xau_session, quarter_end_xau_short, pre_boj_drift, the v2 attempts) all face it. The regime line concentrates the problem because gating cuts cadence further.

77. **Same-bar look-ahead at entry (decision uses close[N], entry at open[N]) is the highest-EV bug class to audit on every new intraday strategy — the existing null-check / cost-zero / walk-forward safeguards CANNOT detect it, only a prior-violation can (2026-05-27).** `fra40_mid_morning_momentum` was reported PASS (decisive) Sh +2.05 / dir-gap +5.20 / WF OOS mean +2.90 in its first run; on review the entry was `entry_px = day_open[entry_i]` where `entry_i = first_bar_i + measure_bars` was the SAME bar whose close was used to form the signal (`morning_end_px = day_close[first_bar_i + measure_bars]`). `open[N]` precedes `close[N]` by ~5 minutes in 5-min bar data, so entry was placed 5 minutes before the signal could form. Fix: shift entry to `open[first_bar_i + measure_bars + 1]` (matches the orb_demo convention at [experiments/_live/orb/orb_demo.py:326](../experiments/_live/orb/orb_demo.py#L326), `entry_px = day_open[i + 1]`). With the fix the strategy is REJECT (Sh −0.17, MDD −29%, 3 KILL criteria fail).

    **Why none of the existing safeguards caught it**:
    - **Fade null-check**: the look-ahead inflates BOTH momentum (positive) and fade (negative) by the same magnitude. dir-gap went from +5.20 (buggy) to +0.79 (fixed) — still nominally PASS the +0.30 threshold even after the fix. The null-check measures *directional content*, which the bug doesn't fake.
    - **Cost-zero diagnostic** (lesson #26): leak inflates both gross and net, so cost-zero stayed positive in both buggy and fixed runs. Doesn't distinguish bug from edge.
    - **Walk-forward**: the leak is uniform across time, so OOS Sh ≈ IS Sh on every split. The "suspiciously smooth" WF result looked *more* confirmatory than a noisy split.
    - **Cost sensitivity sweep**: scales linearly with the leak in both directions, looks plausible.

    The only soft signal the bug existed was the **prior-violation**: the thesis explicitly predicted Sh 0.30-0.50 (smaller than orb_dax's +0.76 due to the 1-2h CFD-data lag). The result was Sh +2.05 — 4-7× the prior, 2.7× the parent. The author rationalized this in the verdict ("counter-intuitively stronger... noise filter") rather than treating "way better than predicted" as the obvious "your math is wrong" signal it was. Pairs with the existing repo discipline of writing fail conditions BEFORE running the backtest: the prior magnitude is part of the pre-commit, and a *positive* result that violates the upside of the prior is also a fail condition. If observed Sh > 2 × prior-midpoint, audit the code before celebrating.

    **Operational rule (binding, intraday strategies)**:
    1. **The audit checklist**: on every new intraday simulator, before reporting any numbers, confirm `entry_bar_index >= signal_bar_index + 1` (entry uses data strictly later than what formed the signal). Same rule for stop/exit triggers — `exit_check_index > signal_index` for any condition checked intra-bar. Default to `entry_px = open[signal_bar + 1]`; only deviate with an explicit comment explaining why.
    2. **The prior-violation tripwire**: any result with observed Sharpe > 2 × pre-committed prior-midpoint triggers a mandatory code audit before the verdict is written. Treat "way too good" as suspect, symmetric with "way too bad." (The bias is to celebrate upside surprises; this rule counters it.)
    3. **The known-direction sibling check**: when a strategy is the horizontal transplant of a deployed one (here: orb_dax → fra40), and the new variant reports a Sharpe that exceeds the parent by >50%, audit the entry/exit timing in particular — the differences from the parent should *not* be magnitude-multiplying.

    **Methodological consequence**: add to the per-strategy thesis-doc template a "Bug-audit checklist" line under Fail conditions, with the three rules above. Companion to lessons #26 (cost-zero diagnostic distinguishes signal-absent from cost-eaten) and #56 (regime-stable vs regime-conditional) — those address *real-edge interpretation*; lesson #77 addresses *fake-edge detection*. The repo's existing safeguards are well-designed for the former and blind to the latter.

78. **Paired cross-venue experiments at thesis time produce sharper mechanistic falsification than single-venue runs + post-hoc cross-checks — the failure SHAPES diverge informatively when both venues fail (2026-05-27).** `eu_close_auction_fade` REJECT decisive on both FRA40 (Euronext) and GER40 (Xetra) at the 17:30 CET close-auction-print post-print FADE thesis. The structurally interesting finding is *how* they failed differently: FRA40 FADE Sh −1.47 with dir-gap **−0.55** (MOMENTUM −0.93 less bad — sign-INVERTED, weak continuation) and cost-zero FADE −0.27 (cost-zero MOMENTUM ≈ +0.27 mirror) — auction print **continues** per Bogousslavsky-Muravyev (2023), but at sub-cost magnitude. GER40 FADE Sh −0.23, dir-gap **+0.09** (both directions ~0), cost-zero +0.05 — **no directional content** at retail M5. Same paired hypothesis; two distinct failure shapes (cost-bound continuation vs signal-absence) that map back to specific venue microstructure: Euronext continuous-call-phase allows in-window order modification (price discovery is gradual, informational) while Xetra single-price call with random end (17:30 +0-30s) is harder to game (print is a clearing event, less informational continuation). A single-venue run on either instrument would have produced REJECT without surfacing the asymmetry; the paired design forces the mechanism-interpretation step to explain *both* failures simultaneously, which is what distinguishes "cost-eaten real edge" (FRA40) from "no edge" (GER40). Operational rule for future cross-venue mechanism families (EU indices, ASEAN sessions, EM-EQ pairs): **design as paired/triplet experiments at thesis-doc time, with a pre-committed cross-venue gate**: (a) both PASS same-sign → strongest deploy candidate; (b) both PASS opposite-sign → REJECT, one is curve-fit; (c) one PASS one FAIL → MARGINAL venue-specific (likely confound); (d) both FAIL same-shape → mechanism absent; (e) both FAIL different-shape (this experiment) → REJECT but mechanistically informative — bank the asymmetry-interpretation as a structural finding. Mirrors lesson #15 (cross-instrument fade test) at the venue-pair level, and complements lesson #71 (ORB is venue-specific) by formalizing the paired-design pattern as a thesis-time choice, not a post-hoc transplant check. **Additional family-level finding**: this experiment is the **fourth independent close-window mechanism REJECT** at retail M5 CFD friction on European indices — pre-close drift (dax/pre_auction, sign-inverted), MOC-imbalance drift (preclose_drift SPX/NDX), EOD leverage unwind (eod_unwind 4 venues), and now post-auction overshoot fade (this experiment, FRA40+GER40). Combined evidence: **retail M5 + 1-1.5pt CFD cost is the binding constraint on every flavor of close-window mechanism so far tested**. The mechanisms exist (cost-zero values are bounded and not symmetrically zero), but no flavor has produced a cost-clearing magnitude on this venue/frequency. Pre-tombstone any further close-window thesis on European-index M5 unless it provides (a) a fundamentally novel mechanism not yet enumerated above, AND (b) a Phase-1-prior gross magnitude > 3× cost-floor (per lesson #75 cost-headroom rule). The next-meaningful close-window experiment would need to move to M1 with sub-second auction-print proxies, or move to non-CFD execution (cash equities at 1-2bp commission) — both are infrastructure changes, not strategy changes.

79. **Scheduled macro events split into two mechanistic families — INFORMATION-RESOLUTION events (extractable LONG drift on NDX) and SUPPLY-CLEARING events (no extractable drift). Treasury auctions are decisively supply-clearing; pre-tombstone the family without Phase 2 (2026-05-27).** `treasury_auction_drift` REJECT decisive on both NDX100 and SPX500 (n=775 coupon auctions 2019-2026, paired-instrument design per lesson #78). Phase 1 calendar-only test: LONG gross +0.70 bp NDX / +0.69 bp SPX vs placebo (non-auction Wed/Thu 13:00 ET) +3.40 / +2.77 bp — **placebo is LARGER than event on both instruments**, t-stat (event vs placebo) = -0.90 / -0.89. |magnitude| identical event-vs-placebo (34.2/32.7 NDX; 23.9/23.0 SPX) — no vol-shock either. Phase 2 (bid-to-cover z-score outcome-conditioning): negative-Sh on both instruments at all thresholds (NDX ann_Sh -0.18 to -0.21; SPX -0.46 to -0.67). Per-tenor sweep (2y/3y/5y/7y/10y/20y/30y): no tenor clears |t| > 1.5; 7y and 20y are mildly positive but spurious under Bonferroni adjustment for the 7-tenor multiple test. Cost-zero NDX ann_Sh +0.15 / SPX +0.20 — **below the +0.30 deploy bar even at zero friction**, signal-absent per lesson #26's cost-zero diagnostic.

    **Two-family taxonomy of scheduled macro events** (operational pre-tombstone tool for future event-drift theses):
    1. **INFORMATION-RESOLUTION events**: produce a measurable surprise relative to consensus/pre-event-pricing, resolve uncertainty about the rate-path or growth-shock. Equity reaction is real and extractable at retail M5 because the post-print discovery period is non-trivial (minutes-to-hours for full equity re-pricing). Members: FOMC (deployed via `event_calendar`), CPI (deployed), NFP (deployed), Retail Sales (deployed), pre-BoJ MPM (`pre_boj_drift` PASS-watchlist). Common shape: (a) consensus published, (b) print = single number released at fixed time, (c) measurable surprise distribution (e.g., CPI surprise std ~0.15pp), (d) NDX/equity reaction continues 30-90 min post-print.
    2. **SUPPLY-CLEARING events**: produce a price (the auction clearing yield, or the OPEC quota number) but the price resolves a supply-demand question, not an information question. The auction IS the demand revelation; once it clears, there's nothing left to discover. Equity reaction is sub-second (HFT closes the bond→equity-duration loop in <5s) and zero at retail M5. Members confirmed REJECT: Treasury coupon auctions (this experiment), `pre_pce_drift` (PCE = confirming-read of CPI, lesson #-13), `pre_ppi_drift` (second-tier release, lesson #65), `pre_natgas_eia` (commodity-on-own-fundamental, lesson #-12). Common shape: (a) no clean consensus (auctions have WI but it adjusts continuously into the print; OPEC has whisper numbers but no formal consensus), (b) print = supply/demand clearing event, not surprise revelation, (c) institutional players have already priced the modal outcome via positioning, (d) HFT-arbed sub-M5 timescales.

    **Operational rule (binding for future macro-event-drift theses, Phase 0 gate before any Phase 1 simulator)**: classify the proposed event into INFORMATION-RESOLUTION vs SUPPLY-CLEARING using three diagnostic questions:
    - Q1: Is there a published pre-event consensus number that the print can MISS? (Yes → INFO; No → SUPPLY)
    - Q2: Does the print resolve uncertainty about a forward-looking quantity (rate path, growth, inflation trajectory)? (Yes → INFO; No → SUPPLY)
    - Q3: Has the deployed `event_calendar` book added the event in 2020-2026? (Yes → INFO already validated; No → check Q1+Q2 to decide)

    If SUPPLY-CLEARING, do NOT run Phase 1 — pre-tombstone. The expected outcome is the pattern observed here (placebo ≥ event, t-stat near zero, cost-zero Sh < +0.30). Future candidates this rule pre-tombstones: weekly EIA crude (`pre_natgas_eia` confirmed SUPPLY-pattern on NG; crude likely same), CFTC COT report (Friday 15:30 ET — supply-clearing positioning revelation), Treasury Bill auctions (weekly, even less surprise than coupon auctions), Fed RRP usage daily print (supply-clearing operational metric).

    **Methodological consequence**: the FOMC/CPI/NFP/RS/BoJ deploys are NOT a generalization to all scheduled US-macro events — they are specifically the subset that satisfies INFORMATION-RESOLUTION criteria. The pre-PCE rejection (#-13) was framed as "PCE is confirming-read of CPI"; the broader frame is now "PCE is SUPPLY-CLEARING (residual revelation after CPI has already moved the consensus)" — same outcome, sharper category. The two-family taxonomy is the missing structural rule that organizes lessons #56, #-12, #-13, #65 alongside this experiment. Pairs with lesson #75 (cost-headroom 3× rule) — supply-clearing events ALSO tend to have gross magnitude below 3× cost-headroom on retail venues, providing a second pre-tombstone signal. **Project-level lesson**: the macro-event-drift book is now near-saturated at 4 deploys + 1 watchlist; further expansion requires identifying *new INFORMATION-RESOLUTION events*, not generic "scheduled macro release." Candidate searches (`pre_ism_drift`, `pre_jolts_drift`, `pre_consumer_confidence_drift`, `pre_durable_goods_drift`) should pre-classify each event via Q1-Q3 before any Phase 2 cycle. Pre-tombstone any SUPPLY-CLEARING candidate (Treasury bills, EIA, CFTC, RRP) at thesis time.

80. **MT5 Python's `MqlRates.time` is broker-server-local-time-as-int, NOT real-UTC Unix epoch — every CSV fetched via `mt5_fetch.py` before 2026-05-28 is mislabeled by the broker offset; localize as broker tz then convert to UTC before storing (2026-05-28).** Surfaced by an empirical mismatch between `mt5.symbol_info_tick('EURUSD').time` interpreted as UTC (`17:14`) versus real UTC at the same moment (`14:14`) — a clean 3h offset matching the Eightcap Athens (EEST GMT+3) broker server clock. The `time` field is documented as "seconds since 1970 in server time" but the practical interpretation matters: the integer is `epoch_seconds_at_server_local_midnight`, NOT real Unix seconds. `pd.to_datetime(df['time'], unit='s', utc=True)` therefore produces labels that visually look like UTC (`2026-04-21 10:45:00+00:00`) but are actually broker-local clock values. **Verification chain**: live ORB-DAX-L deal at broker 10:45 on 2026-04-21 entered at 24591.93; the local CSV bar at `2026-04-21 10:45:00+00:00` has open 24588.93 (matches live within spread); the bar at `2026-04-21 07:45:00+00:00` (the real-UTC moment of the broker-10:45 event) has open 24530.93 (does NOT match) — proving the CSV label is broker-time not UTC. **Operational fix (mandatory for any MT5-sourced CSV)**: `pd.to_datetime(df['time'], unit='s').dt.tz_localize('Europe/Athens', ambiguous='infer', nonexistent='shift_forward').dt.tz_convert('UTC')`. The `ambiguous='infer'` handles DST fall-back (one bar/year exists twice); `nonexistent='shift_forward'` handles spring-forward (one hour/year vanishes). The broker tz is overridable via `MT5_BROKER_TZ` env var for non-Eightcap servers. **Project-level impact**: every research result computed pre-2026-05-28 was on bars from a clock-shifted window. For `orb_dax` specifically, the sim "ORB 09:00-09:30 Berlin" was the bar labeled `07:00-07:30 UTC` in CSV which is actually broker `07:00-07:30` = real Berlin `06:00-06:30 CEST` = **DAX pre-market futures**, not cash open. The +0.76 Sharpe research result describes a pre-market-ORB strategy, NOT the cash-open-ORB strategy the live EA trades (the live EA uses broker time directly via `InpSessionStartHour=10` = broker 10:00 = real DAX cash open, so live is correct; only the research is mis-aligned). Similar shifts apply to every strategy that does `tz_convert(local_tz)` on the loaded CSV (xau_break_retest, lunch_fade) and to any sim that defines a UTC session window (xau_session, event_calendar — their EAs use real UTC via `TimeGMT()` correctly, but the sim CSV is shifted). **Mandatory re-validation set**: 7 deployed strategies + 1 watchlist + `portfolio_risk_parity` = 9 must-rerun. Rejected experiments (~69) stay rejected — re-running them is nice-to-have for completeness but doesn't change deployment posture. **Data-side fix**: re-fetched all ~150 MT5-corrupted CSVs in `ohlc_data/` via patched `mt5_fetch.py`; older Yahoo/Tiingo/FRED CSVs are uncorrupted and stay as-is. **Going-forward rule (binding)**: every fetcher writes `source` and `fetched_at` columns into the CSV schema so source provenance is queryable; `mt5_fetch.py` localizes-then-converts every bar; any future broker tz change requires only updating the `MT5_BROKER_TZ` env var. **MT5 Python gotcha sub-finding**: the same bug exists on every other MT5 Python integration in the stack (`scripts/mt5_probe_depth.py`, `scripts/mt5_bridge.py` in the adjacent `datalake-api` repo — the latter never deployed so non-blocking, but the same fix must be applied before it goes live). **Lake-side fix deferred** — datalake will be reconciled in a separate pass by uploading the repaired local CSVs (`ON CONFLICT UPDATE` does NOT collapse old-broker-label rows with new-UTC-label rows because timestamps differ; a DELETE-then-UPLOAD is required). **Companion to lesson #5 (research-to-live haircut framing)**: this finding is a separate axis of research-vs-live divergence — the bars themselves were sampled from a different window than the live strategy trades. The 10-25% haircut prior in #5 assumes both sides use the same bars; this lesson re-establishes that baseline assumption.

81. **Stop-geometry guard: every level-based-entry simulator must reject trades where the entry bar opens past the stop level, or it bakes in phantom alpha that MT5 wouldn't execute (2026-05-28).** `xau_imbalance` family REJECT decisive after a visual single-trade chart-audit surfaced the bug. Mechanism: 3-bar FVG retest fade on M5/M15 in NY-AM session. Pre-fix Phase 2+3 looked extraordinary — XAU M5 Sh +1.10 / W3 +1.90 / dir-gap +5.30 / 4 instruments cross-validated PASS / 5/5 Phase 3 controls PASS / inter-strategy correlation matrix clean / spread audit confirmed real broker spreads ≤ 5 bp of price for XAU/NDX/SPX. Cross-instrument family produced book Sh +4.14 at 2x leverage (Calmar +20). **The user pushed back hard ("doesn't seem real"), requested visual verification of last-week trades. Audit of a single trade (XAU M5 2026-05-28 14:05 LONG entry 4424.57 / stop 4425.64 / "stop fill" at 4425.64 = +0.024% recorded win) surfaced impossible geometry: stop ABOVE entry for a LONG.** Root cause: when the retest bar (k) overshoots `fvg_hi` by a large margin, the entry bar (k+1) can open BELOW the original `stop_level = fvg_lo - 0.5 × fvg_width`. The simulator's exit-check `if bull and l[k] <= stop_level` then fires on the entry bar itself (because entry-bar low is already below stop_level), and records `exit_price = stop_level` as a "conservative stop fill" — a tiny phantom WIN of (stop_level − entry_price) / entry_price. **In live MT5 the trade would never execute as written**: `OrderSend` rejects SL above entry for long (`invalid stops`, retcode `TRADE_RETCODE_INVALID_STOPS`). The simulator was systematically over-counting these phantom-stop fills. **Patch**: after computing `entry_price` and `stop_level`, before the hold loop, add:
```python
if bull and entry_price <= stop_level:
    i += 1; continue
if not bull and entry_price >= stop_level:
    i += 1; continue
```
**Impact magnitude**: post-patch backtest collapse across the family — XAU M5 Sh +1.10 → +0.56, XAU M15 +1.04 → +0.66, NDX M15 +2.43 → +0.12 (FAIL), SPX M5 +1.26 → -0.13 (FAIL). 12-18% of trades per slot were phantom-stop fills carrying most of the apparent edge. After fix: XAU M5/M15 individually clear the +0.30 deploy bar but at portfolio-Sharpe-additive math (need $S_{new} > S_{book} \cdot \rho$ ≈ 0.57 for +2.85-Sh book + 0.20 corr) they sit right at break-even; with realistic 30-50% live haircut they fall below. **Decisive**: book Sharpe DROPS from +2.85 (without imbalance family) to +2.62 (with the 2 surviving XAU slots) — the family is net-negative even after fix. Family TOMBSTONED 2026-05-28; live service disabled before any trade fired (deployed 18:30 UTC after market close, removed same evening). **Operational rule (binding for any future level-based / pattern-based / breakout / FVG / order-block / SR-retest simulator)**: pre-commit a geometry-validity check on every new entry — `entry_price` must be on the correct side of `stop_price` for the trade direction. Skip-without-counting if violated. Pairs with lesson #77 (same-bar look-ahead) as the two classes of "simulator records a phantom edge that the broker would reject or the data couldn't produce": (a) #77 = decision uses bar's close, entry at bar's open (5-min leak); (b) #81 = entry price geometrically invalid against stop. Both are SILENT in standard null-check / cost-zero / walk-forward / regime / bootstrap diagnostics — they require either prior-violation cross-check (lesson #77 trigger) or visual trade-by-trade audit (lesson #81 trigger). **Visual audit primitive added to standard kit**: `experiments/<name>/last_week_trades.py` — prints every trade in a recent window with entry/stop/exit details suitable for chart cross-check; should be ship-built for every level-based deploy candidate during Phase 2-3. **Project-level lesson on user-loop signal**: the user's explicit "doesn't seem real" + "can we look at the past week" was the load-bearing event that broke this bug open. Subscriber-skepticism of "too good to be true" backtest numbers is itself a research signal — when a thesis-doc records +2.43 Sharpe with all kill criteria PASS, the next move is `last_week_trades.py` + visual chart audit, BEFORE deploy. This is a structural improvement to the validation pipeline. **The user has now caught two production-grade phantom-alpha bugs**: lesson #77 (same-bar-look-ahead on `fra40_mid_morning_momentum`) and lesson #81 (this — geometry-bug on `xau_imbalance` family). Both took longer to surface than they should have. Pre-commit Phase 1.5: visual sanity-check on a recent-week trade set, before any Phase 2+3 ramp-up.

**Same-day extension audit (2026-05-28)**: ran the geometry-bug check across the entire then-deployed book. Findings:
- `xau_break_retest_h1` (deployed 2026-05-26, never fired live): **216/878 trades = 24.6%** violate `entry > stop` for FADE; pre-guard Sh **+1.46**, post-guard Sh **-0.74**. The +40.28% phantom alpha was 100%+ of the apparent edge. Tombstoned same day; sibling M15 (already shelved) shares the same buggy `_enter_and_exit` simulator and the patch was applied there too.
- `orb_dax`, `lunch_fade`, `xau_session`, `event_calendar` (CPI/NFP/RS/FOMC), `quarter_end_xau_short`, `pre_boj_drift`: **all CLEAN**. The intraday breakout (orb_dax) uses `stop = entry ± positive_constant` (geometrically impossible to violate). The other five use no level-based stops in the demo — entries and exits are pre-committed time-of-day bar opens/closes, so the bug class doesn't apply. Live EAs that add synthetic %-stops (lunch_fade 1%, xau_session 1.5%, quarter_end_xau_short 2%) place those stops as `entry × (1 ± k)` which is always geometrically valid.
- Minor hygiene fix bundled in: the `closest_bar_close` picker in the event-calendar demos defaulted to a 30-min tolerance window — tightened to 5 min to eliminate any theoretical forward-fill lookahead in pathological bar-gap edge cases. Re-run book numbers unchanged (confirming it was a pure hygiene fix not a load-bearing one).

**Heuristic for the geometry-bug class going forward**: ANY simulator where the stop level is computed from a structural reference (swing, gap edge, OR boundary, FVG edge, channel) rather than as `entry ± k×something_positive` needs the guard. The two failure modes that produce invalid geometry are: (a) entry bar opens past the structural stop (imbalance family), and (b) entry happens at the close of a violent reversal bar that already exceeds the structural stop (xau_break_retest family). The guard pattern is invariant: `skip if (long and entry <= stop) or (short and entry >= stop)`.

82. **Four-direction screening pass (2026-05-28) — three generalizable rules from one "iterate over a bunch of hypotheses" session.** Screened structural-flow-audit-v2 + non-Fed-CB-drift + US-macro-3-axis + a fresh ORB transplant. Outcomes: 1 candidate (`triple_witch_close_short`, Phase-2 MARGINAL), 1 data-blocked, 2 REJECT. The transferable lessons:
    - **(a) Screen cost-floor calibration is load-bearing — a wrong floor silently buries real cells.** The v1 structural_flow_audit used 4-7 bp cost floors (XAU 7, NDX 4) that were ~10× too high; the bug made the screen STRICTER than intended so it produced no false positives but DID hide `triple_witch_close_short` in WEAK. Re-running with corrected ~0.5-2 bp all-in floors (lesson #32 levels) promoted it to MEDIUM → Phase-2 12/12. **Rule**: any |t|×sign(cost_headroom)-style screen must use measured all-in RT costs (per lesson #32, never carried-across-asset-class assumptions); re-run historical screens whenever the cost model is corrected — buried cells, not just false positives, are at stake.
    - **(b) For any LONG-biased overnight/multi-hour EQUITY window in a bull regime, the direction-null is INSUFFICIENT — a non-event-weekday placebo is mandatory.** `pre_jobless_claims_drift` (NDX 24h-pre-Thursday-08:30ET) showed holdout LONG Sh +0.92 (t+1.68, n=173) and dir-gap +0.77 (SHORT loses) — both "pass". But the non-Thursday Mon/Tue/Wed placebo drifted +0.175% vs the Thursday +0.192% (claims-specific gap +0.017%): the entire holdout edge was 2023-26 NDX bull-beta captured by a long-biased overnight window, NOT a claims-anticipation flow. The dir-gap only proves "NDX went up," not "the event matters." Same confound `pre_pce_drift`'s placebo caught (lesson #13 family). **Rule**: the 3-axis macro framework (#-13: mid-month · non-Friday · first-read) correctly pre-tombstones weekly/confirming/lagged events; when one is tested anyway, the binding gate is event-day-mean − same-window-non-event-weekday-mean, not the long/short dir-gap.
    - **(c) Triple-witch directional close-drift is a DISTINCT mechanism from the tombstoned OPEX-pin MR family — it does NOT inherit the post-2022 sign-inversion.** `opex_pin_fade`/`opex_pin_singlestock` (MR toward max-pain strike) sign-INVERTED in the 2023-26 holdout (#-5/#-11/#43). The triple-witch closing-hour SHORT (dealer ITM-call long-stock-hedge unwind into the settle = directional sell-flow) stays SHORT-positive in W3 on all three large-options-complex indices (SPX W3 +6.8 / NDX +8.8 / UK100 +5.3), dir-gap +1.35, placebo-clean. **Rule**: "OPEX-adjacent" is not a blanket tombstone — separate pin-MR (dead) from expiry-hedge-unwind-directional (alive-but-marginal); the holdout-sign-flip test is the discriminator. Caveat: the surviving candidate is MARGINAL (deflated Sh +0.13 at 28 cells/n=29; recent-half leans on one event) — sparse-event + selection-bias keep it watch-list, not deploy.
    - **(d) ORB is DAX/Xetra-specific, confirmed a 2nd time at the single-venue-auction level.** FRA40 (Euronext) and UK100 (LSE) both have literal single-venue cash auctions (the lesson #71 prerequisite) yet baseline ORB REJECTS (FRA40 Sh −0.42 / fade +0.01 = no directional content; UK100 −0.97). Corroborates `orb_eustx50` and lesson #19 (refinement paths instrument-specific): single-venue auction is NECESSARY but NOT SUFFICIENT — DAX's clean 3h opening-impulse momentum is a venue-specific property CAC/FTSE lack. Pre-tombstone naive ORB transplants to other single-venue European indices.

83. **Second fresh-idea slate (2026-05-28) — a 3rd phantom-alpha catch and a clean "survives-the-placebo" winner; the placebo-gap is the single discriminator separating real sparse-event edges from bull-beta artifacts.** Screened three fresh ideas: one graduated to the private book (details in `experiments/_live/`), two REJECT (below).
    - **(a) NR7-GER40 — 3rd phantom-alpha catch, "gap-through fill" class (lesson #81).** First-pass screen showed continuation Sh **+2.69** / holdout +4.29 / dir-gap +5.59 — which tripped the lesson-#77 "observed Sh > 2× prior ⇒ mandatory code audit" rule. The bug: on NR7 (tight-range) days the next session often gaps through the NR7 level at the open, but the sim filled at the exact level `nr_hi`/`nr_lo`, booking (open − level) as phantom profit. Gap-aware fill (fill at the open when the bar opens past the level) collapsed it to Sh **+0.04** (cost-zero +0.16 = signal-absent per #26). **Now three independent phantom-alpha catches** (#77 same-bar-leak `fra40_mid_morning_momentum`; #81 stop-geometry `xau_imbalance`/`xau_break_retest`; this gap-fill `nr7`). **Generalized guard for ANY level/stop-entry intraday sim**: a fill at a structural level is only valid if the triggering bar did NOT open past it; otherwise fill at the open. Add the gap-through counter to every breakout/stop simulator and print it — a high gap-through % with a great Sharpe is the tell.
    - **(b) The placebo-gap is the single discriminator separating a real sparse-event edge from a bull-beta artifact.** A long-biased equity-session candidate can look alive on holdout Sharpe yet be pure beta: jobless-claims (#82b) had a +0.92 holdout Sh that collapsed to a +0.017% event-specific gap once measured against matched non-event weekdays. A genuine edge instead retains a large `event-mean − matched-non-event-mean` gap. **Rule reinforced (binding for any sparse long-biased equity-session strategy)**: the gate is `event-mean − matched-non-event-mean` AND the deflated Sharpe (selection-adjusted) — NOT raw Sharpe and NOT the long/short dir-gap. One 2026-05-28 candidate cleared this decisively plus a paired walk-forward book-integration test (lesson #36) and graduated to the private book (`experiments/_live/`); a higher-cadence sparse edge also tightens its bootstrap CI fastest, so a lone bootstrap-lower miss at small n is a sample-width issue, not a signal issue.
    - **(c) Mega-cap-earnings INDEX drift REJECT, but with a flagged residual.** NDX reaction-day SHORT on Mag7 report days had Sh +0.77 (event-specific gap −24.9 bp) — suggestive but murky mechanism (likely AH-gap-then-fade) and earnings-family-adjacent; post-cluster relief weak (Sh 0.15–0.42). Not promotable without a clean mechanism + weekday-placebo + holdout. The index-aggregate framing did not produce something distinct enough from the tombstoned single-stock earnings work.

84. **Short-side book-enhancement slate (2026-05-28) — the durable structural shorts are quarter-aligned forced-rebalance, NOT monthly-options-microstructure; and sleeving a sparse-event strategy diversifies by JURISDICTION, not by correlated same-region indices.** Book is heavily long-biased (orb_dax LONG-only, lunch_fade/xau_session/pre_boj LONG, event_calendar 3-of-4 LONG); only genuine shorts are `quarter_end_xau_short` (4/yr) + NFP-leg. Slate tested the two best fresh SHORT candidates + a sleeve of the existing one.
    - **(a) `vix_soq_short` REJECT — one-window-wonder, holdout-decayed.** Best-cadence structural short the v2 audit offered (SPX 08:30-09:30 ET pre-VIX-SOQ, ~12/yr, n=88, MEDIUM tier). 9/12 mechanically but the 3 fails are the deploy-binding ones: W3 holdout −0.43 bp (Sh −0.06), all-regimes, bootstrap-lower −1.37. Edge is entirely pre-2023 (W1 +5.2/W2 +13.6/W3 −0.4), WF H2 +0.2. Griffin-Shams (2018) VIX-settlement effect arbed away post-publication + post-0DTE (academic half-life, lesson #7). Overrode the nominal "MARGINAL 9/12" label with the qualitative reading that decay-fails ≠ sample-width-fails (contrast a sample-width-only miss that still has a live-positive holdout) — the W3-holdout-binding rule (#16/#31) makes a net-negative-holdout one-window-wonder non-deployable, period (cf. btc_volbreak, retail_overshoot_fade).
    - **(b) `letf_eod_rebalance` REJECT at Phase-0 screen.** Leveraged-ETF EOD rebalance continuation (Cheng-Madhavan 2009): SHORT SPX/NDX last 30 min conditioned on a large intraday down-move. Continuation is real but sub-cost (zero-cost +1.2-1.6 bp, dir-gap +1.0-1.3 at shallow thresholds), W3 holdout inverts at every tradeable threshold, and at the DEEPEST down-move (−1.5%) the dir-gap flips to −0.80 (close FADES, not continues — 0DTE dip-buying, the intraday image of lesson #34). 5th member of the tombstoned EOD-US-index cluster (eod_unwind/vwap_fade/preclose_drift/last_hour_month_end_ndx). ~30-line screen killed it pre-Phase-2 (lesson #69 cheap-falsifier).
    - **(c) `triple_witch_close_short` SLEEVE (SPX+NDX+UK100 equal-weight) — strict improvement, still NO-PROMOTE; the diversification lesson is the reusable part.** Basket dominates SPX-only on every axis (Sh +0.61→+0.68, boot-low +1.09→+1.90, deflated +0.13→+0.20, MDD −0.58→−0.26%, PF 3.82→4.44, dir-gap +1.35→+1.50) and removes single-event fragility (largest contributor is 2020-03-20 COVID witch +187.9 bp; drop it → mean still +6.86). But deflated +0.20 < the pre-committed +0.30 promote bar — honored the frozen bar (no goalpost-moving), so it stays watch-list, now tracked AS the sleeve. **Reusable methodological rule for ANY sparse-event multi-instrument strategy**: sleeving cannot manufacture independent n (event count is fixed by the calendar) — its ONLY lever is reducing per-event variance, which requires LOW within-event cross-instrument correlation. Here SPX↔NDX = +0.95 within-witch (same US dealer-unwind shock ⇒ near-redundant; adding NDX barely helps) while UK100↔US = +0.47-0.56 (the sole real diversifier). **Diversification of a forced-flow event strategy comes from a different options JURISDICTION, not a second index in the same region.** Pre-commit the within-event correlation matrix before assuming a basket will tighten the deflated/bootstrap gates; if the candidate instruments are same-region same-asset they will be ~0.9+ correlated and the sleeve adds little. Pairs with lesson #2 (low corr ≠ diversifier — but here the inverse: HIGH corr ⇒ no variance reduction).

85. **Crypto perp funding-fade is correctly signed but institutionalised to ~0 post-2022 — funding-arb is the crypto analogue of equity 0DTE-gamma institutionalisation (2026-05-28).** `crypto_funding_fade` REJECT (2/8). First funding-rate experiment in the repo (new data source: Binance `fapi/v1/fundingRate` + 8h klines, BTC 2019-09→ / ETH 2019-11→, now cached in `ohlc_data/{BTCUSDT,ETHUSDT}_{FUNDING,PERP_8H}.csv` via `scripts/binance_funding_fetch.py`). Vessel = Eightcap BTCUSD CFD proxy (exchange funding as signal, CFD bars as trade). Three transferable findings:
    - **(a) Funding extremes ARE a real directional crowding gauge — but the standalone fade is dead in the current regime.** Fade-gap +1.22 (BTC-CFD) / +2.09 (BTC-perp) / +1.48 (ETH-perp); the inverse direction (long-on-high-funding) is a −1.5 to −2.0 Sh catastrophe with −97% MDD. So leaning against crowded-long funding is decisively correctly-signed (not a noise strategy). BUT it is positive only in the 2021-2022 leverage cycle (BTC W2 +0.33 CFD / +0.94 perp) and **negative in the 2023-2026 holdout on both BTC vessels** (W3 −0.79 CFD / −0.85 perp). Cash-and-carry basis desks + funding-arb bots grew enormously after 2021 and now compress the funding premium / front-run the liquidation-cascade fade. **Same post-2022 holdout-decay shape as the 0DTE-MR family (#43, #-5/#-9), reached via a completely different mechanism** — funding-arb compression is crypto's structural analogue of equity 0DTE-gamma institutionalisation. Any future crypto-microstructure thesis must pre-commit the W3 holdout as binding and expect post-2021 arb compression as the default prior.
    - **(b) Methodological — do NOT z-score a signal against its own rolling mean when the thesis depends on the signal's UNCONDITIONAL bias.** The thesis predicted a structural short tilt (>55% short) because funding is positive the large majority of the time. But z-scoring funding vs its rolling-30d mean *removes exactly that positive bias*, so the realised short fraction was 50%, not >55% (criterion 8 FAIL). To earn a bias-dependent directional tilt, threshold on **absolute level / z-vs-zero**, never z-vs-rolling-mean. (An absolute-level short would have bled worse in the 2023-24 melt-up — short-into-parabola — so this wasn't pursued as a rescue; it's a design lesson, not a goalpost move.)
    - **(c) Operational — the 25% MDD bar must be applied to a VOL-TARGETED equity curve for crypto, not raw full-notional.** Raw-notional overlapping 8h crypto trades blow past 25% from volatility alone (here −70%), which is uninformative; Sharpe (bet-size-invariant) is the real arbiter and it fails the holdout regardless of sizing. Future crypto theses: vol-target (à la `btc_trend` 15%) before applying the MDD gate. Cost-zero diagnostic (lesson #6) confirms the residual edge is real-but-tiny: cost-zero ≈ +0.35 → −0.33 at 10 bp → −1.27 at 20 bp (friction-eaten, not signal-absent). Decisive directional content means funding-z is plausibly viable as a *filter/overlay* on another crypto signal, or on a fresher/less-arbed venue (alt-perps, Bybit) — but not as a standalone in the current regime.

86. **A validated multi-instrument ensemble does NOT survive restriction to the broker-tradeable subset when the ensemble's robustness lived in the un-tradeable names — broker-tradeability AND the per-instrument CFD-swap ceiling are Phase-0 deployability gates, not afterthoughts (2026-05-29).** `softs_ensemble` was VALIDATED_NO_DEPLOY (6-name Yahoo-continuous basket, Sh 0.85 / holdout 1.44, Phases 2-7 PASS), originally blocked only on "Eightcap D1 feed too short to re-validate." A live-book "what can we still add on Eightcap-only" pass re-examined deployability with two Phase-0 gates and found the original blocker understated the problem. **(a) Tradeability (MT5 probe is authoritative, NOT the datalake — lake holds external-source data that says nothing about broker availability):** of the 6 validated names Eightcap carries only cocoa/coffee/cotton/corn; soybean + live-cattle (the two highest-alpha, α +0.29/+0.19) are not offered. **(b) CFD swap ceiling (lesson #59):** live long-side financing probed via `scripts/softs_swap_probe.py` splits the basket — cocoa ~−0.14%/yr and coffee ~+0.13%/yr survive, but cotton ~−16.9% / corn ~−17.2% / wheat ~−11.2% per year are PEAD-redux (swap eats the ~3% gross TSMOM edge). So the only tradeable-AND-swap-survivable cut is a **2-name cocoa+coffee blend**, re-validated on long Yahoo continuous (`COCOA_YF`/`COFFEE_YF`) with the real swap modelled: full Sh +0.59 (clears +0.30 nominally) but **81.8% of return concentrated in the 2023-26 softs bull** (per-window Sh −0.40/−1.14/+0.01/+0.49/+1.67) and **null-gap +0.24 < +0.30** — a `gold_trend`-shape one-window-wonder (lesson #73). REJECT for Eightcap standalone deploy. **The poignant part: cocoa+coffee is genuinely ~zero-correlated to the entire live book (max |corr| 0.018 vs XAU/NDX/GER/JPY), so it would have been an ideal diversifier — but uncorrelated noise is not alpha.** **Operational rule (binding, Phase 0 for any multi-instrument ensemble proposed for deploy):** before counting an ensemble as deployable, intersect its universe with (i) the actual broker symbol list (MT5 probe) and (ii) the per-name CFD-swap-survivability filter, THEN re-validate the *surviving subset* on its own — never assume the validated full-basket Sharpe/robustness transfers to the tradeable residue. The diversification and regime-robustness of an equal-weight ensemble can live disproportionately in a few names; if those are the un-tradeable ones, the deployable cut collapses. Pairs with lesson #59 (CFD-swap ceiling) and #73 (single-instrument/thin-basket retail-CFD TSMOM is usually one-regime passive-beta). Also a clean datalake-vs-broker methodology reminder: lake presence ≠ tradeable (use the MT5 probe), and source-mixing in the lake is real (lake `COCOA` had ~2× a clean Yahoo pull's row count — Yahoo 00:00-UTC + MT5 23:00 D1 stamps not deduping; see `docs/handoffs/`).

87. **A forced-flow calendar mechanism ports across jurisdictions by following the SETTLEMENT TIMING, not the clock — and an event-specific-vs-generic-calendar placebo is the clean inclusion discriminator (2026-05-29).** Widening the US triple-witch-close SHORT sleeve to other large index-options complexes surfaced `jpn225_sq_open_short` — a **13/13 Phase-3 PASS** that is the cleanest member of the whole triple-witch family (clears the bootstrap-lower and all-regime-positive gates the parent SPX-only *failed*: boot-95-low +2.68, regimes W1 +24.9 / W2 +4.9 / W3 +21.8 bp, deflated +0.36, dir-gap +1.81, MDD −1.21%). The transferable methodology: **(a) Window follows settlement, not clock.** US index options are PM-settled → the dealer-hedge-unwind drift is in the **close** hour; the Nikkei quarterly SQ is **open-settled** (2nd Friday) → the same mechanism's drift is **post-open** (09:00-10:00 JST), and the *into-open* window is actually negative (−9 to −18 bp). Eurex EUSTX50 options settle ~midday → its midday window is null/negative, only the generic close-hour OPEX-rebalancing drift is weakly positive (so EUSTX50 is a weak include, US/UK-style close-effect, NOT the options-settlement flow). Blindly copying the US close-hour clock to Japan would have produced a **false negative** (the JPN225 close 14:00-15:00 window is −18.7 bp). **(b) The event-specific-vs-generic-calendar placebo is the discriminator.** Nikkei has a *monthly* SQ (every 2nd Friday) but the futures+options *quarterly* SQ (Mar/Jun/Sep/Dec) is the big one. Same 09:00-10:00 window: quarterly SQ +17.99 bp vs monthly-only +(-2.23) bp → gap +20.22 = the drift is **SQ-specific flow, not generic Tokyo-open momentum**. This placebo (the same window on the *non-event* instances of the same calendar) is the strongest available falsification for any "drift in window X around recurring event Y" thesis — stronger than a random-day placebo because it controls for the time-of-day microstructure exactly. **(c) Widening buys n retrospectively.** Cross-jurisdiction breadth on a sparse calendar mechanism (4/yr) pulls effective-n from history *now* instead of waiting years — the widened triple-witch panel went 29→58 distinct dates and deflated +0.20→+0.43 immediately. **Operational rule:** before declaring a settlement-flow mechanism jurisdiction-specific (or porting it), (i) identify each venue's actual settlement timing and test the settlement-aligned window, not the source venue's clock; (ii) run the event-specific-vs-generic-calendar placebo as the inclusion gate. Pairs with #-20 (structural-flow audit is a productive idea-source) and #84 (sleeve diversification is cross-jurisdiction). `jpn225_sq_open_short` is watch-list (deflated +0.36 < +0.40 deploy bar, n=29 sparse); next quarterly SQ 2026-06-12.

## Open methodological TODOs

These are *cross-experiment* improvements to the validation pipeline itself, not per-strategy work (which lives in STATE.md):

- **Wire statistical battery** (`compute_statistical_report`) into all surviving strategies' validation scripts with honest `n_trials_tested` counts (Deflated Sharpe adjustment for multiple-testing).
- **Add SPY benchmark** (alpha, beta, information ratio, tracking error) to research-side validation scripts — currently only QC live run has this.
- **Codify the CFD-vs-futures Phase-2-gate** for any thesis whose mechanism centers on the overnight/auction transition. Per lesson #22, this gate should be automatic before any refinement work.

## Process

See [WORKFLOW.md](WORKFLOW.md) for the strategy development pipeline with explicit kill criteria at each phase, and [STATE.md](STATE.md) for what's been put through it.
